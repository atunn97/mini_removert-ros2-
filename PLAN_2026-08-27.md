# PLAN phiên 90 phút — 27/08/2026 · đề bài E2 (map tích luỹ)

> Nối tiếp `HANDOFF_2026-08-20.md` mục 9. Repo sạch, đang ở `main`, commit cuối `a7be9af`.
> Dữ liệu đã kiểm: seq 03/04/06/07 đủ `pcd` + `labels` + `calib` + `poses`.

## 0. Kết luận về phạm vi — đọc trước khi bấm giờ

**90 phút KHÔNG đủ để vừa xong E2 vừa gắn vào pipeline chính.** Hai việc đó không cùng
kích cỡ:

- E2 (viết C++ map tích luỹ + đo lại) là **một phiên đầy đủ**, đây là phiên đó.
- Gắn vào FAST-LIO/Go2 còn **3 rào cứng chưa ai đụng** (mục 5). Không rào nào sửa được
  trong 10 phút thừa của phiên này.

Nên plan dưới đây chốt mục tiêu: **E2 chạy được và qua tiêu chí chấm số 1**, đủ để phiên
sau chỉ còn đo rộng rồi mới nói chuyện tích hợp.

Mặc định: **chủ repo tự gõ, Claude hướng dẫn + đọc diff + build + chạy test**
(quy ước CLAUDE.md, xác nhận 17/8). Nếu muốn Claude viết thẳng C++ thì nói rõ đầu phiên —
khi đó block 2 rút còn ~20 phút và block 3 làm được nhiều hơn.

## 1. Chuẩn bị TRƯỚC khi bấm giờ (~5 phút, làm tối nay)

```bash
cd ~/mini_removert && git checkout -b e2-map-tich-luy && colcon build --packages-select mini_removert
```

Build sạch trước để phút đầu phiên không mất vào biên dịch. Không cần đọc lại handoff cũ —
mọi con số cần dùng đã nằm trong file này.

## 2. Thời khoá biểu

| Phút | Việc | Điểm dừng kiểm tra |
|---|---|---|
| 0–5 | Mở repo, xác nhận build sạch | binary chạy được với lệnh mốc |
| 5–20 | **E1** — script chọn nguồn theo thang bậc (Python) | 3 phép thử ở §3 |
| 20–30 | **E2.1** — CMake `filters` + hàm gộp nguồn về hệ scan + voxel 0.2 | `#map_pt` cùng bậc với `src_quality.py` |
| 30–40 | **E2.2** — ground fit MỘT LẦN trên scan, dùng chung hệ số cho map | % ground của scan không đổi so với trước |
| 40–50 | **E2.3** — bỏ vòng lặp map, giữ nguyên tầng voting | build sạch, chạy không crash |
| 50–60 | **E2.4** — in bậc `R` và `#nguồn` thực dùng (tiêu chí 4) | có dòng log đó trong stdout |
| 60–80 | **Đo** theo đúng thứ tự tiêu chí chấm (§4) | tiêu chí 1 là sống/chết |
| 80–90 | Commit + `HANDOFF_2026-08-27.md` ngắn | phiên sau đọc là vào việc được |

## 3. E1 — thang bậc chọn nguồn (5–20′)

Script mới `scripts/select_sources_ladder.py`, **ba bậc và biên là tham số dòng lệnh**
(mục 5b handoff 20/8: chúng đã tune trên toàn bộ 20 scan có trên đĩa, không còn tập sạch
để tune tiếp — nên đừng hard-code):

```
select_sources_ladder.py <poses> <calib> <scan_idx> [--rungs 5:5,10:10,20:10] [--min-src 8]
```

- Logic lọc đã có sẵn, copy nguyên từ `scripts/src_quality.py:select_sources()`:
  `{ j : ||pos(j)-pos(scan)|| <= R và |j-scan| >= t_min và j != scan }`.
- Giữ đúng **contract output** của `select_maps_by_distance.py`: danh sách `map_idx` in ở
  **dòng cuối stdout**, để các script sweep tái dùng được không phải sửa.
- Bậc cuối (`20:10`) chỉ đòi ≥ 1 nguồn. Không bậc nào đạt → **in rõ + exit code ≠ 0**.
  Tuyệt đối KHÔNG im lặng rơi về `t_min=1` (mục 5 handoff 20/8).

Ba phép thử, mỗi cái 5 giây, làm ngay khi viết xong:

| scan | phải ra |
|---|---|
| `06 939` | bậc 1 (`R=5,t=5`) — đây là scan AUC 0.980 |
| `04 150` | rơi xuống bậc 3 — seq04 **0/5 scan** có nguồn ở bậc 1 và 2 (mục 4) |
| `03 217` | rơi xuống bậc 2 hoặc 3 — nó có **đúng 5 nguồn** ở bậc 1, là test biên của `--min-src 8` |

Nếu `03 217` vẫn chọn bậc 1 thì so sánh `>=` với `>` trong điều kiện biên.

## 4. E2 — C++ (20–60′)

### E2.1 · gộp nguồn về hệ scan + voxel

- `CMakeLists.txt:8` thêm `filters` vào `find_package(PCL ... COMPONENTS common io segmentation)`.
  Thiếu nó thì lỗi là `undefined reference to pcl::VoxelGrid<...>::applyFilter` **lúc link**,
  không phải lúc biên dịch — dễ mất 10 phút vì tìm nhầm chỗ.
- Khuôn có sẵn: `src/main.cpp:145-152` (load + pose + transform mỗi map). Sửa đúng một chỗ:
  `relative_pose` hiện là `map_frame.pose.inverse() * scan_frame.pose` (đưa **scan vào hệ map**).
  E2 cần **ngược lại** — `scan_pose.inverse() * (poses[j] * Tr)`, đưa **map về hệ scan**.
  Đối chiếu `scripts/src_quality.py:build_source_cloud()`.
- Nối mọi nguồn vào một cloud rồi `pcl::VoxelGrid` **0.2 m** một lần ở cuối.
- Kiểm: in số điểm map sau voxel, so với cột `#map_pt` mà `src_quality.py` in ra cho cùng
  scan + cùng bộ nguồn. PCL VoxelGrid cũng lấy trọng tâm nên hai số phải **rất sát**, lệch
  nhiều là dấu hiệu sai chiều biến đổi.
- Lưu ý tài nguyên: bộ nguồn có thể lên **116 frame** (seq07/673). ~14 triệu điểm trước
  voxel ≈ 220 MB — chạy được, nhưng load 116 file PCD tốn chục giây. Đừng ngạc nhiên.

### E2.2 · một mặt phẳng ground dùng chung (đề bài C tự tan biến)

- `src/ground_filter/ground_filter.cpp:24` **đã tính `coefficients` rồi vứt đi**. Thêm
  đường trả hệ số ra (out-param hoặc hàm `detectGroundPlane`), cộng một helper
  `maskFromPlane(cloud, coef)` — khoảng 20 dòng, không đụng chữ ký cũ để code hiện tại
  vẫn build.
- Fit trên **scan**, áp mask lên **cả map** (map đã ở hệ scan nên không cần biến đổi mặt phẳng).
- Cạm bẫy: PCL trả pháp tuyến **dấu tuỳ ý**. Chỉ dùng `|n·p + d| <= 0.2`, đừng bao giờ
  dựa vào dấu.
- Việc này **đụng ground** ⇒ theo CLAUDE.md phải chạy `run_seed_sweep.py` trước khi kết luận
  bất cứ điều gì (xem §5 block đo).

### E2.3 · bỏ vòng lặp map, **giữ nguyên tầng voting**

- Scan **giữ nguyên hệ toạ độ của nó**: bỏ hẳn `scan_in_map_frame`, dùng `scan_frame.cloud`
  cho cả `buildRangeImage` lẫn `getDynamicIndices`/`getObservedIndices`. Thứ tự điểm không
  đổi nên `scan_ground_mask` vẫn khớp index.
- **KHÔNG xoá tầng voting** (`src/main.cpp:218-258`) dù giờ chỉ còn 1 map: với một map thì
  `vote_count`/`observed_count` ∈ {0,1}, tỷ lệ 1 > 0.5 vẫn đúng — mà REVERT đa thang vẫn
  chạy qua đúng đường đó. Xoá là mất luôn luật "mọi thang thô phải xác nhận" (F1 0.630 vs 0.492).
- Vòng lặp thang phân giải vẫn ở TRONG, giữ nguyên thứ tự (CLAUDE.md).

### E2.4 · in bậc và số nguồn (tiêu chí chấm 4)

C++ in số map nhận được; script E1 in bậc đã chọn. Cả hai phải nằm trong stdout để log
sweep sau này còn tra được — "thứ đầu tiên phải xem khi kết quả lạ".

## 5. Đo (60–80′) — đúng thứ tự này, hỏng tiêu chí 1 thì dừng

1. **seq06/349 và 06/939 phải khác 0** (đang F1 = 0.000). AUC bộ nguồn đã là 0.876/0.980,
   nên vẫn 0 ⇒ **lỗi cài đặt, không phải ý tưởng** — khi đó so từng bước với `src_quality.py`
   thay vì sửa mò.
2. **Sweep `threshold` 0.5 / 1 / 2 / 5** chỉ trên 2 scan đó. Phải thấy **cao nguyên**,
   không phải vách đá. Giá trị 1.0 cũ đã hết hiệu lực, đừng giữ.
3. **seq04, 5 scan mốc (50/100/150/200/250)**: không được tụt dưới **0.707 ± 0.023**.
4. Còn ≥ 5 phút thì `scripts/run_seed_sweep.py` trên seq04 (~2 phút) — **bắt buộc** vì E2.2
   đụng ground. Nhắc lại quy tắc số: **ΔF1 < 0.07 đo trên MỘT mask thì không kết luận được gì.**

Ghi trước để khỏi tự lừa: **seq07/673 phải VẪN xấu** (chế độ hỏng "map bị nhiễm").
Nếu nó bỗng tốt lên thì nghi phép đo, đừng mừng.

## 6. Cắt khỏi phiên này — cố tình

- Sweep đủ **20 scan / 4 sequence** (đó là việc mở màn phiên sau).
- Đề bài F (Patchwork++), G (seq07/441), hướng persistence cho map bị nhiễm.
- **Toàn bộ việc tích hợp pipeline chính.**

## 7. Vì sao chưa gắn được vào pipeline chính — 3 rào cứng

Ghi ra đây để phiên sau không phải điều tra lại. Đích là
`~/go2_mapping_dev/ros2_ws/src/FAST_LIO` (Livox L1 trên Go2).

1. **FOV dọc hard-code trong `main.cpp`** (`v_angle_min = -24.8`, `v_angle_max = 2.0`) —
   đó là Velodyne HDL-64 của KITTI. Livox L1 khác hẳn, và `64x900` cũng vô nghĩa với scan
   ~20k điểm. Phải đưa FOV ra dòng lệnh **và** chọn lại thang phân giải bằng đo, không đoán.
2. **Nguồn pose**: hiện đọc `poses.txt` + `Tr` của KITTI. FAST-LIO cho pose qua topic/PCD,
   không có `calib.txt`, hệ toạ độ cũng không phải cam0. Cần một lớp adapter, và phải quyết
   chạy **offline trên PCD đã lưu** hay **online trong node** — hai kiến trúc khác nhau,
   đừng để lẫn.
3. **Chế độ hỏng "map bị nhiễm"** (mục 6 handoff 20/8) chính là kịch bản Go2: robot **đứng
   yên** trong phòng có người qua lại thì map tích luỹ tích luôn vệt người, và `t_min` vô
   dụng. Đây là giả định gốc của Removert ("map dựng từ lượt đi sạch") bị vỡ — chưa có cách gỡ.

Rào 1 và 2 là việc kỹ thuật, làm được. **Rào 3 là rào thuật toán** — nên tích hợp trước khi
gỡ nó thì sẽ ra một module chạy tốt trên KITTI và im lặng sai trên Go2. Đáng làm một phiên
riêng, không phải 10 phút cuối phiên này.

## 8. Đóng phiên (80–90′)

- `git diff` đọc lại, build sạch, commit — message **tiếng Việt không dấu** (quy ước repo).
- `HANDOFF_2026-08-27.md`: con số đo được, tiêu chí nào đạt/hỏng, và **việc đầu tiên của
  phiên sau**. Ngắn cũng được, nhưng đừng bỏ.
