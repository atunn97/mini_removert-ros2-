# Bàn giao phiên 2026-08-12 — mini_removert: từ F1 0.164 lên 0.629

> File này tổng kết TOÀN BỘ phiên làm việc ngày 2026-08-12. Đọc file này là đủ để
> nắm trạng thái hiện tại. Bối cảnh sâu hơn về các phiên trước nằm ở
> `HANDOFF_VIEC4.md` (mục 1-12) — file đó vẫn còn giá trị, không thay thế.

---

## 0. Tóm tắt 1 phút

Bốn việc hoàn thành, F1 trung bình (5 scan seq04) đi **0.164 → 0.629**:

| # | Việc | F1 sau khi làm |
|---|---|---|
| 1 | Giảm `max_distance_m` 8m → 4m | 0.256 |
| 2 | Điều tra RANSAC ground (bác bỏ 3 giả định) | — |
| 3 | Ép điểm ground = static (vá lỗ hổng logic) | 0.275 |
| 4 | **Revert stage đa độ phân giải** | **0.629** |

Cấu hình đã chốt: `N=4`, `max_distance_m=4.0`, `threshold=0.5`, `vote ratio > 0.5`,
3 thang phân giải `64x900 / 32x450 / 16x225`.

Commit: `11f1182` (việc 1), `eb27324` (việc 2+3), `cf77b7b` (việc 4). Đã push hết.

---

## 1. LƯU Ý ĐẦU TIÊN: `handover_notes.md` đã lỗi thời một phần

File `~/Downloads/handover_notes.md` (mtime 2026-08-10) bị **vượt qua** bởi
`ke_hoach_ngay_mai.md` (mtime 2026-08-11) ở phần FAST-LIO2/L1:

- `handover_notes.md` mục 1-3 vẫn nghi ngờ **degenerate geometry** (robot đứng
  nhìn thẳng vào tường) là nguyên nhân FAST-LIO2 explode, và coi việc fix
  `extrinsic_R` từ gravity là kết quả tốt cần giữ.
- Nhưng `ke_hoach_ngay_mai.md` sau đó 1 ngày đã **chốt kết luận khác**: nguyên
  nhân gốc là **self-reflection noise cố hữu từ cấu trúc cơ khí gắn L1**
  (87-92% điểm mỗi scan nằm trong 0.1-0.35m, hình cánh quạt 360° quanh cảm
  biến, xuất hiện ở MỌI bag bất kể vị trí robot). Fix `extrinsic_R` từ gravity
  là **sai khái niệm**, đã revert về identity.

→ Việc "thu bag mới ở vị trí khác để verify degenerate geometry" và "tune lại
extrinsic R" trong `handover_notes.md` **không còn là ưu tiên đúng**. Việc 1
trong `ke_hoach_ngay_mai.md` (chạy `check_noise_by_angle.py` trên bag CŨ, không
cần bag mới, không cần `/utlidar/cloud`) mới là bước tiếp theo đúng cho mảng L1.

Lưu ý: mảng **SLAM** (L1/FAST-LIO2/mini_removert) và mảng **trình diễn**
(dance sync, WebRTC/Bluetooth) là 2 việc hoàn toàn riêng biệt, đừng gộp lẫn.

---

## 2. Việc 1 — giảm `max_distance_m` từ 8m xuống 4m

Làm theo đề mục 10.2 của `HANDOFF_VIEC4.md`.

Script mới `scripts/run_maxdist_sweep.sh`: sweep `max_distance_m` ở N cố định
(ngược với `run_n_sweep_v2.sh` vốn sweep N ở `max_distance_m` cố định).

**Kết quả (scan_idx=150):** precision giảm ĐƠN ĐIỆU khi `max_distance_m` tăng, ở
cả N=2 và N=4 — xác nhận đúng giả thuyết baseline/parallax ở mục 4-5 của
`HANDOFF_VIEC4.md`.

| N | max_d | P | R | F1 |
|---|---|---|---|---|
| 4 | 3-4m | 0.100 | 0.976 | **0.182** |
| 4 | 8m (cũ) | 0.057 | 0.989 | 0.108 |
| 2 | ≤2m (sàn dataset) | 0.107 | 0.900 | 0.191 |

Sàn của dataset: seq04 xe chạy liên tục ~1.4-1.5 m/frame nên **không thể có
baseline nhỏ hơn ~1.4m** — không phải hạn chế của thuật toán.

**Generalization:** so N=4/max_d=4 với mốc cũ N=10/max_d=8 trên 5 scan
(50/100/150/200/250) → F1 tăng **5/5**: 0.233→0.254, 0.414→0.469, 0.164→0.182,
0.152→0.202, 0.154→0.175. Không phải overfit vào scan_idx=150.

**Đã chốt:** `N=4, max_distance_m=4.0` (cập nhật trong `run_n_sweep_v2.sh`).

---

## 3. Việc 2 — điều tra RANSAC ground: bác bỏ 3 giả định

Phương pháp: KHÔNG sửa code core. Script Python độc lập đọc `.bin` + `.label`,
tự chạy RANSAC đúng tham số PCL (`SACMODEL_PLANE`, `distance_threshold=0.2`,
200 iterations, refit least-squares). Ground thật theo SemanticKITTI: road(40),
parking(44), sidewalk(48), other-ground(49), lane-marking(60), terrain(72).

### 3.1. BÁC BỎ: "RANSAC detect 69.5% ground, quá cao"

| | scan 150 |
|---|---|
| Ground THẬT (ground-truth) | **43.7%** |
| RANSAC detect | **50.2%** |

Ổn định 50.2% ±0.1% qua 5 seed. Quét `distance_threshold` 0.1→0.5 chỉ cho
41.5%→62.0%, **không tham số nào đạt 69.5%**. Con số cũ lỗi thời hoặc đo khác.

Mốc "30-50% điển hình outdoor" cũng sai với seq04: ground thật dao động
43.7-65.9% tùy scan. Over-detect là có thật nhưng chỉ ~6.5 điểm phần trăm.

### 3.2. BÁC BỎ: "RANSAC gom nhầm tường dài"

Độ nghiêng mặt phẳng tìm được so với mặt ngang: **~1° trên cả 5 scan** (bám
tường thì phải ~90°). Mặt phẳng ở -1.75m dưới sensor — khớp chiều cao gắn
Velodyne trên xe KITTI. **RANSAC bám đúng mặt đất.**

Cái nó thật sự gom nhầm: **vegetation** (87.2% số điểm gom nhầm, nuốt 23.8%
tổng vegetation) và bỏ sót **35.9% sidewalk** (vỉa hè cao hơn mặt đường
10-15cm → là mặt phẳng KHÁC, RANSAC 1-mặt-phẳng không mô tả được cả hai).

→ Kết luận "nên cân nhắc Patchwork++" vẫn đúng, nhưng vì lý do khác.

### 3.3. BÁC BỎ: "threshold thích ứng theo range"

FP theo khoảng cách (scan 150): 0-5m: 6.3%, 5-10m: 17.1%, **10-15m: 26.2%**,
15-20m: 18.0%, 20-30m: 11.7%, 30-40m: 6.7%, 40-60m: 9.3%, 60m+: 4.8%.

Khoảng cách TB: FP = 20.8m, **TP = 27.1m** — điểm động thật còn XA HƠN điểm báo
nhầm. Nới threshold theo range sẽ giết TP nhiều hơn FP. **Gạch hướng này.**

### 3.4. Phát hiện quan trọng nhất: ground KHÔNG phải chỗ mất precision

Thành phần 3241 FP ở scan 150: **vegetation 70.7%**, sidewalk+road 14.3%,
unlabeled 8.3%, còn lại 6.7%.

→ Ground segmentation dù hoàn hảo cũng chỉ cứu ~14% FP.

---

## 4. Việc 3 — vá lỗ hổng: ép điểm ground = static

**Lỗ hổng:** ground bị loại khỏi **range image** (`exclude_mask` của
`buildRangeImage`) nhưng **không bị loại khỏi danh sách ứng viên dynamic** —
`getDynamicIndices` duyệt MỌI điểm rồi tra `discrepancy_image[row][col]`, nên
một điểm mặt đường vẫn bị gán dynamic theo phán quyết của pixel mà nó chia sẻ
với vật khác.

Đo được: **745/3602 (20.7%)** điểm báo dynamic nằm trong mask ground — trong đó
**724 FP / chỉ 21 TP**.

**Đã sửa (`main.cpp`):**
1. `if (scan_ground_mask[i]) continue;` ở tầng voting.
2. `detectGroundMask` chuyển ra NGOÀI vòng lặp map — ground membership **bất
   biến qua phép biến đổi cứng** (phép quay/tịnh tiến bảo toàn khoảng cách
   điểm→mặt phẳng) nên tính 1 lần là đủ. Nhất quán hơn (RANSAC có yếu tố ngẫu
   nhiên) và nhanh hơn ~15%.

**Kết quả:** F1 trung bình 5 scan **0.256 → 0.275** (tăng 4/5 scan).

---

## 5. Việc 4 — revert stage đa độ phân giải (bước lớn nhất)

### 5.1. Nhận ra vấn đề

Code trước đây chỉ implement **nửa đầu** của Removert gốc. Tên đầy đủ của thuật
toán là ***"Remove, then Revert"*** (Kim & Kim, IROS 2020) — có bước thứ hai
khôi phục các điểm static bị xoá oan.

Dấu hiệu nhận biết nằm ngay trong số liệu: **P=0.113 / R=0.976** chính là chữ ký
của một **phép sàng lọc chưa có bước khẳng định** (giống xét nghiệm sàng lọc y
khoa: thà báo nhầm còn hơn bỏ sót, và không bao giờ kết luận chỉ dựa vào nó).

### 5.2. Nguyên lý

> **Vật động THẬT bất nhất ở MỌI thang quan sát. Báo nhầm do nhiễu chỉ bất nhất
> ở thang mịn nhất.**

Chiếc xe biến mất thì nhìn thô hay mịn đều thấy mất. Bụi cây đứng yên chỉ "khác"
khi soi từng tia; lùi lại nhìn cả khối lá thì vẫn nguyên đó.

Đây là **cùng triết lý với multi-frame voting** ("đừng tin một phép đo đơn lẻ")
nhưng áp lên trục KHÔNG GIAN thay vì trục THỜI GIAN:

| | gộp bằng chứng theo | trạng thái |
|---|---|---|
| Multi-frame voting | thời gian (nhiều map) | đã có từ trước |
| Multi-resolution revert | không gian (nhiều thang) | **thêm ở phiên này** |

### 5.3. Kiểm chứng TRƯỚC khi implement

Dự đoán viết ra trước, rồi mới chạy (chạy pipeline ở 3 thang, đối chiếu tập kết quả):

| | dự đoán | thực tế |
|---|---|---|
| TP còn giữ ở thang thô | > 70% | **98.0%** |
| FP còn giữ ở thang thô | < 30% | **23.8%** |

So 5 luật revert khác nhau (trung bình 5 scan):

| luật | P | R | F1 |
|---|---|---|---|
| A. chỉ remove (cũ) | 0.170 | 0.858 | 0.275 |
| B. xác nhận @32×450 | 0.372 | 0.849 | 0.509 |
| C. xác nhận @16×225 | 0.485 | 0.835 | 0.602 |
| **D. xác nhận @ CẢ HAI** | **0.520** | **0.832** | **0.630** |
| E. xác nhận @ ít nhất 1 | 0.355 | 0.852 | 0.492 |

Luật E tệ hơn hẳn D → **revert là bước khẳng định, bằng chứng phải chặt.**

### 5.4. Implement

```
levels = { {64,900} remove, {32,450} revert, {16,225} revert }
```

Điểm thiết kế quan trọng — **vòng lặp map ở NGOÀI, độ phân giải ở TRONG**:

```cpp
for (map_idx : map_indices) {
    load PCD, transform, fit ground     // 1 lần, dùng chung mọi thang
    for (li : levels) {
        build range images @ levels[li]
        discrepancy -> vote_count[li], observed_count[li]
    }
}
```

Ground mask và transform **không phụ thuộc độ phân giải** → 3 thang chỉ tốn
thêm **40%** thời gian (0.74s → 1.03s ở N=4), không phải 200% như chạy 3 lần
riêng (~2.2s).

Tầng phân loại:

```cpp
if (scan_ground_mask[i]) continue;        // ground luôn static
if (!is_dynamic_at(0, i)) continue;       // REMOVE: thang mịn chọn ứng viên
for (li = 1; li < n_levels; ++li)         // REVERT: mọi thang thô phải xác nhận
    if (!is_dynamic_at(li, i)) { confirmed = false; break; }
```

Chi tiết nhỏ nhưng quan trọng: khi `observed_count[li][i] == 0` (không map nào
quan sát được điểm đó ở thang này), `is_dynamic_at` trả `false` = **không xác
nhận** → revert. Thiếu bằng chứng thì trả về static, không suy diễn có lợi cho
việc xoá.

### 5.5. Kết quả — khớp CHÍNH XÁC với mô phỏng

| scan | P | R | F1 | F1 mô phỏng |
|---|---|---|---|---|
| 50 | 0.444 | 0.757 | 0.560 | 0.560 |
| 100 | 0.668 | 0.899 | 0.766 | 0.766 |
| 150 | 0.478 | 0.897 | 0.623 | 0.623 |
| 200 | 0.718 | 0.835 | 0.772 | 0.772 |
| 250 | 0.294 | 0.773 | 0.426 | 0.426 |

**F1 trung bình 0.629.** Khớp 5/5 scan là bằng chứng mạnh rằng implement đúng ý
đồ, không có bug ẩn.

FP giảm **79.7%** tổng thể, theo từng loại vật:

| class | FP trước | FP sau | giảm |
|---|---|---|---|
| vegetation | 7684 | 426 | **−94.5%** |
| pole | 455 | 4 | **−99.1%** |
| trunk | 202 | 4 | **−98.0%** |
| fence | 422 | 60 | −85.8% |
| **road** | 1481 | 1186 | **−19.9%** |

---

## 6. Tại sao road KHÔNG giảm — và bài học quan trọng nhất phiên này

Vegetation/pole/trunk rụng 94-99%, nhưng road chỉ rụng 19.9%. Lý do:

Lỗi ở mặt đường **không phải nhiễu ngẫu nhiên**. Nếu scan và map lệch nhau chút
theo phương thẳng đứng, mặt đường lệch range một cách **hệ thống** — ở *mọi*
thang quan sát đều lệch như nhau. Đổi thang không cứu được.

> **Gộp nhiều bằng chứng chỉ triệt tiêu được sai số NGẪU NHIÊN. Sai số HỆ THỐNG
> thì bao nhiêu bằng chứng cũng vô ích — phải sửa tận gốc.**

Đây là lần thứ **hai** nguyên lý này xuất hiện trong dự án:
1. Mục 4 `HANDOFF_VIEC4.md`: voting đa số không cứu được parallax (baseline xa).
2. Phiên này: revert đa thang không cứu được lệch mặt đường.

Cả hai lần, cách sửa đúng đều là **sửa nguồn gây sai số hệ thống**, không phải
gộp thêm bằng chứng.

---

## 7. Ưu tiên tiếp theo — thứ tự ĐÃ ĐẢO NGƯỢC

Thành phần FP **còn lại** sau revert: road 40.2% + sidewalk 18.7% + terrain
8.6% = **67.5% là ground**.

Đầu phiên tôi xếp Patchwork++ hạng 3 ("ground chỉ 14% FP, ưu tiên thấp"). Sau
khi vegetation bị dọn sạch, **ground nhảy từ 14% lên 67.5% → Patchwork++ lên
hạng 1**.

→ Bài học quy trình: **đo lại sau mỗi lần sửa**, đừng lập kế hoạch dài hạn từ
đầu — sửa xong thứ lớn nhất thì thứ tự ưu tiên đổi hoàn toàn.

**Danh sách theo thứ tự hiện tại:**

1. **Patchwork++ thay `ground_filter.cpp`** — giải quyết cả 2 vấn đề đã đo:
   vỉa hè ở độ cao khác bị bỏ sót (35.9%) và cỏ thấp bị nuốt (23.8%). Có source
   công khai.
2. **Sweep lại `threshold` và `vote_threshold`** — cả hai đang cố định
   (0.5 / 0.5), chưa hề sweep với baseline N=4/max_d=4 + revert stage mới.
3. **Sweep cấu hình thang phân giải** — 3 con số (64×900, 32×450, 16×225) và
   luật "xác nhận ở tất cả" đều chọn theo trực giác rồi kiểm chứng, chưa tối ưu.
   Thử 2 thang, 4 thang, tỷ lệ downscale khác.
4. **Test trên sequence khác** — toàn bộ kết quả phiên này là 5 scan của MỘT
   sequence (seq04). Chưa chứng minh generalize sang seq khác.
5. **Map tích luỹ thay vì 1 scan thô** (mục 10.4 `HANDOFF_VIEC4.md`) — giải
   quyết tận gốc giới hạn baseline.

---

## 8. Cách chạy (cấu hình hiện tại)

```bash
cd ~/mini_removert
colcon build --packages-select mini_removert

# chọn map theo khoảng cách thật
python3 scripts/select_maps_by_distance.py \
    ~/kitti_data/dataset/poses/04.txt \
    ~/kitti_data/dataset/sequences/04/calib.txt \
    150 4 4          # scan_idx=150, N=4, max_distance_m=4

# chạy (map_idx lấy từ dòng cuối lệnh trên)
./build/mini_removert/mini_removert \
    ~/kitti_data/dataset/sequences/04/pcd \
    ~/kitti_data/dataset/poses/04.txt \
    ~/kitti_data/dataset/sequences/04/calib.txt \
    150 0.5 148 149 151 152

# đánh giá
python3 scripts/evaluate.py \
    ~/kitti_data/dataset/sequences/04/labels/000150.label \
    dynamic_indices_scan150.txt
```

Script sweep: `scripts/run_n_sweep_v2.sh` (sweep N), `scripts/run_maxdist_sweep.sh`
(sweep `max_distance_m`).

---

## 9. PHỤ LỤC — vì sao giảm độ phân giải lại ảnh hưởng tới `min()`?

Câu hỏi đặt ra cuối phiên, ghi lại vì đây là cơ chế nền của toàn bộ việc 4.

### 9.1. Số liệu nền (scan 150)

| độ phân giải | pixel ngang | pixel dọc | pixel có data | **điểm/pixel** |
|---|---|---|---|---|
| 64×900 | 0.40° | 0.419° | 45464 | **2.7** |
| 32×450 | 0.80° | 0.838° | 12955 | 9.4 |
| 16×225 | 1.60° | 1.675° | 3335 | **36.3** |

Bề ngang pixel ngoài đời thực:

| khoảng cách | @0.40° (mịn) | @1.60° (thô) |
|---|---|---|
| 10 m | 7.0 cm | 27.9 cm |
| 20 m | **14.0 cm** | 55.9 cm |
| 40 m | 27.9 cm | 111.7 cm |

Độ tán xạ range TRONG 1 pixel thô (max−min, trung vị):

| class | trung vị |
|---|---|
| **vegetation** | **2.42 m** |
| road | 0.57 m |
| fence | 0.39 m |

### 9.2. Cơ chế A — bề mặt xốp làm `min()` thành xúc xắc

Với tán lá, một tia hoặc trúng lá (25m) hoặc lọt khe rồi trúng tường sau (30m).
Phân bố range **lưỡng đỉnh** — số đo tán xạ 2.42m ở trên chính là bằng chứng
(so với road chỉ 0.57m).

Ở 64×900, `min()` chỉ gộp **2.7 điểm**. Với 2-3 mẫu từ phân bố lưỡng đỉnh, việc
pixel đó ra "lá" hay ra "khe" gần như là **tung đồng xu**. Scan ra lá (25m), map
ra khe (30m) → lệch 5m → báo dynamic. Đó chính là false positive.

Ở 16×225, `min()` gộp **36.3 điểm**. Xác suất cả 36 tia đều lọt khe là gần như 0
→ min ở cả scan lẫn map đều là "lá" → lệch ≈ 0 → hết báo nhầm.

**Điểm mấu chốt:** `min()` không phải phép trung bình — nó là **thống kê thứ tự**.
Gộp nhiều mẫu không làm nó "mịn đi", mà làm nó **hội tụ về một tính chất hình học
ổn định**: "vật gần nhất trong nón góc này". Câu hỏi đổi từ *"tia CỤ THỂ này
trúng gì?"* (phụ thuộc viewpoint, không ổn định) sang *"trong nón góc này vật gần
nhất là gì?"* (tính chất hình học, ổn định giữa 2 viewpoint).

### 9.3. Cơ chế B — lượng tử hoá góc và vật mảnh

Cơ chế thứ hai, độc lập với độ xốp. Lệch viewpoint `d` làm vật ở khoảng cách `r`
dịch một góc `Δφ ≈ d/r`. Với d=5cm, r=10m → Δφ ≈ 0.29°.

Pixel mịn rộng 0.40° → dịch 0.29° là **gần trọn một pixel**: vật có thể rơi sang
pixel KHÁC trong map so với scan. Lúc đó pixel (i,j) của scan chứa cây cột, còn
pixel (i,j) của map chứa bức tường phía sau → lệch khổng lồ → báo nhầm.

Pixel thô rộng 1.60° → 0.29° chỉ là 18% pixel → vật ở nguyên pixel cũ → so sánh
đúng.

Điều này giải thích vì sao **vật MẢNH rụng mạnh nhất**: pole −99.1%, trunk −98.0%.
Ở 20m, pixel mịn rộng 14cm — **đúng bằng cỡ đường kính một cây cột**. Đó chính là
vùng mà lượng tử hoá cắn mạnh nhất: dịch chút là cột ra khỏi pixel hoàn toàn. Còn
bức tường rộng thì dịch chút vẫn là tường, không sao.

### 9.4. Vì sao road không được cứu

Cả hai cơ chế trên đều xử lý sai số **ngẫu nhiên** (tia nào trúng gì, pixel nào
chứa gì). Lệch mặt đường là **bias hệ thống** — toàn bộ mặt phẳng lệch cùng một
hướng, ở mọi tia, mọi pixel, mọi thang. Đổi kích thước pixel không đụng gì tới
nó. (Xem mục 6.)

### 9.5. Cái giá phải trả

`min()` gộp nhiều cũng có mặt trái: một vật động NHỎ nằm chung pixel thô với một
vật tĩnh gần hơn sẽ bị vật tĩnh che mất → không được xác nhận → bị revert → mất.
Đó chính là lý do recall tụt nhẹ 0.858 → 0.832.

Đây là lý do phải dùng **thang mịn để remove** (nhạy, bắt cả vật nhỏ) và **thang
thô để revert** (chắc, lọc nhiễu) — chứ không dùng một thang duy nhất cho cả hai
việc. Hai câu hỏi khác nhau thì cần hai phép đo khác nhau.

---

## 10. Ghi chú cho người kế nhiệm

- Script chẩn đoán của phiên này (`diag_ground.py`, `diag_fp.py`, `diag_range.py`)
  để ở thư mục tạm, **KHÔNG commit** vì chỉ dùng một lần. Logic đơn giản, chỉ cần
  numpy — viết lại theo mô tả mục 3 và 9 nếu cần.
- Mọi con số trong file này đo trên **seq04, 5 scan (50/100/150/200/250)**. Đừng
  coi là đã generalize.
- Tham chiếu paper trong `HANDOFF_VIEC4.md` mục 12.7 (Removert, ERASOR,
  Patchwork++, Dynablox, persistence filter...) ghi **theo trí nhớ** — kiểm tra
  lại tên/năm trước khi trích dẫn chính thức.
