# Bàn giao phiên 2026-08-14 — điều tra 3 scan "0 TP tuyệt đối" (mục 16.6 phiên 13/8)

> Toàn bộ phiên là **đo đạc**, không sửa một dòng `.cpp` nào. Script mới:
> `scripts/diag_zero_tp.py`. Nối tiếp `HANDOFF_2026-08-13.md`.

---

## 0. Tóm tắt 1 phút

| # | Việc | Kết luận |
|---|---|---|
| 1 | 3 scan `P=R=F1=0.000` có phải chế độ hỏng riêng? | **KHÔNG** — không có bug. Là **cùng một cơ chế** với phần còn lại, chỉ ở mức cực đoan |
| 2 | Cơ chế đó là gì | **vật động chậm hơn `threshold`**: 0.16-0.56 m/frame vs seq04 1.3-1.7 m/frame |
| 3 | Hạ `threshold` có cứu được không? | **KHÔNG** — thr=0.1 kéo TP lên nhưng precision chỉ còn **0.001-0.054** |
| 4 | Vì sao không cứu được | **tín hiệu nằm LỌT TRONG dải nhiễu**, không tách được bằng độ lớn |
| 5 | Giả thuyết "map loop-closure phá seq06" | **BÁC BỎ, và ngược lại** — chặn map xa thời gian làm seq06 **tụt** 0.145 → 0.068 |
| 6 | Hệ quả của mục 5 | bằng chứng thực nghiệm **ủng hộ map tích luỹ** (mục 15.4 phiên 13/8) |
| 7 | Tương quan tốc độ vật ↔ F1 | **r = 0.731** trên 20 scan. Vật < 0.6 m/frame: F1 TB 0.148; ≥ 0.6: 0.533 |
| 8 | Lỗi đo của chính phiên này | poses KITTI ở hệ **cam0** (y = xuống, z = tới) — lấy `[:2]` làm "mặt phẳng ngang" là SAI, xem mục 5 |

**Câu trả lời cho mục 16.6 phiên 13/8:** ba scan đó **không phải chế độ hỏng riêng**.
Chúng chỉ là ba điểm cực đoan nhất của cùng một hạn chế, và hạn chế đó là **nguyên lý**,
không phải bug: thuật toán phân biệt động/tĩnh bằng **độ lớn** của chênh lệch range, nên
mù với mọi vật di chuyển chậm hơn mức nhiễu nền.

---

## 1. Bối cảnh & phương pháp

`HANDOFF_2026-08-13.md` mục 16.6 để lại 3 scan có `P = R = F1 = 0.000` dù dự đoán hàng
trăm điểm, nghi là "một chế độ hỏng riêng":

| seq | scan | GT dynamic | dự đoán | class GT |
|---|---|---|---|---|
| 06 | 349 | 183 | 87 | `moving-car` (1 instance) |
| 06 | 939 | 179 | 315 | `moving-bicyclist` (1 instance) |
| 07 | 248 | 31 | 201 | `moving-person` (1 instance) |

Cả 3 chỉ có **đúng 1 instance** vật động — nên chỉ cần giải thích một vật là xong.

Script mới `scripts/diag_zero_tp.py <seq> <scan> [...]` đo hai thứ **độc lập nhau**:

- **[A] Phễu 6 tầng** (phụ thuộc pipeline): mô phỏng lại `main.cpp` bằng numpy, đếm xem
  trong số điểm GT dynamic còn bao nhiêu sau mỗi tầng
  `GT → trong FOV → không-ground → được quan sát → REMOVE@L0 → xác nhận @L1 @L2 @L3`.
  Chạy trên **3 ground mask** (seed 0/1/2) vì mục 11 phiên trước.
- **[B] Vật có thật sự di chuyển không** (KHÔNG đụng pipeline): 16 bit cao của nhãn
  SemanticKITTI là **instance id**. Bám cùng một instance qua các frame, đổi về hệ toạ độ
  thế giới, đo dịch chuyển trọng tâm.

Cộng thêm 3 phép đo chạy **binary C++ thật** (không mô phỏng), đều chỉ dùng tham số dòng
lệnh nên không phải sửa code.

---

## 2. Loại trừ trước: KHÔNG phải lệch index, KHÔNG phải sai FOV, KHÔNG phải ground

Bốn giả thuyết rẻ bị loại ngay:

**2.1. Lệch index / hỏng dữ liệu — LOẠI.**

| seq | poses | bin | label | pcd |
|---|---|---|---|---|
| 03 | 801 | 801 | 801 | 801 |
| 04 | 271 | 271 | 271 | 271 |
| 06 | 1101 | 1101 | 1101 | 1101 |
| 07 | 1101 | 1101 | 1101 | 1101 |

Và số điểm khớp đến từng byte ở cả 3 scan hỏng (vd seq06/349: bin 124416 điểm,
label 124416 điểm, pcd = 124416×12 + 174 byte header = 1493166 ✓).

**2.2. Ngoài FOV dọc — LOẠI.** Phễu [A]: `trong FOV` giữ **100%** điểm GT ở cả 3 scan.

**2.3. Ground mask nuốt điểm động — CÓ nhưng KHÔNG phải nguyên nhân chính.**
Chỉ seq06/349 mất đáng kể (183 → 96-113 điểm, tức 38-48%; xe ở xa 27 m nên thân dưới nằm
trong 0.2 m của mặt phẳng RANSAC). Hai scan kia giữ 92.7% và 100%. Mà cả ba đều ra 0 TP
→ không giải thích được.

**2.4. Không có bằng chứng (`observed_count = 0`) — LOẠI.** Tầng `được quan sát` gần như
không mất điểm nào (112→112, 166→166, 31→31).

### Phễu đầy đủ — điểm chết ở đâu

```
                        seq06/349   seq06/939   seq07/248
GT dynamic                    183         179          31
trong FOV                     183         179          31
không-ground                  112         166          31
được quan sát                 112         166          31
REMOVE @64x900                 26  (14%)   72  (39%)   22  (71%)
xác nhận @32x450               13           48           8
xác nhận @16x225                8            4           1
xác nhận @8x112                 0            0           0   <- chết ở đây
```

Điểm chết **không tập trung ở một tầng** — nó rò rỉ đều qua các tầng REVERT. Đây là chữ ký
của "bằng chứng yếu ở mọi thang", không phải của một tầng hỏng.

---

## 3. Nguyên nhân thật: vật động **chậm hơn `threshold`**

### 3.1. Tốc độ thật của vật, đo bằng instance tracking

Trọng tâm instance trong hệ toạ độ thế giới, chênh lệch giữa các frame, **norm 3D**:

| seq/scan | vật | tốc độ thật | F1 |
|---|---|---|---|
| 04 / 150 | `moving-car #1` | **1.50 m/frame** | 0.727 |
| 04 / 150 | `moving-car #6` | **1.36 m/frame** | — |
| 06 / 349 | `moving-car #9` | **0.56 m/frame** | **0.000** |
| 06 / 939 | `moving-bicyclist #1` | **0.31 m/frame** | **0.000** |
| 07 / 248 | `moving-person #7` | **0.16 m/frame** | **0.000** |

`threshold = 1.0 m`. Dịch chuyển của vật giữa scan và map (cách 2-3 frame):

- seq04: 1.4 × 2.5 ≈ **3.5 m** — vượt xa ngưỡng, dư dả
- seq06/939: 0.31 × 2.5 ≈ **0.78 m** — **dưới** ngưỡng
- seq07/248: 0.16 × 4 ≈ **0.64 m** — **dưới** ngưỡng

Đo trực tiếp discrepancy tại pixel của điểm GT (thang 64×900) khớp đúng:

| seq06/939 map | 936 | 941 | 942 |
|---|---|---|---|
| trung vị (map−scan) | 0.93 | 0.01 | 0.17 |

Cả ba đều **dưới** `threshold=1.0`. Vật động thật nằm bên **trái** ngưỡng cùng phía với nhiễu.

### 3.2. Sweep `threshold` xác nhận nhân quả (C++ thật)

TP theo `threshold`, trên đúng bộ map đang dùng:

| thr | 04/150 TP | 06/349 TP | 06/939 TP | 07/248 TP |
|---|---|---|---|---|
| 0.1 | 362 | 78 | 141 | 5 |
| 0.2 | 307 | 40 | 101 | 4 |
| 0.3 | 307 | 33 | 86 | 1 |
| 0.5 | 307 | 17 | 44 | 0 |
| 0.7 | 306 | 0 | 4 | 0 |
| **1.0** | **295** | **0** | **0** | **0** |

**Đây là bằng chứng quyết định.** seq04: TP gần như **phẳng** (307 → 295) khi ngưỡng tăng
gấp 5 lần — tín hiệu ở rất xa ngưỡng, tăng ngưỡng chỉ giết FP. Ba scan kia: TP **sụp thẳng
về 0** — ngưỡng đang cắt ngang giữa tín hiệu.

Đây chính là lý do `threshold = 1.0` là "món hời miễn phí" ở seq04 (mục 12 phiên 13/8) mà
lại là **thảm hoạ** ở nơi khác. Cảnh báo ở cuối mục 12.4 ("ngưỡng neo vào quãng đường đi
được") đã đúng, giờ có số đo cho vế còn lại.

### 3.3. Nhưng hạ ngưỡng KHÔNG cứu được — và đây mới là kết luận quan trọng

Precision ở ngưỡng thấp nhất từng thử:

| seq/scan | thr | TP | tổng dự đoán | **Precision** |
|---|---|---|---|---|
| 06/349 | 0.1 | 78 | 1952 | **0.040** |
| 06/939 | 0.1 | 141 | 2618 | **0.054** |
| 07/248 | 0.1 | 5 | 4034 | **0.001** |
| 04/150 | 0.1 | 362 | 1735 | 0.209 |

Hạ ngưỡng xuống 0.1 m thì bắt được vật động **nhưng kéo theo 1900-4000 điểm rác**. F1 tốt
nhất mà seq06/939 đạt được ở MỌI ngưỡng chỉ là **0.148** (tại thr=0.3).

> **Kết luận cơ chế: tín hiệu nằm LỌT TRONG dải nhiễu.**
> Nhiễu nền (sai số ground + parallax do baseline 4 m + bề mặt xốp) có biên độ
> **0.1 - 1.0 m**. Vật động chậm tạo tín hiệu **0.2 - 0.8 m**. Hai phân bố **chồng lên
> nhau**, nên không tồn tại ngưỡng nào tách được chúng — đó là định nghĩa của việc
> "không phân tách được bằng độ lớn".

Ở seq04 thì tín hiệu (3.5 m) nằm **hoàn toàn bên ngoài** dải nhiễu — đó, và chỉ đó, là lý
do seq04 cho F1 0.65-0.72.

Đây là lần thứ **ba** dự án gặp lại cùng một nguyên lý (mục 15.2 phiên 13/8):
**bằng chứng phải có CẤU TRÚC, không chỉ có ĐỘ LỚN.**

---

## 4. Tương quan tốc độ vật ↔ F1: r = 0.731

Đo tốc độ trung bình (có trọng số theo số điểm) của các instance động ở cả 20 scan test:

| seq | scan | tốc độ (m/frame) | F1 | | seq | scan | tốc độ | F1 |
|---|---|---|---|---|---|---|---|---|
| 03 | 79 | 0.858 | 0.400 | | 06 | 205 | 0.828 | 0.375 |
| 03 | 125 | 0.792 | 0.272 | | 06 | 349 | 0.562 | **0.000** |
| 03 | 171 | 0.797 | 0.478 | | 06 | 763 | 0.350 | 0.123 |
| 03 | 217 | 0.559 | 0.210 | | 06 | 881 | 0.904 | 0.228 |
| 03 | 284 | 0.522 | 0.160 | | 06 | 939 | 0.312 | **0.000** |
| 04 | 60 | 1.317 | 0.774 | | 07 | 248 | 0.166 | **0.000** |
| 04 | 97 | 1.510 | 0.881 | | 07 | 441 | 0.489 | 0.546 |
| 04 | 135 | 1.500 | 0.462 | | 07 | 673 | 1.027 | 0.649 |
| 04 | 172 | 1.495 | 0.751 | | 07 | 798 | 1.183 | 0.723 |
| 04 | 210 | 1.678 | 0.375 | | 07 | 923 | 0.692 | 0.557 |

**Pearson r = 0.731** trên 20 scan.

| nhóm | số scan | F1 TB |
|---|---|---|
| vật chậm < 0.6 m/frame | 7 | **0.148** |
| vật nhanh ≥ 0.6 m/frame | 13 | **0.533** |

Ba scan 0-TP nằm ở nhóm chậm nhất (0.166 / 0.312 / 0.562). Ngoại lệ đáng chú ý:
seq07/441 (0.489 m/frame nhưng F1 0.546) — tốc độ là biến giải thích **mạnh nhưng chưa
đủ**, r² ≈ 0.53 nghĩa là còn ~47% phương sai đến từ chỗ khác (ground mask, khoảng cách,
hướng dịch chuyển so với tia nhìn).

**Hệ quả cho bảng ở mục 16.2 phiên 13/8:** "% frame có vật động" và "điểm động TB/frame"
KHÔNG phải là thứ làm seq04 đặc biệt. Thứ làm seq04 đặc biệt là **vật động ở đó chạy
nhanh gấp 2-8 lần** các sequence khác.

---

## 5. LỖI ĐO CỦA CHÍNH PHIÊN NÀY — phải nhớ

Lần đo đầu tiên tôi tính dịch chuyển bằng `norm((c_map - c_scan)[:2])`, tưởng `(x, y)` là
mặt phẳng ngang. **Sai.**

`poses/<seq>.txt` của KITTI odometry ở hệ **camera 0**: `x = phải`, `y = XUỐNG`,
`z = TỚI TRƯỚC`. Nên `(x, y)` là mặt phẳng **đứng**, và lấy `[:2]` **bỏ mất hướng tiến** —
tức là bỏ đúng thành phần chuyển động chính của mọi xe chạy thẳng.

Hậu quả: xe seq04 đo ra **0.023 m/frame** (thực tế **1.50**), và tôi đã kết luận nhầm rằng
xe ở seq06/349 "đứng yên". Chỉ số Pearson cũng sai theo: **r = 0.248** (đúng: **0.731**).

Phát hiện ra là nhờ một số liệu **không thể đúng**: bảng in ra "khoảng cách tới ego" của
một chiếc xe là 0.2 m mà nó vẫn nằm trong tầm nhìn suốt 24 frame. Đã sửa trong
`diag_zero_tp.py` (dùng norm 3D) kèm comment cảnh báo tại chỗ.

> **Bài học:** khi mượn `poses` KITTI, mặt đất là mặt phẳng **(x, z)**, không phải (x, y).
> Và luôn in kèm một đại lượng có **thứ nguyên vật lý kiểm tra được bằng mắt**
> (khoảng cách tới ego, tốc độ ego) — chính nó bắt được lỗi này.

---

## 6. BÁC BỎ giả thuyết "map loop-closure phá seq06" — và kết quả ngược lại

### 6.1. Quan sát ban đầu

`select_maps_by_distance.py` chọn map **chỉ theo khoảng cách không gian**, không có ràng
buộc thời gian. Ở seq06 (sequence có vòng lặp lớn trong KITTI) điều đó dẫn tới:

| seq | scan | map được chọn (|Δframe|) |
|---|---|---|
| 06 | 205 | 200(5), 202(3), **1028(823)**, **1030(825)** |
| 06 | 881 | **50(831)**, **55(826)**, 883(2), 884(3) |
| 06 | 939 | **106(833)**, 936(3), 941(2), 942(3) |
| 07 | 673 | 638(35), 645(28), **741(68)**, **748(75)** |

Quét toàn bộ frame của từng sequence:

| seq | tốc độ ego (m/frame) | % frame có ≥1 map lệch > 20 frame | > 100 frame |
|---|---|---|---|
| 03 | 0.70 | 2.8% | 0.0% |
| 04 | 1.46 | **0.0%** | 0.0% |
| 06 | 1.12 | **47.1%** | **47.1%** |
| 07 | 0.63 | 15.1% | 4.5% |

Hai nguyên nhân khác nhau: seq06 là **loop closure thật** (đi lại đúng con phố sau ~83 s);
seq07/673 là **xe dừng** — mọi frame lân cận đều nằm trong 4 m nên bộ chọn phải với ra xa
±28-75 frame mới đủ các mốc khoảng cách `linspace(1, 4, 2)`.

### 6.2. Đo: chặn lại thì TỆ ĐI

Chạy binary C++ thật, so sánh **có cặp** (cùng scan, cùng mọi tham số, chỉ khác cách chọn
map), ràng buộc `|map_idx − scan_idx| ≤ 50`:

| seq | map hiện tại (F1 TB) | map có ràng buộc thời gian (F1 TB) |
|---|---|---|
| 03 | 0.304 | 0.304 (không đổi) |
| 04 | 0.649 | 0.649 (không đổi) |
| **06** | **0.145** | **0.068** ↓ |
| 07 | 0.495 | 0.495 (không đổi) |

Chi tiết seq06: scan 205 **0.375 → 0.171**, scan 881 **0.228 → 0.045**.

**Giả thuyết bị bác bỏ, và dấu thì ngược lại.** Map lấy từ 830 frame trước đang là thứ
**cứu** seq06, không phải thứ phá nó.

### 6.3. Vì sao — và vì sao đây là tin tốt

Ở seq06/939, tại pixel của điểm động, trung vị `(map − scan)`:

| map | 106 (Δ=833) | 936 (Δ=3) | 941 (Δ=2) | 942 (Δ=3) |
|---|---|---|---|---|
| trung vị | **17.45 m** | 0.93 | 0.01 | 0.17 |
| % vượt thr=1.0 | **100.0%** | 45.2% | 41.0% | 50.7% |

Map hàng xóm ±2-3 frame **chứa chính con người/xe đó ở gần như cùng chỗ** → chênh lệch
0.01-0.93 m, chìm trong nhiễu. Map từ 83 giây trước **không có vật đó** → chênh lệch
17.45 m, vượt xa **mọi** ngưỡng và **mọi** mức nhiễu.

> Baseline **thời gian dài** biến tín hiệu từ "0.3 m lẫn trong nhiễu 0.1-1.0 m" thành
> "17 m, không thể nhầm". Đây đúng là thứ **map tích luỹ** sẽ cung cấp — và ở đây nó đã
> vô tình xuất hiện, đo được, có lợi.

**Đây là bằng chứng thực nghiệm đầu tiên ủng hộ quyết định ở mục 15.4 phiên 13/8.** Trước
đó map tích luỹ mới chỉ là lập luận (ANTI ngược dấu với lifelong); giờ có số đo.

**Hệ quả ngược lại cũng phải ghi:** nếu sau này thêm ràng buộc thời gian vào
`select_maps_by_distance.py` (nghe rất hợp lý), nó sẽ **âm thầm làm seq06 tệ đi một nửa**.
Đừng làm việc đó trước khi có map tích luỹ.

---

## 7. Ba việc KHÔNG nên làm (đã đo, đã loại)

1. ~~Sửa lệch index / kiểm tra lại dữ liệu~~ — dữ liệu lành, mục 2.1.
2. ~~Thêm ràng buộc thời gian cho `select_maps_by_distance.py`~~ — làm seq06 tụt một nửa,
   mục 6.2.
3. ~~Tune `threshold` riêng cho từng sequence~~ — trần của seq06/939 ở MỌI ngưỡng chỉ là
   F1 0.148, mục 3.3. Khớp với mục 16.4 phiên 13/8 nhưng giờ đã biết **vì sao**.

---

## 8. Ưu tiên phiên sau (cập nhật danh sách mục 9 phiên 13/8)

1. **Map tích luỹ** — vẫn hạng 1, và giờ mạnh hơn hẳn: mục 6.3 cho số đo, không chỉ lập
   luận. Ba hệ quả ở mục 15.4 phiên 13/8 vẫn nguyên giá trị (đặc biệt: `threshold=1.0`
   hết hiệu lực, phải sweep lại từ đầu).
2. **Hạ trần nhiễu, không hạ ngưỡng.** Mục 3.3 cho thấy vấn đề là **dải nhiễu 0.1-1.0 m**
   chứ không phải giá trị ngưỡng. Mọi thứ hạ được nhiễu nền đều mở khoá được vật chậm:
   ground segmentation tốt hơn (Patchwork++), và ~~lây nhiễm theo pixel~~.
   **CẬP NHẬT:** lây nhiễm theo pixel **đã đo và bác bỏ** (mục 13) — điểm "ăn ké" không giàu
   FP hơn điểm chủ-min, nên đường này không hạ được nhiễu. Chỉ còn Patchwork++.
3. **Bổ sung bằng chứng có CẤU TRÚC** thay vì độ lớn — với map tích luỹ thì đó là
   **persistence** (mục 15.3 phiên 13/8).
4. Điều tra ngoại lệ **seq07/441** (0.489 m/frame mà F1 0.546) — nó cho biết ~47% phương
   sai còn lại nằm ở đâu. Nghi: **hướng dịch chuyển so với tia nhìn**. Range image chỉ đo
   thành phần **xuyên tâm**; vật đi ngang (tiếp tuyến) dịch nhiều mà range đổi ít. Đây có
   thể là biến giải thích thứ hai, chưa đo.
5. ~~Điều tra 3 scan 0 TP~~ — **XONG**, phiên này.
6. ~~Tìm hiểu vì sao seq06 phẳng lì với mọi ngưỡng~~ — **XONG**: vật ở seq06 chậm
   (0.31-0.90 m/frame), tín hiệu nằm trong dải nhiễu nên ngưỡng không phải biến điều khiển.

**Khi báo cáo F1 từ giờ, ghi kèm tốc độ vật động trung bình của bộ scan đó.** Không có nó
thì con số F1 không so sánh được giữa hai sequence (mục 4).

---

## 9. File & cách tái tạo

Script mới, để trong `scripts/`, chỉ cần numpy, đọc thẳng `.bin` + `.label`:

- `scripts/diag_zero_tp.py <seq> <scan> [seq scan ...]` — phễu 6 tầng × 3 ground mask,
  instance tracking, discrepancy theo từng map, phân loại FP.
  `python3 scripts/diag_zero_tp.py 06 349 06 939 07 248 04 150` (~4 phút).

Ba phép đo chạy binary C++ thật dùng **script dùng-một-lần trong thư mục tạm** (theo tiền
lệ mục 8 phiên 13/8 thì nên cảnh báo: chúng sẽ mất). Cả ba đều **không cần sửa code**, mô
tả đủ để viết lại:

1. **Sweep threshold trên scan lẻ**: gọi `select_maps_by_distance.py` lấy map → chạy
   binary với `argv[5]` thay đổi → `evaluate.py`. (bảng mục 3.2, 3.3)
2. **So sánh có cặp map ràng buộc thời gian**: như trên nhưng `pick_side()` được gọi trên
   danh sách frame đã cắt về `[s-W, s+W]`. (bảng mục 6.2)
3. **Tốc độ vật ↔ F1**: instance tracking ở `s±5` chia cho 10, trọng số theo số điểm, ghép
   với F1 của cùng scan. (bảng mục 4)

`select_maps_by_distance.py` có thể `import` được (`load_poses`, `load_tr`, `pick_side`) —
không cần gọi qua `subprocess`.

**Chưa đụng vào code C++. `git status` chỉ có 1 file mới (`scripts/diag_zero_tp.py`) và
file bàn giao này.**

---

## 10. Thiết kế map tích luỹ — CHỌN FRAME NGUỒN (đã đo, không phải suy đoán)

Chọn nguồn là thứ quyết định map tích luỹ ăn hay thua. Đo trước khi viết code.

### 10.1. Định nghĩa "nguồn chuẩn" — ba con số

Một bộ frame nguồn tốt phải đồng thời:

| # | tiêu chí | đo bằng | ngưỡng đạt |
|---|---|---|---|
| 1 | **đủ phủ** | số frame nguồn | ≥ 3-5 |
| 2 | **nền nhiễu thấp** | trung vị \|map−scan\| trên điểm GT **tĩnh** | ≤ ~0.3 m |
| 3 | **tín hiệu cao** | trung vị \|map−scan\| trên điểm GT **động** | tỷ lệ (3)/(2) ≥ 10 |

Gộp cả (2) và (3) vào một số duy nhất **không phụ thuộc việc chọn ngưỡng**: **AUC**.
`AUC ≥ 0.9` = bộ nguồn tốt; `AUC ≈ 0.5` = vô dụng, không ngưỡng nào cứu.

Đo được **trước khi** viết pipeline: chỉ cần dựng range image một lần cho mỗi bộ nguồn.

### 10.2. Thuật toán chọn

```
source(scan) = { j :  ||pos(j) − pos(scan)|| ≤ R      // đủ gần về KHÔNG GIAN
                 và  |j − scan|            ≥ t_min    // đủ xa về THỜI GIAN
                 và  j ≠ scan }
pos(j) = (poses[j] · Tr)[:3,3]
```

Hai tham số, **và t_min mới là cái quan trọng** — nó không tồn tại trong code hiện tại
(hiện tại `t_min` ngầm định = 1).

### 10.3. Nguồn CÓ SẴN không — tính chất của quỹ đạo, không phải của thuật toán

Số frame nguồn trung bình / (% frame có ≥ 3 nguồn):

| seq | ego m/frame | R=5,t=1 | R=10,t=10 | R=20,t=10 | R=20,t=50 |
|---|---|---|---|---|---|
| 03 | 0.70 | 16 (100%) | 13 (91%) | 42 (100%) | 3 (19%) |
| **04** | **1.46** | 6 (100%) | **0 (0%)** | 8 (100%) | **0 (0%)** |
| **06** | 1.12 | 14 (100%) | 13 (65%) | 60 (100%) | **39 (97%)** |
| 07 | 0.63 | 29 (100%) | 32 (82%) | 69 (100%) | 24 (37%) |

**seq04 có 0 nguồn ở `t_min=50` với MỌI R** — xe chạy thẳng, không bao giờ quay lại.
seq06 thì 97% số frame có ≥ 3 nguồn cũ (nhờ vòng lặp).

> Khả năng có "map cũ" là **tính chất của quỹ đạo**. Map tích luỹ giúp được seq06/07 nhiều
> hơn hẳn seq04 — và điều đó đúng cả với Go2 (đi lại trong nhà ⇒ quay lại liên tục ⇒ rất
> hợp với hướng này).

### 10.4. Đo chất lượng nguồn: nền nhiễu, tín hiệu, AUC

Voxel 0.2 m, thang 64×900, ground fit **một lần** trên scan rồi dùng chung cho cả map
(đúng đề xuất 1.7). `--` = không có frame nguồn nào.

**seq06/939** — bicyclist 0.31 m/frame, hiện F1 = **0.000**:

| R | t_min | #frame | nền nhiễu | tín hiệu | tỷ lệ | AUC abs | AUC 1 chiều |
|---|---|---|---|---|---|---|---|
| 5 | **1** | 15 | 0.19 | **0.37** | **1.9** | **0.629** | 0.572 |
| 5 | **5** | 8 | 0.19 | **22.22** | **115** | **0.978** | 0.986 |
| 10 | 1 | 31 | 0.22 | 0.42 | 1.9 | 0.629 | 0.535 |
| 10 | 10 | 16 | 0.21 | 22.22 | 104 | 0.978 | 0.988 |
| 20 | 1 | 90 | 0.31 | 0.42 | 1.4 | 0.573 | 0.569 |
| 20 | 20 | 58 | 0.29 | 16.01 | 54 | 0.920 | 0.950 |

**seq06/349** — xe 0.56 m/frame, hiện F1 = **0.000**:

| R | t_min | #frame | nền nhiễu | tín hiệu | tỷ lệ | AUC abs | AUC 1 chiều |
|---|---|---|---|---|---|---|---|
| 10 | 1 | 14 | 0.17 | 0.15 | 0.9 | **0.484** | 0.394 |
| 10 | 5 | 6 | 0.17 | 1.96 | 11.7 | 0.776 | 0.637 |
| 20 | 10 | 52 | 0.39 | 9.59 | 24.4 | **0.878** | 0.912 |
| 20 | 20 | 40 | 0.49 | 2.96 | 6.0 | 0.724 | 0.838 |

**seq04/150** — xe 1.4 m/frame, hiện F1 = 0.727 (đối chứng "đang tốt"):

| R | t_min | #frame | nền nhiễu | tín hiệu | tỷ lệ | AUC abs | AUC 1 chiều |
|---|---|---|---|---|---|---|---|
| 5 | 1 | 6 | 0.15 | 4.09 | 26.6 | 0.855 | **0.040** |
| 10 | 5 | 4 | 0.20 | 7.54 | 37.9 | 0.929 | 0.185 |
| 20 | 10 | 9 | 0.23 | 15.04 | 65.2 | **0.974** | 0.322 |

### 10.5. Bốn quy tắc rút ra

**(1) `t_min` là núm chính, không phải `R`.** seq06/939: đổi `t_min` 1 → 5 làm tỷ lệ
tín hiệu/nhiễu nhảy **1.9 → 115**, AUC **0.629 → 0.978**. Đổi `R` 5 → 20 thì gần như
không đổi gì. *Map phải đủ CŨ, không cần đủ DÀY.*

**(2) `R` chỉ có hai vai trò, và một cái là giá phải trả.**
Vai trò tốt: mở rộng để **có** nguồn ở `t_min` mong muốn (seq04 bắt buộc R=20 mới có nguồn
`t_min=10`). Giá: nền nhiễu tăng theo R — seq06/349 đo được **0.12 → 0.35 → 0.49 m** khi
R đi 5 → 20 (sai số pose + lệch góc nhìn tích luỹ).
→ **Chọn R nhỏ nhất mà vẫn đủ nguồn.**

**(3) Chọn `t_min` theo tốc độ vật chậm nhất muốn bắt**, không phải theo cảm tính:
`t_min ≳ (kích thước vật + biên) / (tốc độ vật × 0.1 s)`.
Bicyclist 0.31 m/frame + vật 2 m → `t_min ≈ 7`. Người 0.16 m/frame → `t_min ≈ 10-13`.
Có **cao nguyên rộng**: seq06/939 ở R=5 cho kết quả **y hệt nhau** với t_min = 5/10/20/50
(vì cả 8 nguồn đều đến từ lượt đi trước, cách ~830 frame). Núm này không nhạy — tin tốt.

**(4) `t_min` quá lớn + `R` quá nhỏ = 0 nguồn.** Không có cảnh báo, chỉ là tập rỗng.
Code phải xử lý trường hợp này tường minh (fallback: nới R, hoặc bỏ qua scan).

### 10.6. ĐÍNH CHÍNH: luật MỘT CHIỀU không phải mặc định đúng

Cột "AUC 1 chiều" bác bỏ đề xuất tôi đưa ra trước khi đo:

| | AUC abs | AUC 1 chiều |
|---|---|---|
| seq04/150 (R=20, t=10) | **0.974** | **0.322** |
| seq06/939 (R=5, t=5) | 0.978 | **0.986** |
| seq06/349 (R=20, t=10) | 0.878 | **0.912** |

**Cơ chế:** map tích luỹ chứa **vệt** của chính vật động. Vật chạy dọc theo tia nhìn (xe
đi cùng chiều đường, như seq04) để lại vệt **trên đúng những pixel đó**, trong đó có những
vị trí **gần hơn** vị trí hiện tại → `min()` của pixel lấy điểm vệt → `map − scan < 0` →
luật một chiều **mù**. AUC 0.322 nghĩa là còn tệ hơn đoán bừa theo chiều ngược lại.

Luật một chiều chỉ thắng khi map là **lượt đi khác hẳn** (seq06 — vật không hề có mặt
trong map). Vậy:

> **Giữ `fabs()` cho bản đầu tiên.** Chỉ chuyển sang một chiều nếu nguồn được đảm bảo
> không chứa vật (map đã làm sạch, hoặc chỉ lấy nguồn từ lượt thăm khác).
> Mục 15.4 phiên 13/8 nói "câu hỏi `fabs()` mở lại" — đã mở, đã đo, **câu trả lời là
> KHÔNG ĐỔI**, vì lý do khác với lý do cũ.

### 10.7. Kết quả kỳ vọng, ghi trước để chấm điểm sau

Với `R=5, t_min=5` (seq06) và `R=20, t_min=10` (seq04), luật ABS giữ nguyên:

- AUC 0.63 → 0.98 ở seq06/939 nghĩa là hai scan 0-TP **phải** ra khác 0. Nếu vẫn 0 thì
  lỗi nằm ở phần cài đặt, không phải ở ý tưởng.
- Tỷ lệ tín hiệu/nhiễu 24-115 nghĩa là **`threshold` hết vai trò tới hạn**: mọi giá trị
  trong khoảng ~1 đến ~5 m đều nằm giữa nhiễu và tín hiệu. Đây mới là dấu hiệu của một
  thiết kế bền — trái ngược với vách đá ở mục 12.1 phiên 13/8.
- seq04 cũng lên (AUC 0.855 → 0.974), nhưng ít hơn seq06, và **cần R lớn** vì không có
  lượt thăm lại.

Script đo: `/tmp/.../src_quality.py` (dùng-một-lần, sẽ mất — mô tả đủ ở mục 10.1/10.4 để
viết lại: gộp nguồn → voxel 0.2 → đưa về hệ scan → 1 range image → thống kê \|Δr\| tách
theo nhãn GT → AUC hạng).

---

## 11. Xác nhận giao thức đo: RANSAC trong C++ là TIỀN ĐỊNH (đo bổ sung)

Chạy `mini_removert` **3 lần** với input y hệt (seq04/150, `thr=1.0`, map 148 149 151 152):

```
7bbd2e99b23582627701ae34d515c8be  out_run1.txt
7bbd2e99b23582627701ae34d515c8be  out_run2.txt
7bbd2e99b23582627701ae34d515c8be  out_run3.txt      441 dòng, cả 3 lần
P / R / F1 = 0.669 / 0.797 / 0.727   (khớp đúng bảng mục 4)
```

Giống nhau **đến từng byte**. `pcl::SACSegmentation` dựng model nội bộ với seed cố định,
nên "RANSAC ngẫu nhiên" chỉ đúng trong Python (`diag_sign.py`), **không** đúng trong C++.

Ba hệ quả cho quy tắc "phải đo trên nhiều ground mask" (mục 9 phiên 13/8):

1. Quy tắc **vẫn đúng về nhận thức**: mask đang dùng chỉ là *một* mask hợp lệ trong nhiều
   mask, và F1 lệch ±0.05 giữa chúng (mục 11.3 phiên 13/8). Con số tuyệt đối vẫn không tin được.
2. Nhưng **không thể thực thi được bằng cách chạy lại binary** — C++ hiện không có cách nào
   sinh mask khác. Quy tắc đang treo. Đây là lý do tồn tại của **đề bài B**.
3. Bù lại: mọi **so sánh có cặp A/B trong C++ là sạch tuyệt đối** — chênh lệch quan sát được
   100% do thay đổi code, không lẫn nhiễu chạy lại. Dùng md5 `7bbd2e99...` làm **mốc
   regression** cho mọi refactor tuyên bố "không đổi kết quả".

---

## 12. ĐỀ BÀI — việc còn treo, chia theo mức khó

Tổng hợp từ mục 8 (phiên này) + mục 6 và 9 (phiên 13/8), rà lại trên code hiện tại để
xác nhận từng chỗ **vẫn còn treo thật**. Thứ tự đề nghị làm: **A → B → E**;
C xen vào lúc nào cũng được; G độc lập.

> **CẬP NHẬT trong cùng phiên:** đề bài D đã được **đo bằng mô phỏng và BÁC BỎ** trước khi
> ai viết dòng C++ nào (mục 13). Hệ quả: **F (Patchwork++) lên hạng** — xem 13.5.

| | Đề bài | Sửa ở | Cỡ | Đổi kết quả? | Nguồn |
|---|---|---|---|---|---|
| ~~**A**~~ | ~~Gộp 2 bản phép chiếu trùng lặp~~ | `filter.cpp` | ~20 dòng | **XONG 14/8** — md5 khớp | 13/8 §6.2 |
| **B** | Đưa `vote_threshold`, thang, `ground_seed` ra dòng lệnh | `main.cpp` | ~40 dòng | KHÔNG ở giá trị mặc định | 13/8 §8, §11 trên |
| | ↳ parser + `--vote-threshold` | | | **XONG 14/8** — 6/6 phép thử đạt | |
| | ↳ `--levels` | | | **XONG 14/8** — 11/11 đạt; tái lập F1 0.720 (4 thang) và 0.691 (3 thang) | |
| | ↳ `--ground-seed` | | | còn treo | |
| **C** | Một mặt phẳng ground duy nhất cho scan + map | `main.cpp` | ~30 dòng + toán | CÓ, nhỏ | 13/8 §6.3, 14/8 §10.4 |
| ~~**D**~~ | ~~Chặn lây nhiễm theo pixel~~ | — | — | **ĐÃ ĐO, BÁC BỎ** → §13 | 13/8 §6.1, 14/8 §8.2 |
| **E** | **Map tích luỹ** | `main.cpp` + script chọn nguồn | ~150 dòng | CÓ, hạng 1 | 14/8 §8.1, §10 |
| F | Patchwork++ thay `ground_filter.cpp` | module mới | lớn, dep ngoài | CÓ | 13/8 §9.6 |
| G | Điều tra seq07/441: xuyên tâm vs tiếp tuyến | Python | script đo | không sửa code | 14/8 §8.4 |

---

### Đề bài A — gộp 2 bản phép chiếu (khởi động)

**Chỗ sai:** `filter.cpp:18-36` chép tay lại đúng công thức của
`range_image::projectToPixel` (`range_image.cpp:10-38`), trong khi `getObservedIndices` ngay
bên dưới (`filter.cpp:59`) thì gọi hàm chung. Hiện hai bản khớp nhau nên **chưa gây sai số**.

**Vì sao vẫn phải làm** (lý do gốc là "làm trước D", nhưng D đã bị bác bỏ ở mục 13 — lý do
còn lại vẫn đủ): đề bài E dựng lại toàn bộ vòng lặp `main.cpp:92-134` và đổi hệ toạ độ làm
việc. Nếu lúc đó hai bản phép chiếu lệch nhau, `vote_count` và `observed_count` sẽ nói về
**hai pixel khác nhau** cho cùng một điểm — bug không crash, không cảnh báo, chỉ làm F1 tụt
một cách vô lý. Đây cũng là bài khởi động tốt nhất vì nó có **tiêu chí chấm tuyệt đối**
(md5), không phụ thuộc mọi thứ phương sai ở mục 11.

**Yêu cầu:** `getDynamicIndices` gọi `range_image::projectToPixel`, xoá phần tính tay.
Chú ý `projectToPixel` nhận `v_min`/`v_max` bằng **radian**, còn tham số hàm đang là **độ** —
`getObservedIndices` đã đổi đơn vị ở `filter.cpp:54-55`, làm y hệt.

**Tiêu chí chấm:** chạy lại đúng lệnh ở mục 11 → `md5sum` phải ra **`7bbd2e99b23582627701ae34d515c8be`**.
Lệch một bit là refactor đã đổi hành vi → tìm chỗ hai công thức vốn không khớp.

---

### Đề bài B — `vote_threshold`, cấu hình thang, và `ground_seed` thành tham số dòng lệnh

**Vì sao:** hai lần sweep gần nhất (`vote_threshold` mục 13, thang phân giải mục 14 phiên
13/8) phải **sed vào `main.cpp` rồi build lại**, script dùng-một-lần đã mất, số liệu không
tái tạo được. Và mục 11 trên cho thấy quy tắc "đo trên nhiều ground mask" **hiện không
thực thi được** trong C++.

**Yêu cầu:** thêm tham số **có tên** (đừng thêm vào cuối dãy positional — `map_idx` đang là
biến-độ-dài, nhét thêm vào sẽ nhập nhằng):

```
--vote-threshold <float>     mặc định 0.5      (đang hard-code main.cpp:167)
--levels 64x900,32x450,16x225,8x112             (đang hard-code main.cpp:70-75)
--ground-seed <int>          mặc định 0 = giữ nguyên hành vi cũ
```

**Cách phân tích tham số.** `argv[1..5]` giữ nguyên positional. Từ `argv[6]` trở đi, duyệt
một vòng: gặp chuỗi bắt đầu bằng `--` thì đọc `argv[i+1]` làm giá trị rồi nhảy 2; ngược lại
coi là `map_idx` rồi nhảy 1. Nhờ vậy cờ đặt ở đâu cũng được, lẫn giữa các `map_idx` cũng được.

> **Cờ lạ phải BÁO LỖI VÀ THOÁT, không được lặng lẽ bỏ qua.** Gõ nhầm `--vote-thresh` mà
> chương trình vẫn chạy nghĩa là bạn tưởng đã sweep nhưng thực ra chạy 10 lần cùng một cấu
> hình — mất cả buổi mới nhận ra. Thiếu giá trị sau cờ cũng vậy.

**`--levels`**: `"64x900,32x450"` → tách theo `,` rồi theo `x`. Trường `name` của
`struct Resolution` (`main.cpp:69`) đang là `const char*` trỏ vào literal — nếu sinh tên
động thì phải đổi sang `std::string`, vì gán `.c_str()` của một `std::string` tạm sẽ để lại
**con trỏ treo**: in ra rác hoặc crash, mà chỉ crash ĐÔI KHI.

**`--ground-seed k`** (k≠0): xáo trộn **thứ tự điểm** bằng RNG seed `k` trước khi fit RANSAC,
để lấy một mặt phẳng hợp lệ khác. Đây là cách rẻ nhất sinh mask khác mà **không** đổi
thuật toán (đổi `distance_threshold` thì trộn lẫn hai hiệu ứng, không dùng). Chữ ký
`ground_filter::detectGroundMask` (`ground_filter.hpp:12-14`) nhận thêm `int seed = 0`.
Truyền **cùng một seed** cho cả ground của scan (`main.cpp:90`) lẫn của map (`main.cpp:102`).

> **CẠM BẪY chí tử:** `evaluate.py` so index của bạn với `.label` theo **thứ tự điểm gốc**.
> Nếu xáo trộn cloud rồi trả index của cloud đã xáo, mọi F1 sẽ thành rác mà **không có
> thông báo lỗi nào** — không crash, không cảnh báo, chỉ là những con số vô nghĩa trông rất
> giống số thật. Cách làm đúng: dựng bảng hoán vị `perm` (0..n-1, `std::shuffle` với
> `std::mt19937(seed)`), fit trên **bản copy đã xáo**, rồi trả mask về hệ index gốc:
> `is_ground[perm[j]] = true` cho mỗi inlier `j`.

**Tiêu chí chấm:**
1. Không cờ nào → md5 `7bbd2e99b23582627701ae34d515c8be` (không đổi hành vi). `--ground-seed 0`
   cũng phải ra đúng md5 đó.
2. `--levels 64x900,32x450,16x225` (bỏ thang thứ 4) phải tái tạo mốc **F1 TB 0.691** ở bảng
   mục 14 phiên 13/8. Đây là bài kiểm tra rằng tham số hoá **cắm đúng chỗ** — nếu ra 0.720
   thì cờ đang bị bỏ qua. **Phải chạy đúng bộ 5 scan `50/100/150/200/250`** của mốc đó, KHÔNG
   phải bộ scan của `run_generalization_test.py` (bộ đó cho 0.649 — lệch 0.071 chỉ vì đổi
   scan, mục 16.5 phiên 13/8).
3. `--ground-seed 1..5` cho 5 mask khác nhau, in ra số điểm ground mỗi lần. Phải xê xích
   vài % quanh **~50%** tổng số điểm (mốc 50.2%, mục 3.1 phiên 12/8): giống hệt nhau = xáo
   trộn không có tác dụng; lệch chục % = đã xáo nhầm cái gì đó.

Xong B thì mọi đề bài sau mới **chấm điểm được** theo quy tắc đa-mask.

---

### Đề bài C — MỘT mặt phẳng ground duy nhất, dùng cho cả scan và map

**Chỗ sai:** `main.cpp:90` fit RANSAC trên scan, `main.cpp:102` fit RANSAC **riêng** trên
từng map. Hai mặt phẳng khác nhau → một pixel bị loại ground ở ảnh này mà không bị loại ở
ảnh kia sẽ cho `|scan − map|` **rất lớn một cách giả tạo**, tức là bơm thẳng vào nền nhiễu
0.1-1.0 m ở mục 3.3 — đúng thứ mà mục 8.2 nói phải hạ. Mục 10.4 phiên này đã đo map tích
luỹ theo cách "fit **một lần** trên scan rồi dùng chung", tức là đã ngầm chọn cách này.

**Yêu cầu:** lấy hệ số mặt phẳng `(a,b,c,d)` từ **một** lần fit trên scan (trong hệ toạ độ
scan), rồi phân loại điểm map bằng **cùng mặt phẳng đó** đã đổi sang hệ map. Số lần gọi
RANSAC đi từ `1+N` xuống **1**.

> **CẠM BẪY toán học:** mặt phẳng **không** biến đổi như điểm. Đừng nhân `T` với `(a,b,c,d)`.
> Với `T` là phép biến đổi điểm scan→map (chính là `relative_pose`, `main.cpp:99`), mặt phẳng
> biến đổi theo **nghịch đảo chuyển vị**:
> ```
> p_map = T⁻ᵀ · p_scan            với p = (a, b, c, d)ᵀ
> ```
> Viết ra thành phần cho `T = [R | t]`:  `n' = R·n`,  `d' = d − (R·n)ᵀ·t`.
> Vì `R` bảo toàn chuẩn nên `‖n'‖ = 1` (PCL trả `(a,b,c)` đã chuẩn hoá) → khoảng cách
> `|n'ᵀp + d'|` vẫn ra **mét**. Cho `assert` kiểm `‖n'‖ ≈ 1`, sai dấu hay quên chuyển vị sẽ
> lộ ngay ở đó.

**Tiêu chí chấm** (đây là thay đổi đụng ground → **một lần chạy không đủ**, quy tắc mục 9
phiên 13/8, và giờ đã làm được nhờ đề bài B):
1. In số điểm ground của map trước/sau. Phải **cùng cỡ** (±5%). Lệch chục % = sai công thức.
2. So sánh **có cặp** trên **≥ 2 sequence** (seq04 + seq06 tối thiểu), **× 3 `--ground-seed`**.
   Báo cáo trung bình ± độ lệch. Kết luận chỉ được rút nếu chênh lệch **vượt** độ lệch đó.
3. Ghi cả trường hợp **xấu đi**. Có một cơ chế thật khiến nó có thể xấu: nếu mặt đường ở
   chỗ map nghiêng khác chỗ scan thì mặt phẳng dùng chung sẽ sai ở map. Với baseline 4 m
   thì khó, nhưng phải đo mới biết.

---

### ~~Đề bài D — chặn lây nhiễm theo pixel~~ → **ĐÃ BÁC BỎ, xem mục 13**

Đề bài này đã được đo bằng mô phỏng ngay trong phiên (`scripts/diag_pixel_bleed.py`) và
**bác bỏ** trước khi viết dòng C++ nào. Đừng code nó. Đọc mục 13 để biết vì sao, và để
lấy hai thứ có giá trị rút ra từ đó: một **cơ chế mới giải thích được vì sao REVERT đa
thang hoạt động**, và một **núm điều khiển precision** dùng được cho mục tiêu khác.

---

### Đề bài E — MAP TÍCH LUỸ (hạng 1, việc lớn nhất còn lại)

Thiết kế **đã đo xong** ở mục 10 phiên này, không phải suy đoán. Đọc kỹ mục 10.5 (bốn quy
tắc) và 10.6 (đính chính về `fabs()`) trước khi viết dòng nào.

**E1 — script chọn nguồn** (Python, theo tiền lệ `select_maps_by_distance.py`):
```
source(scan) = { j : ‖pos(j) − pos(scan)‖ ≤ R  và  |j − scan| ≥ t_min  và  j ≠ scan }
pos(j) = (poses[j] · Tr)[:3,3]
```
`t_min` **là núm chính, không phải `R`** (§10.5 quy tắc 1: seq06/939 đổi `t_min` 1→5 làm
tỷ lệ tín hiệu/nhiễu nhảy 1.9 → 115; đổi `R` 5→20 gần như không đổi gì). Chọn `R` **nhỏ
nhất mà vẫn đủ nguồn**, vì nền nhiễu tăng theo `R` (đo được 0.12 → 0.49 m khi R đi 5 → 20).
Tập rỗng là **có thật** (seq04 `t_min=50` cho 0 nguồn với **mọi** R — xe chạy thẳng, không
bao giờ quay lại) → phải xử lý tường minh, không được để thành tập rỗng im lặng.

**E2 — gộp map trong C++.** Thay vòng lặp `main.cpp:92-134`:
1. Mỗi frame nguồn `j`: load, biến đổi về **hệ toạ độ SCAN** bằng `T = scan_pose⁻¹ · pose_j`,
   nối vào một cloud duy nhất.
2. `pcl::VoxelGrid` **0.2 m** trên cloud gộp.
3. Fit ground **một lần** trên scan — và vì mọi thứ giờ ở hệ scan, mặt phẳng đó **dùng
   trực tiếp** cho cả map, không cần biến đổi gì (đề bài C tự tan biến ở đây).
4. Dựng **một** range image cho map, **một** cho scan, ở mỗi thang.

> **Đổi hệ toạ độ là bắt buộc, và là một sửa lỗi thiết kế.** Code hiện tại làm **ngược**:
> `main.cpp:100` đưa scan vào hệ **map**, nên ảnh range của scan bị dựng lại từ **điểm nhìn
> của map** — scan bị nội suy lại, tự-che-khuất thay đổi. Removert gốc đưa map về hệ scan.
> Làm đúng chiều còn được thêm: `4` ảnh scan thay vì `4×N`, và ảnh scan là ảnh sensor thật.
>
> **GIỮ `fabs()`.** Mục 10.6 đã đo và **bác bỏ** đề xuất luật một chiều: map tích luỹ chứa
> **vệt** của chính vật động, vệt đó có chỗ **gần hơn** vị trí hiện tại → `min()` lấy điểm
> vệt → `map − scan < 0` → luật một chiều **mù**. AUC 1 chiều ở seq04/150 chỉ **0.322**,
> tệ hơn đoán bừa. Chỉ chuyển sang một chiều khi nguồn được đảm bảo không chứa vật.
>
> **ĐỪNG bóc tầng voting ra.** Với 1 map gộp thì `vote_count/observed_count` suy biến về
> `{0,1}/{0,1}`, `vote_threshold=0.5` vẫn cho đúng "dynamic ⟺ observed và vote". Không cần
> sửa `main.cpp:164-210`, và giữ lại thì sau này còn dùng được cho nhiều map-theo-lượt-thăm.
>
> **`threshold=1.0` HẾT HIỆU LỰC.** Nó neo vào quãng dịch 1.4 m/frame của seq04 (§12.2
> phiên 13/8). Với map tích luỹ, phải **sweep lại từ đầu**.

**Tiêu chí chấm — ghi TRƯỚC khi chạy** (§10.7, đây là phần quan trọng nhất của đề bài này):
1. `R=5, t_min=5` ở seq06: **06/939 và 06/349 bắt buộc phải khác 0**. AUC đo được đi từ
   0.63 → 0.98; nếu vẫn ra F1 = 0.000 thì **lỗi ở phần cài đặt, không phải ở ý tưởng** —
   đừng kết luận "map tích luỹ không ăn".
2. `threshold` phải có **cao nguyên rộng**: mọi giá trị trong khoảng ~1 đến ~5 m cho kết quả
   tương đương (vì tỷ lệ tín hiệu/nhiễu 24-115). **Đây là tiêu chí phân biệt quan trọng
   nhất** — nếu vẫn thấy vách đá như §12.1 phiên 13/8 thì map gộp chưa thật sự cũ, kiểm
   `t_min` và số nguồn thực tế đã dùng.
3. seq04 cũng lên (AUC 0.855 → 0.974) nhưng **ít hơn** seq06, và **cần `R=20`** vì không có
   lượt thăm lại. seq04 lên nhiều hơn seq06 = dấu hiệu đo sai.
4. In số nguồn thực dùng cho mỗi scan. Đây là thứ đầu tiên phải xem khi kết quả lạ.

**Tôi sẽ làm phần chấm điểm:** dựng lại `src_quality.py` đã mất (gộp nguồn → voxel 0.2 →
hệ scan → 1 range image → thống kê `|Δr|` tách theo nhãn GT → AUC hạng) thành
`scripts/src_quality.py`. Nó cho biết bộ nguồn **có tốt hay không trước khi** bạn viết C++,
và tách được "ý tưởng sai" khỏi "cài đặt sai" ở tiêu chí 1.

---

### Đề bài F — Patchwork++ (**đã lên hạng**, xem 13.5)

Lý do đã đổi hai lần. Ban đầu (12/8 §7.1): vì ground chiếm 67.5% FP. Rồi (13/8 §11.4): vì
`ground_filter.cpp` bơm ±0.05 F1 nhiễu vào toàn hệ. Giờ có lý do thứ ba và mạnh nhất:
**đề bài D — cách rẻ để dập FP-ground mà không đụng tới ground segmentation — đã bị bác bỏ**
(mục 13). Không còn đường tránh nữa. Xem 13.5.

### Đề bài G — seq07/441: xuyên tâm vs tiếp tuyến (Python, không sửa code)

Tốc độ vật giải thích r² ≈ 0.53 (§4), còn ~47% phương sai ở đâu chưa biết. Nghi: **hướng
dịch chuyển so với tia nhìn**. Range image chỉ đo thành phần **xuyên tâm**; vật đi ngang
dịch nhiều mà range đổi rất ít. seq07/441 (0.489 m/frame nhưng F1 0.546) là ngoại lệ để
kiểm: tách vector dịch chuyển instance thành phần xuyên tâm/tiếp tuyến, tính lại tương quan
với F1 **chỉ bằng thành phần xuyên tâm**. Nếu r vọt lên thì đã tìm được biến thứ hai.

---

## 13. Đề bài D — ĐO VÀ BÁC BỎ ngay trong phiên (chưa đụng C++)

Script: `scripts/diag_pixel_bleed.py`. Mô phỏng numpy, đọc thẳng `.bin`+`.label`, chạy ở
đúng cấu hình chốt (`threshold=1.0`, 4 thang, `vote_threshold=0.5`, N=4, max_dist=4 m).
Số liệu dưới đây: **seq04 + seq06, 5 scan mỗi seq (giao thức §16.1 phiên 13/8), 2 ground
mask = 20 lần chạy**, mọi luật dùng **chung** mask và chung map ⇒ so sánh có cặp.

### 13.1. Cơ chế thì ĐÚNG như dự đoán — 64% điểm bị flag là "ăn ké"

| | |
|---|---|
| điểm không-ground rơi vào pixel bị flag `disc > 0` (cộng dồn) | 344,780 |
| trong đó **là chủ của `min`** — cũng chính là số pixel bị flag, vì mỗi pixel có đúng một chủ | 122,963 (**35.7%**) ⇒ **2.80 điểm/pixel** |
| trong đó **ăn ké** (không phải `min`) | 221,817 (**64.3%**) |

Khớp gần như chính xác dự đoán ở §6.1 phiên 13/8 ("2.7 điểm/pixel, trần lý thuyết ~60%").
Đây là lần đầu con số đó được **đo** thay vì suy ra từ mật độ.

### 13.2. Nhưng giả định NGẦM phía sau đề bài thì SAI

Đề bài D ngầm giả định *ăn ké ≈ báo nhầm*. Tách theo nhãn GT:

| nhóm | số điểm | là GT dynamic |
|---|---|---|
| chủ của `min` | 122,963 | 9,062 (**7.4%**) |
| **ăn ké** | 221,817 | 13,417 (**6.0%**) |

Tỷ lệ 6.0% / 7.4% = **0.81**. Ăn ké chỉ "sạch" hơn chủ-min **19%**, chứ không phải sạch hơn
nhiều lần. Muốn chặn ăn ké mà có lãi thì tỷ lệ này phải cỡ 0.1.

**Vì sao:** một chiếc xe chiếm nhiều điểm trong cùng pixel. Điểm `min` là mặt trước xe,
những điểm "ăn ké" phần lớn là **phần còn lại của chính chiếc xe đó** — cũng là GT dynamic
thật. Hình ảnh "xe 25 m + tường 30.2 m + tường 31.0 m" trong §6.1 có tồn tại, nhưng nó là
**thiểu số**; đa số là "xe 25 m + xe 25.4 m + xe 26.1 m".

### 13.3. Sweep xác nhận: mọi biến thể đều ≤ hiện tại

Baseline mô phỏng: F1 **0.408**, P 0.498, R 0.384, TP 270.0, FP 114.6.

| luật | F1 | P | R | TP | FP | ΔF1 |
|---|---|---|---|---|---|---|
| `inf` (hiện tại) | **0.408** | 0.498 | 0.384 | 270.0 | 114.6 | — |
| eps=1.0 @L0 | 0.409 | 0.526 | 0.368 | 259.3 | 84.2 | **+0.001** |
| eps=0.5 @L0 | 0.403 | 0.526 | 0.360 | 251.6 | 82.5 | −0.005 |
| eps=0.1 @L0 | 0.374 | 0.540 | 0.311 | 215.9 | 65.3 | −0.035 |
| eps=0.0 @L0 | 0.176 | 0.487 | 0.112 | 78.2 | 36.0 | −0.232 |
| eps=1.0 @all | 0.374 | 0.643 | 0.290 | 197.6 | **19.5** | −0.034 |
| eps=0.3 @all | 0.275 | 0.586 | 0.199 | 126.9 | 15.4 | −0.134 |
| eps=0.0 @all | 0.007 | 0.317 | 0.004 | 2.1 | 3.5 | −0.401 |

Tốt nhất là **+0.001**, trong khi độ lệch theo (scan × mask) là **0.32**. Không phân biệt
được với 0. Đúng dự đoán "recall sập" ở cạm bẫy 1 — chỉ là nó sập **nhanh hơn** mức
precision tăng, ở mọi `eps`.

### 13.4. Luật thay thế cũng thất bại — và thất bại theo cách CÓ GIÁ TRỊ

Thấy `eps` thất bại vì cắt mất phần sau của vật, tôi thử luật hợp lý hơn hẳn: bỏ hoàn toàn
`scan_img`, so **range của CHÍNH điểm** với map:

```
flag điểm i  ⟺  | map_img[pixel_i] − dist_i | > threshold
```

Luật này về nguyên tắc giải được đúng ví dụ của §6.1: xe 26 m vẫn vượt ngưỡng so với map
30.2 m (**giữ recall**), còn tường 30.2 m thì trùng map (**hết FP**).

| luật | F1 | P | R | TP | FP | ΔF1 |
|---|---|---|---|---|---|---|
| per-point @L0 | 0.406 | 0.496 | 0.383 | 268.6 | 112.3 | −0.002 |
| **per-point @all** | **0.076** | **0.043** | 0.425 | 273.6 | **6071.0** | **−0.333** |
| pixel & per-point @all | 0.385 | 0.516 | 0.339 | 230.6 | 84.2 | −0.024 |

@L0 thì ngang hệt hiện tại. @all thì **FP nổ 53 lần** (114.6 → 6071), precision còn 0.043.

> **ĐÂY LÀ PHÁT HIỆN ĐÁNG GIÁ NHẤT CỦA CẢ ĐỀ BÀI D, và nó giải thích một thứ dự án chưa
> từng giải thích được: vì sao REVERT đa thang lại hoạt động.**
>
> Ở thang thô (8×112), `min` của một pixel là vật gần nhất trong một **hình nón rất rộng**.
> Đó là một estimator **chệch rất mạnh** — range của pixel nhỏ hơn hẳn range của đa số điểm
> trong đó. Nhưng `computeDiscrepancy` so **hai ảnh cùng bị chệch y như nhau**, nên độ chệch
> **triệt tiêu**. Luật per-point phá vỡ tính đối xứng đó: nó so một đại lượng chệch
> (`map_img` thô) với một đại lượng không chệch (`dist_i`) → phần chệch còn nguyên và bị đọc
> thành "dynamic" ở gần như mọi điểm.
>
> Từ 12/8 tới giờ REVERT vẫn được giải thích là "thang thô triệt tiêu nhiễu ngẫu nhiên"
> (`main.cpp:54-68`). Giải thích đó **chưa đủ**: cái làm nó chạy được là **HAI ẢNH PHẢI ĐI
> QUA CÙNG MỘT PHÉP RÚT GỌN**. Hệ quả thực dụng cho mọi thay đổi sau này: đừng bao giờ so
> một đại lượng đã qua `min()` với một đại lượng chưa qua `min()`. Đây là lý do kỹ thuật để
> **`computeDiscrepancy` phải giữ nguyên dạng ảnh-với-ảnh**, không được "tối ưu" thành
> điểm-với-ảnh.

### 13.5. Hai thứ mang đi được

**(1) Núm điều khiển PRECISION cho mục tiêu khác F1.** `eps @all` đánh đổi rất mạnh và
rất đơn điệu. Đo riêng trên seq04 (5 scan, 1 mask):

| luật | P | R | F1 | FP |
|---|---|---|---|---|
| hiện tại | 0.781 | 0.637 | 0.656 | 135.2 |
| eps=1.0 @all | **0.951** | 0.490 | 0.608 | **17.8** |

**Cắt 87% FP để mất 23% recall.** F1 xấu đi nên **không dùng cho mốc hiện tại** — nhưng mục
tiêu của lifelong mapping *không phải* F1: xoá oan một điểm tường là làm hỏng bản đồ vĩnh
viễn, còn bỏ sót một điểm xe thì lần thăm sau còn sửa được. Nếu sau này chuyển sang hàm mục
tiêu bất đối xứng thì **quay lại lấy `eps @all` ở đây**, đừng đo lại từ đầu.

**(2) Patchwork++ (đề bài F) lên hạng.** Đề bài D vốn là **đường tránh rẻ**: dập FP-ground
mà không phải thay ground segmentation (§11.5 phiên 13/8 gọi lây nhiễm pixel là "cơ chế
khuếch đại sai số ground thành hàng trăm FP"). Đường tránh đó vừa bị bịt: 13.2 cho thấy
điểm ăn ké **không** giàu FP hơn điểm chủ-min, nên FP-ground **không đi qua** cơ chế khuếch
đại này. Nó đến trực tiếp từ chỗ khác: **mô hình MỘT mặt phẳng không mô tả được vỉa hè**
(RANSAC bỏ sót 35.9% sidewalk, `HANDOFF_VIEC4.md` §12) → những điểm đó không nằm trong
ground mask, sinh discrepancy thật, và bị tính là FP một cách hoàn toàn "hợp lệ" theo
thuật toán. Không có cách nào vá ở tầng gán nhãn. **Phải sửa mô hình ground.**

Xếp hạng sau phiên này: **E (map tích luỹ) > F (Patchwork++) > C > B > A**, và A vẫn nên
làm trước vì rẻ nhất.

### 13.6. Ghi nhận về phương pháp

Đề bài D ước lượng ~50 dòng C++ + một buổi sweep. Mô phỏng numpy bác bỏ nó trong **~20
phút**, và còn trả về hai thứ ở 13.5 mà bản C++ sẽ không cho (vì sẽ không ai chạy
`per-point @all` trong C++ — nó trông quá vô lý để thử). Đây là lần thứ **tư** phương pháp
"script đo đạc độc lập trước, sửa core sau" chặn được một thay đổi nghe rất hợp lý:

| lần | giả thuyết | kết cục |
|---|---|---|
| 1 | RANSAC detect 69.5% ground, quá cao | bác bỏ, 12/8 §3.1 |
| 2 | RANSAC gom nhầm tường dài | bác bỏ, 12/8 §3.2 |
| 3 | bỏ `fabs()` (dấu discrepancy) | bác bỏ, 13/8 §2 |
| **4** | **chặn lây nhiễm pixel** | **bác bỏ, mục này** |
