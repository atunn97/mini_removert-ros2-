# Bàn giao phiên 2026-08-17 — `src_quality.py` + ĐỀ BÀI B ĐÓNG LẠI (`--ground-seed`)

> Hai phần: (1) dựng lại `scripts/src_quality.py`, món còn nợ ở mục 12 phiên 14/8 —
> mục 2-5 dưới đây; (2) chủ repo tự cài `--ground-seed`, đóng nốt đề bài B — mục 7.
> Nối tiếp `HANDOFF_2026-08-14.md`.

---

## 1. Tóm tắt 1 phút

| # | Việc | Kết luận |
|---|---|---|
| 1 | Dựng lại script chấm chất lượng nguồn đã mất (mục 10.7 phiên 14/8) | **XONG** — `scripts/src_quality.py` |
| 2 | Bản dựng lại có khớp bảng gốc mục 10.4 không? | **CÓ** — lệch ≤ 0.005 AUC ở các dòng chính |
| 3 | Phép đo MỚI, chưa có ở mục 10.4: **hồ sơ ngưỡng** | tách bạch **cao nguyên** với **vách đá** rõ hơn cả AUC |
| 4 | Cấu hình nguồn nên dùng khi viết C++ | seq06: `R=5, t_min=5`; seq04: `R=20, t_min=10` (không có lựa chọn khác) |
| 5 | **Đề bài B** — `--ground-seed` | **XONG**, 3/3 tiêu chí đạt (mục 7) |
| 6 | Số mới quan trọng nhất phiên này | **mốc 0.720 là mẫu MAY** — trung thực là **0.707 ± 0.023**, biên độ mask 0.066 |
| 7 | Trạng thái đề bài sau phiên | **E → F**; C tự tan biến nếu làm E; G độc lập |

---

## 2. `scripts/src_quality.py` — nó trả lời câu hỏi gì

Câu hỏi: **với `(R, t_min)` này, bộ frame nguồn có đủ tốt để tách động/tĩnh không** —
trả lời được **trước khi** viết dòng C++ nào cho đề bài E.

Đây chính là thứ tách "ý tưởng sai" khỏi "cài đặt sai" ở tiêu chí chấm số 1 của đề bài E:

- AUC ở đây **thấp** → bản C++ dù viết đúng cũng không thể ăn. Đừng viết.
- AUC ở đây **cao** mà C++ ra F1 = 0 → **lỗi ở cài đặt**, không phải ở ý tưởng. Đi tìm bug.

Chuỗi phép đo (đúng mô tả ở mục 10.7 phiên 14/8):

```
chọn nguồn theo (R, t_min)  →  gộp về HỆ TOẠ ĐỘ SCAN  →  voxel 0.2 m (trọng tâm)
  →  ground fit MỘT LẦN trên scan, dùng CHUNG hệ số cho map
  →  1 range image 64×900 cho map + 1 cho scan
  →  Δr = map_img[px] − scan_img[px], tách theo nhãn GT  →  AUC hạng
```

Ba chỗ cố ý làm khác code C++ hiện tại, và đều là **thiết kế của đề bài E**, không phải
tuỳ tiện:

1. **Chiều biến đổi ngược lại.** Script đưa map về hệ scan; `main.cpp:100` đang đưa scan
   về hệ map. Xem mục 10 §E2 phiên 14/8 — làm đúng chiều thì ảnh scan là **ảnh sensor
   thật**, không bị dựng lại từ điểm nhìn của map.
2. **Một mặt phẳng ground duy nhất** (đề bài C), và vì map đã ở hệ scan nên **không cần**
   biến đổi mặt phẳng — cái bẫy `T⁻ᵀ` ở đề bài C tự biến mất.
3. **Một map gộp** thay vì N map riêng, nên không có tầng voting nào ở đây.

Cách chạy:

```bash
python3 scripts/src_quality.py 06 939            # lưới (R,t_min) mặc định
python3 scripts/src_quality.py 06 939 349        # nhiều scan
python3 scripts/src_quality.py 04 150 --grid 5:1,20:10
python3 scripts/src_quality.py 06 939 --seeds 3  # 3 ground mask, in ±
```

Thời gian: ~1-4 phút/scan cho cả lưới 7 cấu hình (R=20 phải đọc 50-90 frame `.bin`).

---

## 3. Kiểm chứng: bản dựng lại có khớp bảng gốc không

Script gốc đã mất, nên phải chứng minh bản mới **tái lập được** số cũ trước khi tin nó.
So với bảng mục 10.4 phiên 14/8 (cột "gốc"):

| trường hợp | #frame | nhiễu (gốc) | tín hiệu (gốc) | AUC abs (gốc) | AUC 1 chiều (gốc) |
|---|---|---|---|---|---|
| seq06/939 `R=5,t=1` | 15 (15) | 0.17 (0.19) | 0.40 (0.37) | 0.668 (0.629) | 0.618 (0.572) |
| seq06/939 `R=5,t=5` | 8 (8) | 0.18 (0.19) | **22.30** (22.22) | **0.980** (0.978) | 0.988 (0.986) |
| seq06/939 `R=10,t=10` | 16 (16) | 0.21 (0.21) | 22.09 (22.22) | 0.975 (0.978) | 0.987 (0.988) |
| seq06/939 `R=20,t=20` | 58 (58) | 0.29 (0.29) | 14.28 (16.01) | 0.916 (0.920) | 0.950 (0.950) |
| seq06/349 `R=20,t=10` | 52 (52) | 0.39 (0.39) | 9.56 (9.59) | 0.876 (0.878) | 0.909 (0.912) |
| seq04/150 `R=5,t=1` | 6 (6) | 0.14 (0.15) | 3.89 (4.09) | 0.858 (0.855) | **0.028** (0.040) |
| seq04/150 `R=20,t=10` | 9 (9) | 0.21 (0.23) | 14.98 (15.04) | **0.975** (0.974) | 0.267 (0.322) |

**Số frame nguồn khớp tuyệt đối** (phần chọn nguồn là tiền định, không có chỗ cho khác
biệt). Các đại lượng đo lệch ≤ 0.005 AUC ở mọi dòng chính; hai nguồn lệch nhỏ còn lại là
RANSAC khác seed và voxel lấy **trọng tâm** thay vì điểm đại diện. **Kết luận của mục 10
đứng vững**, kể cả đính chính 10.6 (AUC 1 chiều ở seq04 vẫn thảm: 0.028-0.267).

---

## 4. Phép đo MỚI: hồ sơ ngưỡng — cao nguyên hay vách đá

Mục 10.4 chỉ có AUC. AUC là số **xếp hạng**, nó không nói cho biết `threshold` còn là
biến tới hạn hay không — mà đó lại chính là **tiêu chí chấm số 2** của đề bài E. Nên
script in thêm bảng thứ hai: recall tại `thr` = 0.5 / 1 / 2 / 5 m.

**seq06/939** (bicyclist 0.30 m/frame, pipeline hiện tại F1 = 0.000):

| R | t_min | thr=0.5 | thr=1.0 | thr=2.0 | thr=5.0 | |
|---|---|---|---|---|---|---|
| 5 | **1** | 0.46 | 0.24 | 0.15 | 0.15 | **vách đá** — recall sụp 3× |
| 5 | **5** | **1.00** | **1.00** | **1.00** | **1.00** | **cao nguyên phẳng lì** |
| 10 | 10 | 1.00 | 1.00 | 1.00 | 1.00 | cao nguyên |
| 20 | 10 | 0.99 | 0.97 | 0.92 | 0.76 | cao nguyên nghiêng (R lớn → nhiễu lớn) |

**seq04/150** (xe 1.42 m/frame, pipeline hiện tại F1 = 0.727):

| R | t_min | thr=0.5 | thr=1.0 | thr=2.0 | thr=5.0 | |
|---|---|---|---|---|---|---|
| 5 | 1 | 0.99 | 0.99 | 0.94 | **0.03** | vách đá ở 5 m (≈ quãng dịch của vật) |
| 20 | 10 | 1.00 | 1.00 | 1.00 | 0.96 | cao nguyên |

Đọc ra ba điều:

1. **Dự đoán ở mục 10.7 đúng.** "Tỷ lệ tín hiệu/nhiễu 24-115 nghĩa là threshold hết vai
   trò tới hạn" — đo trực tiếp: recall **1.00 ở cả bốn ngưỡng** khi `t_min ≥ 5`. Đây là
   thứ đối lập hẳn với vách đá ở mục 12.1 phiên 13/8.
2. **`t_min` là núm chính, xác nhận lần hai.** Cùng `R=5`, đổi `t_min` 1 → 5 làm recall
   @1m đi từ **0.24 → 1.00**. Đổi `R` 5 → 20 (giữ `t_min` cũ) gần như không đổi gì.
3. **`R` lớn làm cao nguyên nghiêng đi.** seq06/939 `R=20`: recall @5m chỉ còn 0.76, vì
   nền nhiễu 0.30 (so với 0.18 ở `R=5`) kéo theo cả đuôi phân bố. Đúng quy tắc 2 mục 10.5
   — **chọn R nhỏ nhất mà vẫn đủ nguồn**.

> **Cạm bẫy khi đọc bảng này:** cột "% điểm tĩnh bị flag" (in trong ngoặc khi chạy script)
> rất cao — 12-40%. **Đừng đọc nó như precision của pipeline.** Ở đây chỉ có MỘT thang,
> KHÔNG voting, KHÔNG revert đa thang; dập FP là việc của ba tầng đó, không phải của bộ
> nguồn. Vì lý do y hệt, script **cố tình không in F1**: với 160 điểm động trên 34.000
> điểm, F1 của luật một-thang bé tí ở mọi ngưỡng (đo thử: 0.017-0.083) và con số đó nói
> về luật rút gọn chứ không nói gì về bộ nguồn.

---

## 5. Hệ quả trực tiếp cho đề bài E

**Cấu hình nguồn nên cắm vào bản C++ đầu tiên** (không phải suy đoán, đã đo ở trên):

| seq | `R` | `t_min` | #nguồn | AUC | ghi chú |
|---|---|---|---|---|---|
| 06 | **5** | **5** | 8 | 0.980 | rẻ nhất, tốt nhất, cao nguyên phẳng |
| 04 | **20** | **10** | 9 | 0.975 | **không có lựa chọn khác** — xem dưới |

**seq04 gần như không có map cũ.** Đo lại lần nữa trong phiên này: `R=5,t_min=5` → **0
nguồn**; `R=10,t_min=10` → **0 nguồn**; `R=20,t_min=20` → **0 nguồn**. Chỉ đúng một ô
`R=20,t_min=10` cho 9 nguồn. Xe chạy thẳng thì không có gì để tích luỹ — đúng mục 10.3.
Nên khi cài E:

- **Tập rỗng phải xử lý tường minh** (quy tắc 4 mục 10.5). Nó không phải trường hợp hiếm,
  nó là **trường hợp thường gặp của seq04** — mà seq04 lại đúng là sequence đang giữ mốc
  0.720. Fallback im lặng ở đây sẽ làm mốc tụt mà không ai biết vì sao.
- **Đừng kỳ vọng seq04 lên nhiều.** Mục 10.7 tiêu chí 3 đã ghi trước: seq04 lên **ít hơn**
  seq06; seq04 lên nhiều hơn seq06 = **dấu hiệu đo sai**.

---

## 6. Trạng thái đề bài (không đổi so với mục 12 phiên 14/8)

| | Đề bài | Trạng thái |
|---|---|---|
| ~~A~~ | Gộp 2 bản phép chiếu | **XONG 14/8** |
| **B** | `--vote-threshold`, `--levels` | **XONG 14/8**; **`--ground-seed` CÒN TREO** |
| C | Một mặt phẳng ground dùng chung | treo — **tự tan biến nếu làm E trước** |
| ~~D~~ | Chặn lây nhiễm theo pixel | **ĐÃ BÁC BỎ** (mục 13 phiên 14/8) |
| **E** | Map tích luỹ | treo, **hạng 1** — giờ đã có công cụ chấm điểm |
| F | Patchwork++ | treo, hạng 2 |
| G | seq07/441 xuyên tâm vs tiếp tuyến | treo, độc lập, Python thuần |

Thứ tự đề nghị: **B(`--ground-seed`) → E → F**. Lý do `--ground-seed` vẫn đứng trước E dù
nhỏ: mọi kết luận về E đều là so sánh "trước/sau" ở tầng ground-nhạy-cảm, mà quy tắc
"đo trên nhiều ground mask" **hiện chưa thực thi được trong C++** (mục 11 phiên 14/8).
Không có nó thì một cải tiến +0.03 F1 của E không phân biệt được với nhiễu ±0.05 của mask.

---

## 7. ĐỀ BÀI B ĐÃ XONG — `--ground-seed`, và con số ±0.05 lần đầu đo được trong C++

Chủ repo tự cài (Claude review từng bước). Cách cài **khác** mô tả ở mục 12 phiên 14/8, và
khác theo hướng tốt hơn — xem 7.1.

### 7.1. Cài bằng `setIndices`, không xáo point cloud

Mục 12 phiên 14/8 mô tả: xáo **thứ tự điểm** của cloud, fit, rồi ánh xạ inlier ngược về hệ
index gốc bằng bảng `perm`, kèm cảnh báo "cạm bẫy chí tử" (quên ánh xạ ⇒ F1 thành rác mà
không có cảnh báo nào).

Đọc source PCL 1.14 thì thấy có đường rẻ hơn và **không có cạm bẫy đó**:

| chỗ | phát hiện |
|---|---|
| `sac_model.h:96-99` | `random=false` (mặc định) ⇒ `rng_alg_.seed(12345u)` — đây là lý do gốc khiến C++ tiền định |
| `sac_model.h:470-478` | `drawIndexSample` bốc mẫu từ **`shuffled_indices_`** |
| `sac_model.h:158`, `:327` | `shuffled_indices_ = *indices_` — **giữ nguyên thứ tự** vector index truyền vào |
| `impl/sac_model_plane.hpp:213` | `inliers.push_back((*indices_)[i])` — inlier trả về ở **hệ index gốc của cloud** |
| `impl/pcl_base.hpp:72-77` | `setIndices` đặt `fake_indices_ = false` ⇒ thứ tự của ta được tôn trọng |

Nên chỉ cần: xáo một vector `0..n-1` bằng `std::mt19937(seed)` rồi `seg.setIndices(...)`.
Không copy cloud, không bảng `perm`, và vòng `is_ground[idx] = true` **không phải sửa**.

`detectGroundMask(cloud, int seed = 0, float distance_threshold = 0.2f)` — chú ý `seed` ở
**vị trí 2**. Gọi `detectGroundMask(cloud, 0.2f, seed)` theo quán tính sẽ biên dịch trót lọt
và có nghĩa là "seed = 0, ngưỡng ground = seed mét". Đã cảnh báo, chưa ai vấp.

> **KHÔNG dùng `SACSegmentation<PointT> seg(true)`.** Nó cũng cho mask khác nhau mỗi lần,
> nhưng seed lấy từ `std::random_device` nên **không tái tạo lại được** — không chạy lại
> được "mask số 3" để kiểm chứng. Với mục đích đo đạc thì đó là bước lùi.

### 7.2. Ba tiêu chí chấm — ĐẠT cả ba

| # | tiêu chí | kết quả |
|---|---|---|
| 1 | không cờ / `--ground-seed 0` → md5 `7bbd2e99b23582627701ae34d515c8be` | **ĐẠT**, khớp từng byte |
| 1b | `--ground-seed 1..5` → 5 md5 khác nhau đôi một | **ĐẠT** |
| 2 | `--levels 64x900,32x450,16x225` → tái lập F1 TB **0.691** | **ĐẠT**, ra đúng 0.691 |
| 3 | 5 mask khác nhau, mỗi mask ~50% số điểm | **ĐẠT**: 49.75 / 46.92 / 49.79 / 50.12 / 50.03 / 49.83 % |
| — | cờ gõ nhầm / thiếu giá trị → báo lỗi + exit 1 | **ĐẠT** |

### 7.3. Số mới: biên độ ground mask trong C++ = **0.066**, không phải ±0.05 ước lượng

`scripts/run_seed_sweep.py` (mới), seq04, 5 scan mốc, 4 thang, 6 ground mask:

| scan | seed 0 | seed 1 | seed 2 | seed 3 | seed 4 | seed 5 |
|---|---|---|---|---|---|---|
| 50 | 0.701 | 0.705 | 0.622 | 0.684 | 0.689 | 0.639 |
| 100 | 0.793 | 0.819 | 0.703 | 0.755 | 0.773 | 0.791 |
| 150 | **0.727** | **0.545** | 0.650 | 0.720 | 0.643 | 0.629 |
| 200 | 0.855 | 0.824 | 0.692 | 0.790 | 0.777 | 0.844 |
| 250 | **0.526** | 0.638 | 0.620 | 0.645 | 0.683 | **0.713** |
| **TB** | **0.720** | 0.706 | 0.657 | 0.719 | 0.713 | 0.723 |

```
trung binh cua 6 mask : 0.707
do lech chuan         : 0.023
min / max             : 0.657 / 0.723   (bien do 0.066)
```

Ba kết luận:

1. **Mốc 0.720 nằm ở ĐẦU LẠC QUAN của phân bố.** Nó là F1 của mask mà PCL tình cờ chọn với
   seed cứng 12345, và nó cao thứ nhì trong 6 mask. Con số trung thực để báo cáo là
   **0.707 ± 0.023**. Điều này **không** có nghĩa thuật toán tệ đi — chỉ có nghĩa 0.720 vốn
   đã là một mẫu may mắn, đúng như mục 11 phiên 14/8 nghi ngờ.
2. **Ước lượng ±0.05 từ Python là đúng bậc.** Đo trong C++ ra biên độ 0.066 (≈ ±0.033 quanh
   trung bình). Quy tắc thực dụng: **ΔF1 < 0.07 trên một mask thì KHÔNG kết luận được.**
3. **Một scan đơn lẻ còn tệ hơn nhiều.** Scan 150 chạy từ 0.545 đến 0.727 (biên độ **0.182**),
   scan 250 từ 0.526 đến 0.713 (**0.187**) — gấp gần 3 lần biên độ của trung bình 5 scan.
   Đây là bằng chứng số cho quy tắc "một scan đơn lẻ không đủ kết luận" vốn mới chỉ là kinh
   nghiệm. Và nó **đảo thứ hạng**: ở seed 0 thì scan 150 (0.727) hơn scan 250 (0.526), ở
   seed 5 thì gần bằng nhau (0.629 vs 0.713).

Từ giờ, mọi thay đổi đụng tới ground **bắt buộc** chạy `run_seed_sweep.py`, không chấp nhận
một lần chạy đơn.

---

## 8. File phiên này

| file | vai trò |
|---|---|
| `scripts/src_quality.py` | **mới** — chấm điểm bộ nguồn cho đề bài E |
| `scripts/run_seed_sweep.py` | **mới** — chạy N ground mask, báo cáo F1 ± độ lệch |
| `src/ground_filter/*`, `src/main.cpp` | `--ground-seed` (đề bài B, xong) |
| `HANDOFF_2026-08-17.md` | file này |

Lệnh tái tạo mọi con số:

```bash
python3 scripts/src_quality.py 06 939 349          # mục 3, 4
python3 scripts/src_quality.py 04 150              # mục 3, 5
python3 scripts/run_seed_sweep.py                  # mục 7.3
python3 scripts/run_seed_sweep.py --seeds 0 --levels 64x900,32x450,16x225   # tiêu chí 2
```

---

## 9. Trạng thái cuối phiên — đọc mục này trước khi làm gì tiếp

### 9.1. Đã XONG (toàn dự án)

| | |
|---|---|
| **A** | Gộp 2 bản phép chiếu trùng lặp trong `filter.cpp` (14/8) |
| **B** | `--vote-threshold`, `--levels` (14/8) + `--ground-seed` (17/8, mục 7) — **đóng lại** |
| — | Sweep `threshold` / `vote_threshold` / số thang → chốt `thr=1.0, vote=0.5`, 4 thang |
| — | Test đa sequence 03/06/07 → biết thuật toán **không** generalize |
| — | Điều tra 3 scan F1 = 0.000 → không phải bug, là giới hạn nguyên lý |
| — | Thiết kế chọn nguồn cho map tích luỹ (mục 10 phiên 14/8) + công cụ chấm (mục 2-5 phiên này) |

### 9.2. Đã BÁC BỎ — đừng đề xuất lại

Bỏ `fabs()` · luật ANTI · ràng buộc thời gian khi chọn map · tune `threshold` riêng từng
sequence · `threshold` thích ứng theo range · chặn lây nhiễm theo pixel (đề bài D) · 2 giả
định về RANSAC (12/8). **Bảy** hướng, tất cả bị bác bỏ bằng số đo, không phải bằng lập luận.

### 9.3. CÒN TREO

| | đề bài | ghi chú |
|---|---|---|
| **E** | **Map tích luỹ** — hạng 1, ~150 dòng | thiết kế đã đo xong; **giờ đã đủ cả hai công cụ chấm**: `src_quality.py` cho bộ nguồn (trước khi code), `run_seed_sweep.py` cho kết quả (sau khi code) |
| **F** | Patchwork++ thay `ground_filter.cpp` | hạng 2; là đường **duy nhất** còn lại để hạ 67.5% FP-ground sau khi D bị bác |
| **G** | seq07/441 xuyên tâm vs tiếp tuyến | Python thuần, độc lập, giải thích ~47% phương sai chưa rõ |
| ~~C~~ | Một mặt phẳng ground dùng chung | **tự tan biến nếu làm E** — mọi thứ về hệ scan thì không cần biến đổi mặt phẳng |

### 9.4. Giới hạn còn nguyên vẹn

1. **Mù với vật chậm**: < 0.6 m/frame → F1 TB 0.148. Chỉ E gỡ được.
2. **FP chủ yếu là ground**: road 40.2% + sidewalk 18.7% + terrain 8.6% ≈ 67.5%. Chỉ F gỡ được.
3. **Không generalize**: seq04 0.707 vs seq 03/06/07 0.145-0.495.
4. **~47% phương sai F1 chưa giải thích** (đề bài G).
5. **Con số tuyệt đối không tin được**: ground mask ±0.033, chọn scan ±0.07, chọn sequence
   ±0.25. Chỉ **so sánh có cặp** mới tin được.

### 9.5. Việc đầu tiên của phiên sau, nếu bắt đầu đề bài E

**KHÔNG phải viết C++.** Chạy `src_quality.py` trên bộ scan định dùng để tìm `(R, t_min)`
cho AUC ≥ 0.9 trước đã. Và nhớ cảnh báo ở mục 5: **seq04 gần như không có map cũ**
(`R=5,t=5` / `R=10,t=10` / `R=20,t=20` đều cho **0 nguồn**), nên E phải đo chủ yếu trên
seq06/07, và **mốc 0.707 của seq04 không phải thước đo tốt cho E**.
