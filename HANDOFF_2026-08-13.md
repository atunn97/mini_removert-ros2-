# Bàn giao phiên 2026-08-13 — review `discrepancy` + `filter`, bác bỏ giả thuyết `fabs()`

> Gần như toàn bộ phiên là đọc code + đo bằng script Python độc lập, **không sửa `.cpp`**.
> Chỉ ở bước cuối (mục 14.2) mới áp dụng đúng **một dòng** vào `main.cpp`: thêm thang
> phân giải thứ 4. Script chẩn đoán để trong `scripts/` (mục 8).
>
> Nối tiếp `HANDOFF_2026-08-12.md` (trạng thái F1=0.629).
> **Mốc mới sau phiên này: F1 = 0.720.**

---

## 0. Tóm tắt 1 phút

| # | Việc | Kết luận |
|---|---|---|
| 1 | Giả thuyết A: `fabs()` ở `discrepancy.cpp:58` là bug | **BÁC BỎ** — bỏ `fabs()` làm recall sập về 0 |
| 2 | Vì sao? | dấu discrepancy **đảo chiều** giữa map quá khứ và map tương lai |
| 3 | Từ đó tìm ra luật mới: **ANTI** (dùng tính đảo chiều làm bằng chứng) | mô phỏng F1 **0.712 → 0.784**, precision 0.63 → 0.89 |
| 4 | Nghi ngờ "C++ không tái lập được vì RANSAC ngẫu nhiên" | **BÁC BỎ** — chạy 3 lần ra đúng 695 điểm giống hệt |
| 5 | 3 điểm yếu khác trong `filter.cpp` | ghi lại ở mục 6, chưa đo |
| 6 | Làm mô phỏng khớp C++ (việc số 1 của mục 9) | **KHÔNG LÀM ĐƯỢC** — và lý do mới là phát hiện lớn nhất, xem **mục 11** |
| 7 | Sweep `threshold` (việc 2 của mục 7 phiên 12/8) | **0.5 → 1.0**, F1 thật **0.629 → 0.691**, tăng 5/5 scan (mục 12) |
| 8 | Sweep `vote_threshold` | **giữ nguyên 0.5** — đã tối ưu sẵn ở `thr=1.0` (mục 13) |
| 9 | Sweep thang phân giải (việc 3 của mục 7 phiên 12/8) | **thêm thang thứ 4** `8×112`, F1 thật **0.691 → 0.720** (mục 14) |
| 10 | Đo lại giá trị của ANTI ở mốc mới | **BỎ ANTI** — chỉ còn +0.017, xấp xỉ mức nhiễu (mục 15) |
| 11 | Test seq 03/06/07 (việc 4 của mục 7 phiên 12/8) | **KHÔNG generalize**: F1 0.649 (seq04) → **0.145-0.495** (mục 16) |

### Kết quả cuối phiên: F1 thật (C++) 0.629 → 0.720

| bước | F1 TB 5 scan |
|---|---|
| đầu phiên | 0.629 |
| + `threshold` 0.5 → 1.0 (mục 12) | 0.691 |
| + thang thứ 4 `8×112` (mục 14) | **0.720** |

Cả hai đều **miễn phí** (không thêm module), **vẫn nhân quả**, và đã áp dụng vào code.
Cấu hình chốt: `N=4, max_distance_m=4.0, threshold=1.0, vote_threshold=0.5`,
4 thang `64×900 / 32×450 / 16×225 / 8×112`.

**Phát hiện quan trọng nhất (mục 11):** F1 **cực nhạy** với ground mask — đổi mặt phẳng
RANSAC dưới 1% số điểm thì F1 scan 150 nhảy từ 0.606 đến 0.751. *Deterministic không có
nghĩa là robust.* Mọi phép đo đụng tới ground từ giờ phải chạy trên nhiều mask.

**Bài học phương pháp của phiên (mục 15):** ANTI ban đầu đo được +0.094 và trông rất hấp
dẫn. Sau khi hai biện pháp rẻ hơn được áp dụng, nó chỉ còn +0.017 — vì cả ba **cùng tấn
công một loại rác**. Luôn đo lại giá trị của một ý tưởng ở cấu hình MỚI NHẤT, đừng tin con
số đo ở cấu hình cũ.

**Cảnh báo lớn nhất của phiên (mục 16):** đã test trên seq 03/06/07 — **thuật toán không
generalize**. Cùng cấu hình đó, F1 rơi từ 0.649 (seq04) xuống **0.145 / 0.495 / 0.304**.
Tune riêng `threshold` cho từng seq cũng không cứu được. Và ngay trong seq04, chỉ đổi 5
scan đem đo đã làm F1 lệch **0.071**.

> **`F1 = 0.720` không phải tính chất của thuật toán** mà của *(seq04 + đúng 5 scan đó +
> đúng ground mask đó)*. Các so sánh **có cặp** trong phiên này vẫn đúng (threshold, số
> thang, ANTI); **con số tuyệt đối thì không mang đi đâu được.**

**Việc nên làm đầu tiên phiên sau:** đọc mục 16 rồi tới 15.4 (hệ quả của việc chốt map
tích luỹ).

---

## 1. Bối cảnh: vì sao lại đi soi `discrepancy`

Danh sách ưu tiên cuối phiên 12/8 xếp **Patchwork++ hạng 1** (ground = 67.5% FP còn lại).
Nhưng Patchwork++ là viết cả một module mới. Trước khi trả giá đó, đọc lại 2 hàm lõi
xem có lỗi logic rẻ hơn không. Tìm được 1 ứng viên đáng đo:

`discrepancy.cpp:58` dùng `std::fabs(scan_range - map_range)` — **mất dấu**. Suy luận
ban đầu: `scan > map` nghĩa là map có vật gì đó ở gần mà scan nhìn xuyên qua → vật đó đã
biến mất khỏi map → điểm của scan là **nền phía sau, đứng yên** → gán dynamic là FP.
Removert gốc dùng bằng chứng **một chiều**, không phải trị tuyệt đối.

Nghe rất hợp lý. Và **sai**.

---

## 2. Đo: bỏ `fabs()` thì recall sập về 0

Mô phỏng lại toàn bộ pipeline bằng numpy (`diag_sign.py`), so 2 luật trên **cùng một
ground mask, cùng mọi tham số**, chỉ khác điều kiện dynamic:

- **ABS** (code hiện tại): `|scan − map| > threshold`
- **SIGNED** (giả thuyết A): `map − scan > threshold` (scan gần hơn map)

Số điểm sống sót qua từng tầng, scan 150:

| tầng | ABS | ABS-TP | SIGNED | SIGNED-TP |
|---|---|---|---|---|
| REMOVE @64×900 | 2824 | 340 | 961 | 29 |
| + xác nhận @32×450 | 764 | 340 | 119 | 2 |
| + xác nhận @16×225 (cuối) | 542 | 340 | **37** | **0** |

Bắt được bao nhiêu trong 370 điểm GT dynamic, xét riêng từng thang:

| thang | SIGNED | ABS |
|---|---|---|
| 64×900 | 29/370 | 340/370 |
| 32×450 | 4/370 | 345/370 |
| 16×225 | **0/370** | 370/370 |

SIGNED không bắt được **một điểm động thật nào** ở thang thô. Đây không phải "chặt hơn
một chút" — đây là dấu hiệu giả thuyết sai từ gốc.

---

## 3. Vì sao: dấu discrepancy đảo chiều theo trục thời gian

Đo dấu của `(map_range − scan_range)` tại pixel, **trên 370 điểm GT dynamic**, tách theo
từng map (scan 150, map 148/149/151/152):

| map | vị trí thời gian | scan GẦN hơn | scan XA hơn | **trung vị (map−scan)** |
|---|---|---|---|---|
| 148 | quá khứ −2 | 112 | 198 | **−2.94 m** |
| 149 | quá khứ −1 | 120 | 187 | **−1.44 m** |
| 151 | tương lai +1 | 170 | 131 | **+1.40 m** |
| 152 | tương lai +2 | 173 | 120 | **+2.92 m** |

Đối xứng gần như hoàn hảo quanh 0, biên độ **≈1.45 m/frame** — đúng cỡ chuyển động tương
đối giữa vật và ego trong 0.1s.

**Giải thích:** vật động thật KHÔNG biến mất khỏi map. Với `N=4, max_distance_m=4`, các
map chỉ cách scan ±1, ±2 frame (0.1-0.2s). Chiếc xe có mặt trong CẢ 5 frame, chỉ **dịch
dần theo thời gian**. Nhìn tại một pixel cố định: ở frame quá khứ nó ở một khoảng cách, ở
frame tương lai nó ở khoảng cách khác — **hai bên trái dấu nhau**.

Hệ quả cho luật SIGNED: map quá khứ cho dấu âm, map tương lai cho dấu dương → mỗi bên chỉ
được 2/4 phiếu → `2/4 = 0.5`, **không** lớn hơn `vote_threshold = 0.5` → trượt. Luật một
chiều tự cắt bỏ một nửa bằng chứng của chính nó.

> **`fabs()` không phải bug. Nó chính là thứ khiến phương pháp chạy được với tập map đối
> xứng quá khứ/tương lai.** Removert gốc dùng bằng chứng một chiều được là vì "map" của nó
> là **map tích luỹ tĩnh**, không phải các scan hàng xóm hai bên.

Đây là lần thứ ba trong dự án một giả thuyết "hợp lý" bị dữ liệu bác bỏ (xem mục 3 của
`HANDOFF_2026-08-12.md`: 3 giả định về RANSAC cũng bị bác bỏ y hệt). Giữ thói quen đo
trước khi sửa.

---

## 4. Phát hiện thay thế: dùng chính tính ĐẢO CHIỀU làm bằng chứng (luật ANTI)

Bảng ở mục 3 gợi ra một discriminator mới, **không cần thêm module nào**, chỉ dùng thông
tin dấu vốn đã có sẵn:

> Vật động thật di chuyển **đơn điệu theo thời gian** → dấu discrepancy ở map quá khứ phải
> NGƯỢC với dấu ở map tương lai. Nhiễu/parallax không có tính chất đó.

Luật ANTI: điểm là dynamic ở một thang ⟺ (đa số map quá khứ nghiêng một dấu) **và** (đa số
map tương lai nghiêng dấu còn lại).

Kết quả mô phỏng, 5 scan (50/100/150/200/250), **cùng ground mask cho mọi luật**:

| luật | P | R | **F1 TB** |
|---|---|---|---|
| ABS (mô phỏng code hiện tại) | 0.61 | 0.85 | 0.712 |
| ANTI @L0 + ABS revert | 0.83 | 0.65 | 0.728 |
| ANTI mọi thang | 0.89 | 0.64 | 0.739 |
| **ABS @L0 + ANTI revert** | **0.86** | **0.72** | **0.784** |

Cấu hình thắng đúng theo tinh thần "remove nhạy / revert chắc" đã chốt phiên trước:
**giữ ABS ở thang REMOVE** (nhạy, không bỏ sót), **dùng ANTI ở tầng REVERT** (khẳng định
chặt). F1 tăng ở **4/5 scan** (scan 100 giảm nhẹ 0.815→0.808). FP ở scan 150: **202 → 40**.

### 4.1. Cơ chế: vì sao dấu bị buộc vào trục thời gian

Code đưa scan về hệ toạ độ của map rồi dựng CẢ HAI ảnh range từ **cùng một điểm nhìn**
(vị trí sensor lúc quét map), nên chuyển động của robot đã bị khử — phần còn lại thuần là
chuyển động của vật. Gọi `r(t)` là khoảng cách của vật nhìn từ điểm nhìn đó:

```
diff = r(t_map) − r(t_scan) ≈ ṙ · (t_map − t_scan)
                              ^^   ^^^^^^^^^^^^^^^
                       vận tốc     ĐỔI DẤU giữa map quá khứ và map tương lai
                       hướng tâm
```

Vật động thì `ṙ ≠ 0` và không đổi dấu trong 0.4 giây, nên dấu của `diff` bị quyết định
hoàn toàn bởi map nằm TRƯỚC hay SAU scan. Số đo ở mục 3 khớp chính xác: **1.44 và 2.92
đúng tỉ lệ 1:2**, khớp ±1 và ±2 frame, tức `ṙ ≈ 1.45 m/frame`.

> **ANTI thực chất là một máy đo vận tốc thô sơ.** ABS hỏi "có bất nhất không"; ANTI hỏi
> "sự bất nhất này có đúng dạng của một vật đang chạy đều không". Nhiễu `min()` của tán lá
> không liên quan gì tới việc map ở quá khứ hay tương lai, nên không qua được.

### 4.2. BỐN TRƯỜNG HỢP ANTI SẼ HỎNG — đọc kỹ trước khi implement

**(a) Vật chạy ngang (`ṙ ≈ 0`).** Vật đi vòng quanh sensor ở khoảng cách gần như không
đổi thì không có dấu để đảo → **bỏ sót hoàn toàn**. Đây là cái giá đã đo được: dùng ANTI ở
cả tầng REMOVE làm recall tụt 0.85 → 0.65, và nhiều khả năng là lý do scan 100 không được
lợi (giả thuyết, chưa xác minh). ABS không có điểm mù này vì nó chỉ nhìn độ lớn.

**(b) Không nhân quả — bắt buộc có map CẢ HAI phía.** Chỉ có map quá khứ thì
`pos_f = neg_f = 0` vĩnh viễn → không điểm nào được xác nhận → xoá sạch thành static.
Chạy offline trên dataset thì được, nhưng real-time cạnh FAST-LIO2 (`HANDOFF_VIEC4.md`
mục 7) chỉ có frame quá khứ → **ANTI không dùng được**, trừ khi chấp nhận độ trễ vài frame
để chờ map tương lai. Đây là cái giá kiến trúc, phải quyết trước khi viết code.

**(c) Vật đổi hướng đúng lúc.** Xe phanh gấp rồi lùi, hoặc đang ở điểm gần nhất khi lướt
qua ngang → `ṙ` đổi dấu ngay quanh `t_scan` → không đảo chiều giữa hai phía → bỏ sót.
Hiếm trong 0.4 giây nhưng có thật, và sẽ nặng hơn nếu tăng `max_distance_m`.

**(d) Nó chỉ đúng vì map đang RẤT gần nhau.** ±0.2 giây thì "vật chạy đều" là giả định
tốt. Nếu sau này đổi sang **map tích luỹ** (`HANDOFF_VIEC4.md` mục 10.4) thì giả định sụp,
và lúc đó luật MỘT CHIỀU kiểu Removert gốc mới là đúng. **Hai hướng này loại trừ nhau —
chọn một, không thể có cả hai.** Đây là ngã ba kiến trúc quan trọng nhất của dự án hiện giờ.

Ngoài ra, một tính chất (không phải lỗi): điểm thiếu bằng chứng ở một phía
(`pos_f = neg_f = 0`) thì ANTI trả về static — đúng tinh thần "thiếu bằng chứng thì giữ
lại, không suy diễn có lợi cho việc xoá" vốn đã có sẵn trong `main.cpp:167`.

---

## 5. Kiểm tra phụ: C++ có tái lập được không? — CÓ

RANSAC trong Python lệch tới **2337 điểm (1.84%)** giữa các seed dù tổng số ground ổn định
(50.1-50.2%). Nghi ngờ: `ground_filter.cpp` không set seed → mỗi lần chạy C++ ra kết quả
khác → mọi so sánh A/B trong các phiên trước đều nhiễu.

Đã đo: chạy binary **3 lần** trên scan 150, cùng tham số →

```
lần 1/2/3: Predicted dynamic 695, P=0.478 R=0.897 F1=0.623   (giống hệt nhau)
Jaccard giữa 3 tập chỉ số: 1.000 — lệch 0 điểm
```

**PCL dùng seed cố định → C++ tái lập 100%.** Nghi ngờ bị bác bỏ, mọi số liệu A/B các
phiên trước vẫn đứng vững. (Đây là lý do lệch Python↔C++ ở mục 7: khác luồng RNG, không
phải khác thuật toán.)

---

## 6. Ba điểm yếu khác đã đọc ra, CHƯA đo

**6.1. Lây nhiễm theo pixel** — cơ chế gốc, đáng ưu tiên đo.
Range image là phép rút gọn *nhiều → 1* (`min`), nhưng gán nhãn là phép tra ngược
*1 → nhiều*: `getDynamicIndices` (`filter.cpp:20-40`) chiếu lại MỌI điểm rồi phát phán
quyết của pixel cho tất cả điểm rơi vào đó. Một pixel chứa 3 điểm (xe 25m, tường 30.2m,
tường 31.0m) → cả 3 bị gán dynamic, dù chỉ điểm 25m tạo ra discrepancy → 1 TP + 2 FP.

Bug ground đã vá phiên trước (`main.cpp:185`) chỉ là **một trường hợp riêng** của cơ chế
này; cơ chế thì vẫn còn nguyên cho mọi loại điểm khác. Ở 64×900 trung bình 2.7 điểm/pixel
→ trần lý thuyết ~60% số điểm bị flag là "ăn ké". Cách đo: với mỗi pixel bị flag, đếm số
điểm rơi vào và bao nhiêu điểm không phải điểm `min`.

**6.2. Phép chiếu bị chép làm hai bản.** `filter.cpp:20-36` viết tay lại đúng công thức
của `range_image::projectToPixel`, trong khi `getObservedIndices` ngay bên dưới
(`filter.cpp:57`) thì gọi hàm chung. Hiện tại hai bản khớp nhau → **chưa gây sai số**.
Nhưng sửa FOV/công thức ở một chỗ mà quên chỗ kia thì `vote_count` và `observed_count` sẽ
nói về hai pixel khác nhau — bug rất khó thấy. Dọn dẹp thuần tuý, không đổi kết quả.

**6.3. Hai ground mask fit độc lập.** `main.cpp:83` (scan) và `main.cpp:95` (map) chạy
RANSAC riêng → hai tập ground khác nhau. Pixel bị loại ground ở ảnh này mà không bị loại
ở ảnh kia sẽ lệch range rất lớn một cách giả tạo. Khớp với việc FP còn lại 67.5% là ground
trong khi RANSAC bỏ sót 35.9% vỉa hè. Đây là **cùng gốc rễ với việc Patchwork++** — nếu
làm Patchwork++ thì nên đo luôn cái này.

---

## 7. HẠN CHẾ của phiên này — đọc trước khi tin số ở mục 4

Mô phỏng Python **chưa khớp C++**, scan 150:

| | Python (mô phỏng) | C++ (thật) |
|---|---|---|
| số điểm dự đoán dynamic | 542 | 695 |
| P / R / F1 | 0.627 / 0.919 / **0.746** | 0.478 / 0.897 / **0.623** |
| giao nhau giữa 2 tập | 447 điểm | 447 điểm |

Nguyên nhân đã truy ra: **ground mask khác nhau**. Tổng số thì rất khớp (Python 63664 =
50.15% vs C++ 63156 = 49.75%, đúng con số 50.2% mục 3.1 phiên 12/8) nhưng *tập* điểm khác
— RANSAC hai bên chọn mẫu ngẫu nhiên khác nhau, và riêng trong Python đổi seed đã lệch tới
2337 điểm.

Vì vậy:

- ❌ **Đừng trích con số tuyệt đối** F1=0.784 ra ngoài. Nó ở thang đo của mô phỏng
  (baseline mô phỏng 0.712, không phải 0.629).
- ✅ **So sánh tương đối thì tin được**: cả 4 luật ở mục 4 dùng CHUNG một ground mask,
  chung mọi thứ khác, chỉ khác điều kiện dynamic → chênh lệch giữa chúng là thật.
- ➡️ **Bước xác nhận cần làm phiên sau:** cách rẻ nhất là thêm vài dòng debug dump
  ground mask từ C++ ra file, cho Python đọc vào thay cho RANSAC của nó. Lúc đó mô phỏng
  sẽ khớp C++ và mới kết luận được ANTI có thật sự tăng F1 hay không.

> **CẬP NHẬT (làm ngay trong phiên này):** đã thử làm khớp và **thất bại** — nhưng thất bại
> theo cách hữu ích. Chẩn đoán "chỉ do ground mask" ở trên là ĐÚNG nhưng chưa đủ mạnh:
> không phải mask lệch làm F1 lệch một chút, mà F1 **hỗn loạn** theo mask. Xem **mục 11**.
> Kết luận ở mục 4 vẫn đứng vững, thậm chí mạnh hơn.

---

## 8. File & cách tái tạo

Script chẩn đoán đã để trong `scripts/` (khác tiền lệ phiên 12/8 — phiên đó để ở thư mục
tạm rồi mất, phải viết lại từ mô tả). Chạy từ thư mục gốc repo:

- `scripts/diag_sign.py` — mô phỏng pipeline bằng numpy, so ABS vs SIGNED, bảng chéo dấu
  + phân loại FP theo class. `python3 scripts/diag_sign.py 150 148 149 151 152`
- `scripts/diag_anti.py` — so 4 luật (ABS / ANTI@L0 / ANTI mọi thang / ABS@L0+ANTI revert)
  trên 5 scan cố định. `python3 scripts/diag_anti.py` (không tham số, ~10s)
- `scripts/diag_seed.py` — đo độ nhạy theo ground mask (mục 11).
  `python3 scripts/diag_seed.py 8` (8 seed, ~90s)

`diag_anti.py` và `diag_seed.py` import `diag_sign.py` nên ba file phải nằm cùng thư mục.
`diag_sign.py` còn có `ransac_ground_pcl()` — bản sao có dừng sớm theo xác suất, đúng như
PCL làm thật (mục 11.1).

Cả hai chỉ cần numpy, đọc thẳng `.bin` + `.label` (không cần `.pcd`), và **không import
gì từ code C++** — đúng phương pháp "kiểm chứng độc lập" của mục 3 phiên 12/8.

---

## 9. Ưu tiên phiên sau (đã cập nhật lần 2, sau mục 11)

1. ~~Làm mô phỏng khớp C++~~ — **đã thử trong phiên này, không làm được và không còn cần**
   cho mục đích xác nhận ANTI. Xem mục 11.
2. ~~Xác nhận luật ANTI trong C++~~ — **HUỶ**, đã quyết bỏ ANTI (mục 15).
3. ~~Sweep `threshold` / `vote_threshold` / thang phân giải~~ (12/8 mục 7.2, 7.3) —
   **ĐÃ LÀM VÀ ĐÃ ÁP DỤNG**, xem mục 12, 13, 14.
4. **Map tích luỹ** (12/8 mục 7.5) — đã chốt là hướng kiến trúc cho lifelong (mục 15.4).
   Việc lớn nhất còn lại. Đọc kỹ 3 hệ quả ở 15.4 trước khi bắt tay.
5. **Đo lây nhiễm theo pixel** (mục 6.1) — vẫn đáng làm và độc lập với hướng kiến trúc:
   mục 11.5 cho thấy nó là cơ chế khuếch đại sai số ground thành hàng trăm FP.
6. Patchwork++ (12/8 mục 7.1) — lý do đã đổi (mục 11.4): không phải vì ground chiếm 67.5%
   FP mà vì nó bơm ±0.05 F1 nhiễu vào toàn hệ.
7. ~~Test sequence khác~~ (12/8 mục 7.4) — **ĐÃ LÀM**, xem mục 16. Seq 03/06/07 đã rút sẵn
   trong `~/kitti_data/dataset/sequences/`, dùng lại được ngay.
   **Việc mới đẻ ra từ đó, ưu tiên cao:**
   - Điều tra 3 scan có **0 TP tuyệt đối** (mục 16.6) — nghi là một chế độ hỏng riêng.
   - Tìm hiểu vì sao seq06 **phẳng lì với mọi ngưỡng** (0.117-0.145).
   - Thử cho `threshold` **thích ứng theo khoảng thời gian scan↔map** thay vì hằng số
     (mục 16.4) — không tồn tại một hằng số phục vụ được cả seq03 lẫn seq04.
   - **Mọi phép đo từ giờ nên chạy trên ít nhất 2 sequence**, không chỉ seq04.
8. Dọn phép chiếu chép 2 bản (mục 6.2) — dọn dẹp, không đổi kết quả.

**Lưu ý khi làm tiếp:** mọi so sánh từ giờ chạy ở `threshold=1.0` + 4 thang, không phải
mốc cũ — nếu không sẽ tối ưu quanh một cấu hình đã lỗi thời (đúng cái bẫy ở mục 13.2, và
là thứ đã làm ANTI trông giá trị hơn thực tế, mục 15).

**Quy tắc mới cho mọi phép đo từ giờ:** nếu thay đổi có đụng tới ground (đổi
`distance_threshold`, đổi thuật toán segmentation), **không được** kết luận từ một lần
chạy — hiệu ứng thật sẽ trộn lẫn với ±0.05 nhiễu mask (mục 11.4).

---

## 10. Phụ lục — `discrepancy` và `filter` hoạt động chính xác thế nào

Ghi lại vì đây là phần dễ quên nhất, và mọi phát hiện ở trên đều nằm trong đó.

### 10.1. Một phép RÚT GỌN, rồi một phép TRA NGƯỢC

```
~127k điểm 3D  --buildRangeImage-->  57600 pixel  --computeDiscrepancy-->  57600 phán quyết
                    nhiều → 1 (min)                                              |
                                                                                 | 1 → nhiều
~127k nhãn dynamic/static  <--getDynamicIndices--------------------------------- +
```

Chiều đi: mỗi pixel giữ `min(range)` = "vật gần nhất trong nón góc này"
(`range_image.cpp:60`). Chiều về: phán quyết của MỘT pixel phát cho MỌI điểm rơi vào đó
(`filter.cpp:38-40`). Gần như mọi FP đều sinh từ sự bất đối xứng này (mục 6.1).

### 10.2. `computeDiscrepancy` — ảnh 3 trạng thái, không phải nhị phân

| Giá trị pixel | Nghĩa | Sinh ra ở |
|---|---|---|
| `+infinity` | **không có ý kiến** — ít nhất một ảnh trống ở pixel này | khởi tạo + `continue` `discrepancy.cpp:51-56` |
| `0.0f` | **static** — hai ảnh khớp trong `threshold` | `discrepancy.cpp:65` |
| `> 0` (= `diff`) | **dynamic** | `discrepancy.cpp:60-63` |

Chỗ dễ nhầm nhất: `+infinity` và `0.0f` đều dẫn tới "không xoá điểm", nhưng vì hai lý do
trái ngược — "tôi thấy và xác nhận nó tĩnh" vs "tôi không thấy gì cả". Toàn bộ tầng voting
(`main.cpp:165-170`, mẫu số `observed_count`) được thiết kế quanh đúng sự phân biệt đó.

### 10.3. `filter::getDynamicIndices` — chỉ tra bảng, không so sánh gì

Với mỗi điểm: chiếu xuống `(row, col)` → đọc `discrepancy_image[row][col]` → nhận điểm khi
`isfinite(disc) && disc > 0.0f` (`filter.cpp:39`). `isfinite` loại trạng thái "không có ý
kiến", `> 0.0f` loại "static". Nó chiếu lại **mọi** điểm, kể cả điểm đã bị `min()` nuốt
lúc build ảnh — đó chính là mục 6.1.

### 10.4. `getObservedIndices` — mẫu số của voting

Trả lời câu hỏi khác hẳn: "map này có *nhìn thấy* gì ở pixel của điểm i không?", bằng
`isfinite(map_image[row][col])` (`filter.cpp:63`). Không quan tâm dynamic hay static. Điểm
chỉ được 2/4 map quan sát thì tỷ lệ phiếu tính trên 2, không phải trên 4.

### 10.5. `splitCloud`

Thuần cơ học: đổ index vào `unordered_set` rồi chia đôi đám mây. Không có logic cần nhớ.

---

## 11. Hậu kiểm mục 7 — không làm mô phỏng khớp C++ được, và vì sao đó mới là tin quan trọng

Mục 9 xếp "làm mô phỏng khớp C++" là việc số 1. Đã bắt tay làm ngay trong phiên. Kết quả:
**không khớp được** — nhưng lý do thất bại có giá trị hơn cả mục tiêu ban đầu.

### 11.1. Truy nguồn lệch bằng chính số liệu C++ in ra

`main.cpp` in sẵn khối `[DEBUG] observed_count stats` và các mốc trung gian — đối chiếu
trực tiếp được, không cần sửa gì. Scan 150, seed 0:

| | Python | C++ |
|---|---|---|
| mean observed @64×900 | 1.757 | 1.797 |
| `count==0` @64×900 | **47.5%** | **43.7%** |
| ép ground = static | 63664 | 63156 |
| ứng viên sau REMOVE | 2824 | 3192 |
| dynamic cuối cùng | 542 | 695 |

`count==0` cao hơn có hệ thống ⇒ ground mask của Python **to hơn** ⇒ loại nhiều pixel map
hơn ⇒ ít điểm được "quan sát" hơn.

Vì sao to hơn? Vì `ransac_ground` chạy đủ 200 vòng, còn `pcl::RandomSampleConsensus` **dừng
sớm** theo xác suất: `k = log(1−0.99) / log(1 − w³)`, với `w ≈ 0.5` thì **k ≈ 35 vòng**.
Python chọn "mặt phẳng tốt nhất trong 200", PCL chỉ lấy "đủ tốt". Đã cài lại bản có dừng
sớm (`ransac_ground_pcl`) và xác nhận: nó dừng ở **35-39 vòng**, số ground rơi về đúng
quanh mốc C++ (63006-63727 so với 63156). **Giả thuyết đúng.**

### 11.2. Nhưng vá xong vẫn không khớp — vì F1 hỗn loạn theo mask

| RANSAC | seed | ground | dynamic cuối | P | R | **F1** |
|---|---|---|---|---|---|---|
| **C++ (thật)** | — | 63156 | 695 | 0.478 | 0.897 | **0.623** |
| 200 vòng | 1 | 63581 | 694 | 0.490 | 0.919 | 0.639 |
| 200 vòng | 2 | 63736 | 590 | 0.576 | 0.919 | 0.708 |
| 200 vòng | 0 | 63664 | 542 | 0.627 | 0.919 | 0.746 |
| dừng sớm | 1 | 63006 | 1001 | 0.342 | 0.924 | **0.499** |
| dừng sớm | 2 | 63643 | 553 | 0.615 | 0.919 | 0.737 |

Ground mask lệch **dưới 1%** số điểm, F1 nhảy **0.499 → 0.746**. Không có "phiên bản RANSAC
đúng" nào để chỉnh cho khớp — đại lượng này đơn giản là không ổn định theo mask.

### 11.3. Đo có hệ thống: ABS nhạy, ANTI thì không

Scan 150, 8 ground mask khác nhau (seed 0-7), cùng một pipeline
(`python3 scripts/diag_seed.py 8`):

| | ABS (code hiện tại) | ABS@L0 + ANTI revert |
|---|---|---|
| dải F1 | **0.606 – 0.751** | **0.839 – 0.879** |
| min-max | 0.145 | **0.040** |
| độ lệch chuẩn | 0.049 | **0.012** |

Hai dải **không chồng lấn một chút nào**: mask xấu nhất của ANTI (0.839) vẫn cao hơn mask
tốt nhất của ABS (0.751). Đây là bằng chứng mạnh hơn nhiều so với con số trung bình ở mục 4
— nó cho thấy ANTI thắng không phải nhờ chọn được mask may mắn.

### 11.4. Ba hệ quả

**(a) Không cần làm mô phỏng khớp C++ nữa** để tin kết luận mục 4. Việc đó chỉ còn cần nếu
muốn *dự đoán con số tuyệt đối* mà C++ sẽ ra — mà điều đó thì đã biết là vô nghĩa (11.2).

**(b) Con số F1 = 0.629 đã chốt KHÔNG phải tính chất bền của thuật toán.** Nó tái lập được
100% (mục 5) nhưng đó là do PCL cố định seed — *deterministic không có nghĩa là robust*.
Cùng thuật toán, cùng dữ liệu, một mặt phẳng ground khác (cũng hợp lệ như nhau) cho ra
0.50 hoặc 0.75.
→ Các sweep phiên trước **vẫn đứng vững**, vì chúng không đụng tới ground nên mọi nhánh
so sánh dùng chung một mask (so sánh có cặp).
→ Nhưng từ giờ, thay đổi nào **đụng ground** thì phải đo trên nhiều mask, xem quy tắc cuối
mục 9.

**(c) Lý do làm Patchwork++ đã đổi.** Không còn là "ground chiếm 67.5% FP còn lại" mà là
"ground segmentation đang bơm ±0.05 F1 nhiễu vào toàn hệ thống". Nhưng cũng chính vì vậy
mà nó nên đứng **sau** ANTI: ANTI làm độ nhạy giảm 4 lần, tức là tự nó đã xử lý phần lớn
tác hại của ground xấu.

### 11.5. Vì sao lệch 1% mask lại lật F1 mạnh đến thế

Ground mask đi vào pipeline ở **hai chỗ**, và chỗ thứ hai có đòn bẩy rất lớn:

1. `main.cpp:185` — ép điểm ground thành static. Ảnh hưởng tuyến tính: sai 1 điểm mask thì
   nhiều nhất sai 1 nhãn.
2. `exclude_mask` của `buildRangeImage` — bỏ 1 điểm ground khỏi ảnh làm **đổi `min` của cả
   pixel**, mà phán quyết của pixel lại được phát cho **mọi** điểm rơi vào đó (mục 6.1,
   trung bình 2.7 điểm/pixel ở thang mịn). Sai 1 điểm mask → sai cả chùm điểm.

Tổng FP chỉ vài trăm, nên vài trăm điểm mask lệch là đủ lật tỷ lệ. **Đây chính là cơ chế
lây nhiễm theo pixel ở mục 6.1** — giờ nó không còn là một khiếm khuyết lý thuyết nữa mà là
thứ đang khuếch đại nhiễu ground lên thành ±0.05 F1. Vì vậy mục 6.1 được nâng lên hạng 3
trong danh sách ưu tiên.

---

## 12. Sweep `threshold` — việc số 2 của mục 7 phiên 12/8, ĐÃ LÀM

Số liệu **thật từ C++**, không phải mô phỏng: `threshold` vốn đã là tham số dòng lệnh
(`argv[5]`) nên sweep được mà không sửa một dòng code.
Script: `scripts/run_threshold_sweep.sh` (N=4, max_d=4, 5 scan 50/100/150/200/250).

Sweep này **hợp lệ bất chấp phát hiện ở mục 11**: ground mask không phụ thuộc `threshold`
nên mọi nhánh dùng chung đúng một mask → so sánh có cặp, nhiễu mask bị khử.

### 12.1. Đường cong — KHÔNG đơn điệu, có bướu và có vách đá

| thr | Precision | Recall | **F1 TB** |
|---|---|---|---|
| 0.2 | 0.339 | 0.850 | 0.474 |
| 0.3 | 0.428 | 0.839 | 0.553 |
| 0.4 | 0.481 | 0.836 | 0.598 |
| **0.5 (đang dùng)** | 0.520 | 0.832 | **0.629** |
| 0.7 | 0.588 | 0.795 | 0.669 |
| 0.9 | 0.628 | 0.766 | 0.685 |
| **1.0** | 0.652 | 0.748 | **0.691** ← |
| **1.1** | 0.661 | 0.734 | **0.691** ← |
| 1.2 | 0.672 | 0.699 | 0.681 |
| 1.3 | 0.673 | 0.647 | 0.655 |
| 1.5 | **0.554** | **0.353** | 0.412 |

Mốc 0.5 ra đúng **0.629** — khớp con số đã chốt phiên 12/8, tức sweep này đáng tin.

Ba vùng rõ rệt:
- **0.2 → 1.0**: precision gần gấp đôi (0.339 → 0.652), recall chỉ rơi 0.10. Đổi rất lời.
- **0.9 → 1.2**: cao nguyên phẳng, F1 ≈ 0.68-0.69.
- **sau 1.3**: recall sụp (0.699 → 0.353) mà precision cũng tụt theo (0.673 → 0.554).
  Ở đoạn này tăng ngưỡng phá **cả hai** chỉ số, không còn là đánh đổi.

### 12.2. Vì sao vách đá nằm đúng chỗ đó — dự đoán được từ mục 3

`threshold` tính bằng **MÉT**, nên nó phải nằm **trên mức nhiễu** và **dưới mức tín hiệu
thật**. Mục 3 đã đo tín hiệu thật: map gần nhất (±1 frame) cho trung vị **1.40 và 1.44 m**.

- `thr = 1.0` → dưới 1.40 → hai map gần nhất vẫn thấy được vật động.
- `thr = 1.5` → **vượt** 1.40 → map 149 và 151 mù hoàn toàn với vật động → mất nửa số
  phiếu → recall sụp còn 0.353.

Vách đá không ngẫu nhiên: nó nằm đúng tại **quãng đường vật đi được trong một frame**.

### 12.3. Kiểm tra generalization (không phải ăn may trên trung bình)

| scan | F1@0.5 | F1@1.0 | thay đổi | đỉnh riêng của scan |
|---|---|---|---|---|
| 50 | 0.560 | 0.644 | **+0.084** | 1.0 |
| 100 | 0.766 | 0.777 | +0.011 | 0.9 |
| 150 | 0.623 | 0.688 | +0.065 | 1.1 |
| 200 | 0.772 | 0.838 | +0.066 | 1.1 |
| 250 | 0.426 | 0.508 | **+0.082** | 1.2 |

Tăng ở **5/5 scan**; đỉnh riêng của từng scan đều rơi vào **0.9-1.2**, chụm chứ không tán.

### 12.4. Kết luận & cảnh báo

**Đề xuất: đổi `threshold` 0.5 → 1.0.** Không cần sửa code (tham số dòng lệnh), F1 TB
**0.629 → 0.691** (+0.062 — nhiều hơn toàn bộ phần lợi của việc 3 phiên 12/8, +0.019).
Lưu ý các script sweep hiện vẫn hard-code `THRESHOLD=0.5`, phải sửa nếu muốn đổi mặc định.

> **CẢNH BÁO cho Go2 / dữ liệu khác:** ngưỡng tối ưu bị **neo vào quãng đường đi được giữa
> scan và map**, nên **không phải hằng số phổ quát**. seq04 là ô tô chạy ~1.4 m/frame. Go2
> đi bộ chậm hơn nhiều → khoảng dịch nhỏ hơn → ngưỡng tối ưu sẽ **thấp hơn hẳn**. Đừng bê
> thẳng 1.0 sang. Cách chuyển: đo trung vị `|diff|` trên vật động thật ở map gần nhất (như
> mục 3) rồi đặt ngưỡng dưới con số đó.

---

## 13. Sweep `vote_threshold` — nốt việc số 2 của mục 7 phiên 12/8

`vote_threshold` là `constexpr` ở `main.cpp:160`, không có đường vào từ dòng lệnh. Cách đo:
**vá tạm hằng số → build → chạy → trả lại nguyên trạng** (script dùng `trap` để luôn khôi
phục). Đã kiểm chứng sau khi xong: `git status` sạch, `main.cpp` về `0.5f`, binary chạy lại
scan 150 ra đúng `F1 = 0.623` như cũ. **Không để lại thay đổi nào trong code.**

Sweep 2 chiều, vì `vote_threshold` và `threshold` **có tương tác** — chỉnh riêng từng cái
sẽ ra kết luận sai. F1 trung bình 5 scan:

| `vote_threshold` | @ thr=0.5 | @ thr=1.0 |
|---|---|---|
| 0.0 (≥1 phiếu) | 0.203 | 0.290 |
| 0.25 | 0.404 | 0.517 |
| 0.4 | 0.454 | 0.575 |
| **0.5 (đang dùng)** | 0.629 | **0.691** ← tốt nhất |
| 0.6 | 0.629 | 0.691 |
| 0.75 (đồng thuận tuyệt đối) | **0.656** | 0.645 |
| 0.9 | 0.656 | 0.645 |

### 13.1. `vote_threshold` là công tắc 5 nấc, không phải núm xoay

Để ý 0.5 và 0.6 cho **kết quả giống hệt nhau**; 0.75 và 0.9 cũng vậy. Không phải trùng hợp:
với N=4 map và mẫu số động (`observed_count` từ 1 đến 4), tỷ lệ phiếu chỉ có thể nhận
**6 giá trị**: `0.25, 0.33, 0.5, 0.67, 0.75, 1.0`. Luật là `ratio > vote_threshold`, nên
mọi ngưỡng nằm giữa hai giá trị liền kề đều cho cùng một kết quả:

| khoảng ngưỡng | thực chất nghĩa là |
|---|---|
| 0.0 – 0.24 | có ít nhất 1 phiếu |
| 0.25 – 0.32 | > 1/4 |
| 0.33 – 0.49 | > 1/3 |
| **0.5 – 0.66** | **đa số quá bán** |
| 0.67 – 0.99 | đồng thuận tuyệt đối |

→ Sweep mịn (0.55, 0.58, 0.62...) là **vô nghĩa**, chỉ tốn thời gian. Và nếu đổi N thì các
nấc này đổi theo — N=6 sẽ có nhiều nấc hơn.

### 13.2. Tối ưu của `vote_threshold` phụ thuộc `threshold`

- Ở `thr=0.5` (ngưỡng thấp, nhiều ứng viên rác): **đồng thuận tuyệt đối thắng**
  (0.656 > 0.629). Bằng chứng bẩn thì phải đòi hỏi chặt hơn.
- Ở `thr=1.0` (đã sạch sẵn): đồng thuận tuyệt đối **phản tác dụng** — precision nhích
  0.652 → 0.739 nhưng recall sụp 0.748 → 0.575, F1 tụt về 0.645.

Đây là lý do phải sweep 2 chiều. Nếu chỉ sweep `vote_threshold` ở `thr=0.5` cũ thì đã kết
luận "nên đổi sang đồng thuận tuyệt đối" — và đó là **kết luận sai** ở cấu hình tốt nhất.

### 13.3. Kết luận

**Không cần đổi `vote_threshold`.** Ở cấu hình tốt nhất (`thr=1.0`), giá trị 0.5 đang dùng
đã là tối ưu. Toàn bộ phần lợi của việc số 2 nằm ở `threshold` (mục 12), không ở đây.

Việc số 2 của mục 7 phiên 12/8 coi như **XONG**: cả hai tham số đã sweep, một cái nên đổi
(0.5 → 1.0), một cái giữ nguyên.

---

## 14. Sweep cấu hình thang phân giải — việc số 3 của mục 7 phiên 12/8, ĐÃ LÀM VÀ ĐÃ ÁP DỤNG

`levels` hard-code trong `main.cpp` nên đo bằng cách vá tạm rồi trả lại (như mục 13).
Chạy ở `threshold=1.0` — mốc mới, đúng theo lưu ý cuối mục 9. F1 TB 5 scan:

| cấu hình | các thang | P | R | **F1** |
|---|---|---|---|---|
| 2 thang | 64×900 + 32×450 | 0.493 | 0.793 | 0.599 |
| 2 thang | 64×900 + 16×225 | 0.617 | 0.756 | 0.673 |
| 2 thang | 64×900 + 8×112 | 0.631 | 0.772 | 0.689 |
| 3 thang (cũ) | 64×900/32×450/16×225 | 0.652 | 0.748 | 0.691 |
| 3 thang /3 | 64×900/21×300/7×100 | 0.652 | 0.737 | 0.686 |
| 3 thang bỏ giữa | 64×900/16×225/8×112 | 0.687 | 0.746 | 0.712 |
| **4 thang** | **64×900/32×450/16×225/8×112** | **0.709** | **0.738** | **0.720** ← |
| 4 thang bỏ giữa | 64×900/16×225/8×112/4×56 | 0.707 | 0.689 | 0.693 |
| 5 thang | + 4×56 | 0.726 | 0.681 | 0.698 |
| 6 thang | + 2×28 | 0.708 | 0.459 | 0.546 |
| gốc mịn hơn | 64×1800/32×900/16×450 | 0.586 | 0.744 | 0.651 |
| gốc thô hơn | 32×450/16×225/8×112 | 0.590 | 0.772 | 0.662 |

### 14.1. Ba điều đọc ra từ bảng

**Sức nặng nằm ở ĐẦU THÔ, không ở thang giữa.** Bỏ 16×225 mất rất nhiều (0.691 → 0.599);
bỏ 32×450 gần như không mất (0.691 → 0.673). Thậm chí "3 thang bỏ giữa" (0.712) còn hơn
"3 thang cũ" (0.691) dù cùng số thang.

**Nhưng thô quá thì sụp — và sụp qua RECALL.** 5 thang: 0.698. 6 thang: 0.546 với recall
chỉ còn 0.459. Đúng cơ chế đã ghi ở phụ lục 9.5 phiên 12/8: pixel quá thô nuốt cả vật động
lẫn vật tĩnh gần hơn, vật động bị che mất nên không được xác nhận → bị revert oan.

**Thang REMOVE 64×900 là điểm ngọt.** Mịn hơn (64×1800) tệ hơn hẳn (0.651), thô hơn
(bắt đầu từ 32×450) cũng tệ (0.662). Không cần đụng vào.

Tỷ lệ thu nhỏ /2 hơn /3 nhưng không đáng kể (0.691 vs 0.686).

### 14.2. ĐÃ ÁP DỤNG vào code

- `main.cpp`: thêm `{8, 112, "rất thô 8x112"}` vào `levels` (kèm comment ghi lại bảng sweep).
- `scripts/run_maxdist_sweep.sh`, `scripts/run_n_sweep_v2.sh`: `THRESHOLD` 0.5 → 1.0.
  (`run_n_sweep.sh` là bản cũ đã bị thay thế, để nguyên.)

Xác minh sau khi build lại, 5 scan @ `threshold=1.0`:

| scan | 50 | 100 | 150 | 200 | 250 | **TB** |
|---|---|---|---|---|---|---|
| F1 | 0.701 | 0.793 | 0.727 | 0.855 | 0.526 | **0.720** |

Khớp chính xác con số sweep. **Mốc mới của dự án: F1 = 0.720** (từ 0.629).

---

## 15. Hiệu chỉnh lại giá trị của ANTI — và quyết định BỎ

Luật ANTI (mục 4) được đo khi `threshold` còn 0.5 và mới có 3 thang. Sau mục 12 và 14,
phải đo lại — vì cả ba biện pháp đều tấn công **cùng một loại rác** (nhiễu parallax/tán lá),
nên chúng chồng lấn nhau.

| cấu hình | F1 ABS | F1 ANTI | **ANTI thêm được** |
|---|---|---|---|
| thr=0.5, 3 thang (lúc đo ANTI ở mục 4) | 0.691 | 0.785 | **+0.094** |
| thr=1.0, 3 thang | 0.748 | 0.785 | +0.037 |
| **thr=1.0, 4 thang (mốc mới)** | **0.769** | 0.786 | **+0.017** |

(mô phỏng, trung bình 5 scan × 3 ground mask)

**82% giá trị của ANTI thực chất là "ngưỡng đang quá thấp và thiếu thang thô".** Phần còn
lại +0.017 **xấp xỉ mức nhiễu đo** — độ lệch chuẩn của F1 theo ground mask là 0.012-0.013
(mục 11.3). Không phân biệt được với 0 một cách chắc chắn.

### 15.1. Quyết định: BỎ ANTI

Cân đối lợi/hại ở mốc mới:

| | |
|---|---|
| **Lợi** | +0.017 F1 (≈ mức nhiễu); ổn định hơn với ground mask (std 0.012 vs 0.049); miễn phí về tính toán |
| **Hại** | **không nhân quả** (phải chờ frame tương lai); mù với vật chạy ngang (`ṙ≈0`); hết hạn ngay khi đổi sang map tích luỹ (mục 4.2) |

Hai bước ở mục 12 và 14 cho **+0.091 F1 thật, miễn phí, vẫn nhân quả**. ANTI chỉ thêm được
0.017 nữa mà đổi lại là độ trễ kiến trúc. **Không đáng.**

### 15.2. Vì sao vẫn giữ mục 2-4 trong báo cáo này

Vì phần lớn giá trị của nó không nằm ở luật:
- Số đo ở mục 3 (tín hiệu thật ở map ±1 frame ≈ 1.4 m) chính là thứ giải thích được vách đá
  của `threshold` ở mục 12.2. Không có nó thì `threshold=1.0` chỉ là con số mò ra.
- Nó chứng minh `fabs()` **không phải bug** — chặn được một "fix 1 dòng" nghe rất hợp lý mà
  thực ra sẽ phá recall về 0.
- Nguyên lý "bằng chứng phải có cấu trúc, không chỉ có độ lớn" vẫn đúng; ở thang dài hạn
  nó mang tên khác: **persistence** (xem 15.3).

### 15.3. Vì sao ANTI KHÔNG kéo dài sang lifelong được

Không phải vì yếu, mà vì **ngược dấu**. Thay đổi dài hạn là **hàm bậc thang** (có → không),
không phải chuyển động liên tục. Xe đỗ hôm qua, hôm nay không còn:

| | vật đang chạy (0.2 giây) | vật biến mất (1 tháng) |
|---|---|---|
| so với lần thăm TRƯỚC | dấu âm | dấu dương |
| so với lần thăm SAU | dấu **dương** | dấu **dương** |
| ANTI kết luận | dynamic ✓ | **static ✗** |

Dấu không đảo vì vật không *đi qua* điểm quan sát — nó chỉ biến mất. ANTI đo đạo hàm, mà
bậc thang có đạo hàm bằng 0 ở cả hai phía. Kéo dài cửa sổ càng làm sai thêm.

**Bản dài hạn của cùng nguyên lý là persistence filter**: vật tĩnh thì được quan sát lại ở
mọi lần thăm; vật tạm thời thì xuất hiện một lần rồi mất. Cùng triết lý "chữ ký thời gian
phải khớp mô hình vật lý", khác đại lượng quan sát. (`HANDOFF_VIEC4.md` mục 12.7)

### 15.4. Chốt hướng kiến trúc cho lifelong: MAP TÍCH LUỸ

Quyết định của chủ dự án, có căn cứ ở 15.3. Ba hệ quả phải nhớ khi bắt tay:

1. **Câu hỏi `fabs()` MỞ LẠI.** Kết luận ở mục 2-3 chỉ đúng với tập map hàng xóm đối xứng.
   Với map tích luỹ tĩnh, "scan xa hơn map" thật sự nghĩa là *vật đã biến mất* → luật MỘT
   CHIỀU kiểu Removert gốc trở lại đúng.
2. **`threshold=1.0` hết hiệu lực.** Nó neo vào quãng dịch 1.4 m/frame (mục 12.2). Với map
   tích luỹ, tín hiệu là sai số đăng ký + nhiễu sensor (~0.1-0.3 m) → ngưỡng phải nhỏ hơn
   nhiều. Phải sweep lại từ đầu.
3. **Được thêm một lợi ích miễn phí:** map tích luỹ xoá tận gốc giới hạn baseline/parallax
   ở mục 4-5 `HANDOFF_VIEC4.md`, vì map không còn chỉ có một viewpoint cách 4 m.

---

## 16. Test trên sequence khác — việc số 4 của mục 7 phiên 12/8. KẾT QUẢ XẤU, VÀ ĐÓ LÀ TIN QUAN TRỌNG

Đã rút seq **03, 06, 07** từ `~/Downloads/data_odometry_velodyne.zip` (velodyne + labels,
convert sang pcd). Script: `scripts/run_generalization_test.py`.

### 16.1. Giao thức chọn scan — chốt TRƯỚC khi chạy

1. Chỉ xét frame có **> 0 điểm GT dynamic**. Đây là lọc **tính hợp lệ**, không phải lọc
   hiệu năng: không có vật động thật thì recall không có mẫu số. Không dùng kết quả thuật
   toán để chọn.
2. Trong danh sách đó lấy 5 vị trí **cách đều** (20/35/50/65/80%).
3. Bỏ 10 frame đầu/cuối (không đủ map hai phía).
4. **Báo cáo tất cả**, kể cả sequence ra kết quả xấu.

Script đã được kiểm chứng: chạy trên đúng bộ scan cũ của seq04 (50/100/150/200/250) ra
đúng **0.720**.

### 16.2. Đặc tính 4 sequence — seq04 là NGOẠI LỆ

| seq | frame | tốc độ (m/frame) | % frame có vật động | điểm động TB/frame |
|---|---|---|---|---|
| 03 | 801 | 0.70 | 38.5% | 97 |
| **04** (nơi tune mọi thứ) | 271 | **1.46** | **100%** | **1167** |
| 06 | 1101 | 1.12 | 37.1% | 111 |
| 07 | 1101 | 0.63 | 77.7% | 970 |

seq04 chạy nhanh gấp đôi và có nhiều vật động gấp **~10 lần** seq03/06. Toàn bộ dự án
đang được tune trên một sequence không đại diện.

### 16.3. Kết quả ở cấu hình đã chốt (`threshold=1.0`, 4 thang)

| seq | 03 | **04** | 06 | 07 |
|---|---|---|---|---|
| F1 TB | 0.304 | **0.649** | **0.145** | 0.495 |

**Thuật toán KHÔNG generalize tốt.** F1 rơi từ 0.649 xuống 0.145-0.495.

### 16.4. Có phải chỉ do `threshold` không chuyển được sang seq khác? — KHÔNG

Sweep lại ngưỡng riêng cho từng sequence (5 scan đã chọn):

| seq | 0.5 | 0.7 | 1.0 | 1.3 | 1.6 | 2.0 | tối ưu riêng |
|---|---|---|---|---|---|---|---|
| 03 | 0.179 | 0.221 | 0.304 | 0.357 | 0.386 | **0.393** | ≥ 2.0 |
| 04 | 0.628 | 0.641 | **0.649** | 0.622 | 0.103 | 0.050 | 1.0 |
| 06 | 0.136 | 0.117 | **0.145** | 0.123 | 0.129 | 0.135 | phẳng, vô nghĩa |
| 07 | 0.484 | **0.502** | 0.495 | 0.500 | 0.492 | 0.492 | phẳng |

Tune riêng chỉ cứu được seq03 (0.304 → 0.393), **không cứu được** seq06 (0.145) hay seq07
(0.502). Nên kết luận "không generalize" đứng vững, không phải lỗi tham số.

**Và không tồn tại một ngưỡng chung:** seq03 cần ≥ 2.0, đúng chỗ seq04 đã sụp
(0.103 ở 1.6). Một hằng số duy nhất không phục vụ được cả hai.

Có một điểm khớp với cơ chế ở mục 12.2: seq03 chạy chậm (0.70 m/frame) nên với cùng bán
kính 4 m, khoảng **thời gian** giữa scan và map dài gấp đôi → vật động dịch xa hơn → ngưỡng
tối ưu cao hơn. Tỷ lệ khớp khá tốt (04: 4/1.46 ≈ 0.27 s ↔ thr 1.0; 03: 4/0.70 ≈ 0.57 s ↔
thr ≥ 2.0). **Nhưng seq07 phá quy luật** (0.63 m/frame mà đường cong phẳng lì), nên đây mới
là quan sát, chưa phải luật. Nếu xác minh được, hướng đúng là cho `threshold` **thích ứng
theo khoảng thời gian scan↔map** thay vì để hằng số.

### 16.5. Nguồn phương sai thứ BA: chọn scan

Cùng seq04, cùng mọi tham số, chỉ đổi 5 scan đem đo:

| bộ scan | F1 TB |
|---|---|
| cũ (50/100/150/200/250) | **0.720** |
| mới, theo giao thức 16.1 (60/97/135/172/210) | **0.649** |

Chênh **0.071** — lớn hơn cả phần lợi của `threshold` (+0.062) và của thang thứ 4 (+0.029)
cộng lại gần bằng. Vậy con số mốc chịu **ba** nguồn phương sai: ground mask (±0.05, mục 11),
chọn scan (±0.07), chọn sequence (±0.25).

> **`F1 = 0.720` KHÔNG phải tính chất của thuật toán.** Nó là tính chất của
> *(seq04 + đúng 5 scan đó + đúng ground mask đó)*.
>
> Các so sánh **có cặp** trong phiên này vẫn đứng vững (threshold, số thang, ANTI — mọi
> nhánh dùng chung scan/mask/sequence). Nhưng **con số tuyệt đối thì không mang đi đâu được**.

### 16.6. Bất thường chưa giải thích được

Ba scan có **P = R = F1 = 0.000** dù dự đoán hàng trăm điểm: seq06 scan 349 (GT 183, dự
đoán 87), seq06 scan 939 (GT 179, dự đoán 315), seq07 scan 248 (GT 31, dự đoán 201).
**Không trùng một điểm nào** với ground truth. Đáng điều tra — đây có thể là một chế độ
hỏng riêng chưa biết, không phải chỉ là "kém chính xác".

seq06 còn đặc biệt ở chỗ **phẳng lì với mọi ngưỡng** (0.117-0.145). Ngưỡng không phải
biến điều khiển ở đó; nguyên nhân nằm chỗ khác, chưa rõ.

### 16.7. Dự đoán ghi trước khi chạy — chấm điểm

- "seq03 và 06 sẽ thấp vì chỉ ~100 điểm động/frame" → **ĐÚNG** (0.304 / 0.145).
- "seq07 có thể tốt bất ngờ vì xe chậm ⇒ baseline thời gian dài hơn ⇒ tín hiệu tách khỏi
  nhiễu tốt hơn" → **SAI** (0.495, thấp hơn seq04). Cơ chế đó có xuất hiện, nhưng ở
  seq**03** (qua đường cong ngưỡng), không phải seq07.
