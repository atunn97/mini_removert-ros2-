# HANDOFF — phiên Linux 02/09/2026 · khám nghiệm FP bằng CSV (mục 15.9) — **ĐÃ ĐÓNG**

> Phiên này làm đúng một việc và làm xong: trả lời *FP đến từ LUẬT QUYẾT ĐỊNH hay từ ĐƯỜNG ỐNG?*
> **Không đụng một dòng C++ nào.** Toàn bộ kết luận đọc ra từ `scan150_gt.csv` bằng pandas.
> Bối cảnh đề tài, khung nghiên cứu, lịch 4 tuần: `HANDOFF_2026-09-01.md` mục 13–15 (bản ổ D,
> 824 dòng — **bản trong repo chỉ có 643 dòng, thiếu hẳn mục 15**).

---

## 1. Kết luận một câu

> **FP chủ yếu đến từ ĐƯỜNG ỐNG, và tách được theo cự ly:** ngoài 40 m là hình học/mật độ lấy mẫu
> (41% số FP, lệch âm trung vị 8,5 m); vùng 20–40 m là luật quyết định (25% FP, lệch trung vị
> 1,2 m); vùng dưới 20 m (34% FP) còn **hỗn hợp, chưa gọi tên sạch được**.
> Kèm hai kết quả phụ quan trọng: **cơ chế REVERT hoàn toàn lành mạnh**, và **dấu của discrepancy
> không dùng để vá được gì**.

---

## 2. Bước 0 — CSV dựng lại, mọi mốc khớp phiên 01/09

`scan150_gt.csv` **không còn trên đĩa** đầu phiên (quét cả `~` lẫn ổ D). Dựng lại bằng:

```bash
./build/mini_removert/mini_removert ~/kitti_data/dataset/sequences/04/pcd \
    ~/kitti_data/dataset/poses/04.txt ~/kitti_data/dataset/sequences/04/calib.txt \
    150 0.5 136 137 138 139 140 160 161 162 163 --csv scan150.csv
python3 scripts/add_gt_label.py scan150.csv 04
```

`map_idx` lấy từ `select_source_ladder.py` → **bậc 3 (R=20, t_min=10), 9 nguồn: 136–140, 160–163**.

| Mốc | Phải ra | Ra thật |
|---|---|---|
| số dòng | 126.941 | ✅ |
| `is_ground.sum()` | 63.156 (49,7522%) | ✅ |
| `baseline_label.sum()` | 1.445 | ✅ |
| TP / FP / FN | 301 / 1.144 / 69 | ✅ P 0,208 · R 0,814 · F1 0,332 |
| map gộp | 1.141.089 → 118.574 | ✅ |
| `observed_count` | min=0 max=1 (cả 4 thang) | ✅ |
| ứng viên sau REMOVE | 13.809 · REVERT 89,54% | ✅ |

⚠️ **Môi trường:** `python3` hệ thống **không có pandas**. Dùng `~/pointcloud_env/bin/python`
(pandas 3.0.3, matplotlib 3.10.9). `add_gt_label.py` chỉ cần numpy nên `python3` thường vẫn chạy.

---

## 3. Đã giải toả trước khi chạy: ngữ nghĩa cột `vote_*`

Checklist 15.9 dặn mở code trước khi tin bảng 2. Đã mở `src/main.cpp`:

- `vote_L{i}` / `observed_L{i}` là **con đếm** (dòng 136–140, tăng ở 293–295), không phải cờ. Nhưng
  map đã gộp còn một nên `observed ∈ {0,1}` ⇒ `vote ∈ {0,1}`. Kiểm trên dữ liệu: `vote > observed`
  ra **0**, `vote max = observed max = 1`.
- `L0` = thang mịn nhất = bước REMOVE (dòng 366) ⇒ ứng viên **đúng bằng** `vote_L0 == 1` trên tập
  không-ground. Đếm ra **13.809**, khớp log.
- REVERT đòi **mọi** thang L1–L3 xác nhận (dòng 374) ⇒ `so_thang == 4` ⇔ sống sót. Đếm ra **1.445**, khớp.

---

## 4. BẢNG 1 — FP/TP theo vành đai 10 m

⚠️ Code trong `CHECKLIST-15.9-FP.md` **crash** ở `(d['range'] // 10 * 10).astype(int)`: có **5.529
điểm không-ground (8,67%) `range` rỗng** (C++ chỉ ghi range khi pixel hợp lệ). Cả 5.529 đều là TN,
bỏ ra không mất TP/FP nào. Bảng chạy trên 58.256 điểm.

```
vanh_dai   FN    FP     TN   TP   TP_luy_%  FP_luy_%    tong   FP/tong_%
   0-10     0   108  18250    0        0,0       9,4   18358        0,59
  10-20     0   283  29690    0        0,0      34,2   29973        0,94
  20-30    65   173   5442  266       88,4      49,3    5946        2,91
  30-40     0   109   1674   34       99,7      58,8    1817        6,00
  40-50     0   113    790    1      100,0      68,7     904       12,50
  50-60     0   130    431    0      100,0      80,1     561       23,17
  60-70     0   158    346    0      100,0      93,9     504       31,35
  70-80     0    70    121    0      100,0     100,0     191       36,65
```

**Không tiêu chí nào của checklist bắn ra:** FP ngoài 30 m chỉ **50,7%** (không phải >70%), mà FP
cũng không rải giống TP (TP dồn hết vào 20–40 m). Tín hiệu nằm ở **cột `FP/tong_%`** — tỉ lệ FP trên
tổng số điểm *trong cùng vành đai*: **0,59% → 36,65%, tăng đơn điệu, gấp 62 lần**. Ở 70–80 m, hơn
một phần ba số điểm không-ground bị vu là động.

Hai quần thể lộ ra: tỉ lệ tăng theo cự ly (dấu vân tay mục 13.4), **nhưng** vành 0–20 m chứa 83% số
điểm nên tỉ lệ 0,6–0,9% vẫn đẻ ra **391 FP** — mà trong 0–20 m **không có một điểm động thật nào**.

---

## 5. BẢNG 2 — ứng viên theo số thang xác nhận

```
so_thang   tinh_gt0   dong_gt1    tong   ty_le_dong_%
   1          5.058          0    5.058       0,00
   2          5.077          0    5.077       0,00
   3          2.229          0    2.229       0,00
   4          1.144        301    1.445      20,83
```

### 5.1 ⭐ REVERT được minh oan hoàn toàn

```
TINH (gt=0):  1.144 / 13.508 song sot =   8,47%
DONG (gt=1):    301 /    301 song sot = 100,00%
```

Ba số 0 rồi nhảy thẳng lên 301. **Không một điểm động thật nào bị REVERT giết.** Nó vứt 12.364 ứng
viên và cả 12.364 đều là điểm tĩnh thật. Suy ra: **cả 69 FN đều mất ở bước REMOVE** (65 điểm động
không-ground không lọt nổi vào danh sách ứng viên; 4 điểm còn lại bị luật ground nuốt — mục 14.4),
**không** phải do revert quá tay. Luật "đòi mọi thang xác nhận" hoạt động đúng thiết kế.

### 5.2 Và 1.144 FP còn lại được **cả bốn thang** xác nhận

Nhiễu quanh ngưỡng không hành xử như vậy — hạ phân giải thì pixel to hơn, `min(range)` gộp thêm
điểm lân cận, chênh lệch bị san phẳng, điểm phải rụng ở đâu đó trong L1–L3. Sống qua cả 4 thang
nghĩa là bất đồng hình học **thật và có cấu trúc**.

### 5.3 BẢNG 2b — tách theo cự ly

| vùng | ứng viên | sống sót | tỉ lệ sống | TP | FP | **precision vùng** |
|---|---|---|---|---|---|---|
| <20 m | 11.370 | 391 | 3,4% | 0 | 391 | **0%** |
| 20–40 m | 1.639 | 582 | 35,5% | 300 | 282 | **51,6%** |
| >40 m | 800 | 472 | **59,0%** | 1 | 471 | **0,2%** |

**Trong vành 20–40 m baseline đạt precision 51,6%** — gấp 2,5 lần con số 0,208 của cả scan. Ngưỡng
đặt tay **không đặt sai chỗ**; nó chết ở hai đầu. Tỉ lệ sống sót qua REVERT **3,4% ở gần / 59% ở xa**
nói hết: cùng cơ chế, cùng ngưỡng, ra xa thì revert mất tác dụng vì mọi thang đều thấy bất đồng.

---

## 6. BẢNG 3 — dấu của `range_diff` (= `map − scan`; **dương = scan gần hơn map**)

```
L0  FP: n=1144  duong=27,2%  trung_vi= −1,432  |  TP: n=301  duong=22,9%  trung_vi= −14,693
L1  FP: n=1144  duong=18,3%  trung_vi= −2,890  |  TP: n=301  duong=13,6%  trung_vi= −17,142
L2  FP: n=1144  duong= 6,4%  trung_vi= −6,378  |  TP: n=301  duong=10,3%  trung_vi= −19,340
L3  FP: n=1144  duong= 2,9%  trung_vi=−10,633  |  TP: n=301  duong= 0,0%  trung_vi= −20,759
```

### 6.1 ⭐⭐ Câu hỏi `fabs()` đóng lại lần thứ ba, dứt khoát hơn hai lần trước

Điểm động thật lệch về phía **ÂM**: trung vị −14,7 m ở thang mịn, −20,8 m ở thang thô, **0% dương ở
L3**. Ngay tại tia có chiếc xe đang chạy, map có bề mặt **gần hơn 15–20 m** — vệt của chính chiếc xe
ở thời điểm khác, đúng cơ chế mục 13/08, nhưng **biên độ lớn hơn nhiều** vì nguồn giờ cách 1,0–1,4 s
(±10–14 frame) thay vì 0,1–0,2 s (±1–2 frame).

Chạy thử luật một chiều ngay trên CSV (ước lượng, không phải mô phỏng đầy đủ như `diag_sign.py`):

```
TP hien tai (fabs)       : 301
TP con lai neu MOT CHIEU :   0   (0,0%)
FP con lai neu MOT CHIEU :  16 / 1144
```

> **Bộ nguồn mới KHÔNG mở được cái cửa mục 13/08 để lại** (*"chỉ chuyển sang một chiều khi nguồn
> được đảm bảo không chứa vật"*) — nó **đóng chặt hơn**. Càng lùi xa thời gian, vệt càng dịch mạnh
> về phía âm. **`fabs` là chỗ cấm đụng, vĩnh viễn.**

### 6.2 Dấu KHÔNG tách được TP khỏi FP

```
FP: n=1144  duong ca 4 thang=  33 ( 2,9%)   am ca 4 thang= 833 (72,8%)   doi dau= 278
TP: n= 301  duong ca 4 thang=   0 ( 0,0%)   am ca 4 thang= 232 (77,1%)   doi dau=  69
```

77,1% TP âm ở cả 4 thang, 72,8% FP cũng vậy. **Không có cái vá nào dựa trên dấu.**

### 6.3 ⭐ BẢNG 3b — tách FP theo vùng, kèm **biên độ** lệch

```
             n     % duong   trung_vi (co dau)   trung_vi |lech|    <1m     >5m
FP  <20 m   391     61,1%        +0,84 m             2,62 m       24,3%   29,7%
FP 20-40 m  282     19,9%        −1,05 m             1,19 m       39,4%   12,4%
FP  >40 m   471      3,4%        −8,46 m             8,52 m       10,4%   58,6%
TP (toàn bộ) 301    22,9%       −14,69 m            14,99 m        0,0%   95,7%
```

> ⚠️ **Vì sao phải có hai cột `<1m` / `>5m`, đã gom theo cự ly rồi:** cự ly nói điểm **ở đâu**, biên
> độ nói map và scan **bất đồng nặng cỡ nào** — hai trục khác nhau. Trung vị **có dấu** của vùng
> <20 m là **+0,84 m**, trông y hệt "nhiễu sát ngưỡng 0,5". Nhưng đó là ảo giác do dấu trộn lẫn:
> trung vị **|lệch|** của chính nhóm đó là **2,62 m** và **29,7% lệch trên 5 m**. Không có hai cột
> này thì mục 1 của handoff hôm nay đã ghi sai vĩnh viễn là *"vùng gần = nhiễu ngưỡng"*.

**Ba quần thể, ba cơ chế — checklist giả định FP là một khối nên cả hai tiêu chí của nó đều không mô tả nổi:**

- **>40 m (471 FP, 41%)** — 96,6% âm, trung vị lệch **8,5 m**, 58,6% lệch trên 5 m. Map có bề mặt nằm
  hẳn phía trước trên đúng tia đó. Parallax + mật độ map gấp 6,7 lần. → **ĐƯỜNG ỐNG, rõ ràng.**
- **20–40 m (282 FP, 25%)** — trung vị lệch **1,19 m**, 74,1% dưới 2 m. Quần thể **sát ngưỡng nhất**,
  nằm đúng vành đai có vật động thật. → **LUẬT QUYẾT ĐỊNH.**
- **<20 m (391 FP, 34%)** — hỗn hợp: 61,1% dương, trung vị |lệch| 2,62 m, 24,3% dưới 1 m nhưng 29,7%
  trên 5 m. → **CHƯA GỌI TÊN SẠCH.** Giả thuyết chưa đo: parallax cận cảnh (điểm nhìn cách 10–20 m
  thì vật ở 10 m lệch góc cực lớn).

### 6.4 Con số để dành cho phần viết bài

**TP có trung vị |lệch| = 15,0 m, 95,7% lệch trên 5 m**, trong khi ngưỡng đặt ở **0,5 m** — thấp hơn
30 lần biên độ của thứ nó cần bắt. Đó là luận điểm đề tài ở dạng số: **ngưỡng cứng đơn biến không
phân biệt nổi "lệch 2 m do parallax" với "lệch 15 m do vật động"**, còn bộ phân loại học từ dữ liệu
thì có (nó thấy cả `range`, cả biên độ ở 4 thang, cả dấu).

⛔ **KHÔNG được vì thế mà nâng ngưỡng.** Ngưỡng cứng CHÍNH LÀ baseline của đề tài.

---

## 7. Việc chốt theo nhánh ĐƯỜNG ỐNG — **để phiên sau, vì đụng C++**

- [ ] **Cắt bán kính 40 m vào baseline** — bỏ **471 FP**, mất **1 TP**.

| | P | R | F1 |
|---|---|---|---|
| hiện tại | 0,208 | 0,814 | 0,332 |
| dự kiến sau khi cắt 40 m | **0,308** | 0,811 | **0,446** |

  Đây **không phải tune ngưỡng**: giới hạn bán kính là thứ removert gốc vốn có, thuộc loại "vá hiển
  nhiên" ở điều kiện (2) mục 15.9. Vá vào baseline để MLP **không** học được cái cổng khoảng cách đó
  như một chiến thắng giả.
- [ ] Đo lại ba mốc P/R/F1 trên seq04/150, seq06/349, seq06/939.
- [ ] Rồi mới đi bước 3 (chạy nhiều scan, nối CSV lại).

> ⚠️ Con số 0,308 tính từ **một** scan. Phải đo lại trên cả ba mốc trước khi tin.

---

## 8. Còn treo, KHÔNG làm hôm nay

- **391 FP vùng <20 m** chưa gọi tên được cơ chế. Điều kiện (1) của "baseline đủ tử tế" (*mỗi cụm FP
  lớn gọi tên được*) vì thế **chưa đạt trọn vẹn** — đạt 2/3 cụm.
- **Bag `standstill_*`** (mục 15.2) — nửa buổi riêng, quân bài rẻ nhất chưa lật.
- **Bước 3 + bước 4** (nối nhiều scan → MLP numpy).
- **`scripts/viz_scan.py`** — gói mấy panel vẽ hình thành script dùng lại được (~60 dòng), để tự chạy
  khi cần nhìn hình học của một scan bất kỳ.

## 9. Đồng bộ tài liệu — CẦN XỬ LÝ

Bản trong repo **lệch** bản ổ D và đang cũ hơn:

| File | Ổ D | Repo `~/mini_removert` |
|---|---|---|
| `HANDOFF_2026-09-01.md` | **824 dòng, có mục 15** | 643 dòng, **thiếu hẳn mục 15** |
| `START_HERE.md` | có khối soát 02/09 | bản cũ |

Ổ D là bản chuẩn. Chưa copy sang repo, chưa commit.
