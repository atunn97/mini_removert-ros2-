# HANDOFF — mini_removert, phiên Linux 01/09/2026

> File này viết cho một phiên làm việc **mới, trên máy Linux**, không có ngữ cảnh gì từ phiên
> Windows. Đọc hết mục 1 và 2 trước khi động vào code.
>
> ⚠️ Người viết file này **chưa từng thấy `mini_removert`** — nó không nằm trên máy Windows.
> Mọi mô tả về trạng thái code ở đây là do người dùng kể lại, phải kiểm tra trước khi tin.

---

## 1. Bối cảnh — vì sao đụng vào removert lúc này

Chủ dự án là sinh viên HCMUTE, đang chạy song song nhiều môn. Sáng 01/09 vừa **đăng ký đề tài
bài tập nâng cao** cho môn TT KTMT, và đề tài đó biến `mini_removert` từ "việc để dành" thành
**việc nằm trên đường găng**.

| | |
|---|---|
| Tên đề tài | **Learning-Based Dynamic Point Removal for Low-Cost LiDAR Mapping** |
| Hướng | Mô hình DL |
| Mô tả | Học máy thay ngưỡng cứng để lọc điểm động trong bản đồ LiDAR |
| Paper base | **LMNet** — Chen et al., RA-L/IROS 2021 · `arxiv.org/abs/2105.08971` · code `PRBonn/LiDAR-MOS` |
| Baseline | **Removert** — Kim & Kim, IROS 2020 · `github.com/gisbi-kim/removert` |

**Ý tưởng:** removert quyết định một điểm là động hay tĩnh bằng **ngưỡng đặt tay** trên độ
lệch range image. Ngưỡng đó suy giảm mạnh trên LiDAR giá rẻ nhiễu nặng. Đề tài thay chỗ quyết
định đó bằng một bộ phân loại học từ dữ liệu.

Nghĩa là mô hình cần **đặc trưng theo từng điểm**, mà thứ tính ra đặc trưng đó chính là
`mini_removert`. Không có dữ liệu xuất ra từ nó thì tầng 1 chỉ chạy được trên dữ liệu mô phỏng
mãi mãi.

---

## 2. ⚠️ MỤC TIÊU PHIÊN NÀY — hẹp, đọc kỹ trước khi làm

Chỉ **hai** gạch. Không hơn.

```
1. Pipeline chạy trơn ĐẦU–CUỐI trên bag đã có sẵn
2. Thêm hàm xuất đặc trưng từng điểm ra CSV
```

**Luật do chính chủ dự án đặt ra, chép nguyên từ HANDOFF-DS:**

> *"Không thêm feature, không tune tham số, chỉ ghép module. Nếu thấy mình đang tune tham số
> là đã đi lạc."*

**Không phải mục tiêu:**

- ❌ Làm map đẹp hơn. LiDAR L1 có **nhiễu tự phản xạ 87–92%** — đây là **giới hạn phần cứng,
  không phải lỗi code**. Đụng vào là mất buổi. Ghi vào mục Limitations của báo cáo, không sửa.
- ❌ Accumulated maps — đã bỏ khỏi phạm vi tháng này, cần block dài và có thể ngốn hết thời
  gian mà vẫn không tổng quát hoá được.
- ❌ Tune ngưỡng cho ra kết quả đẹp. Ngưỡng đặt tay chính là **baseline** của đề tài; tune nó
  là tự phá baseline của mình.

**Chỗ ngược đời cần nhớ:** `mini_removert` làm chưa tốt **là nguyên liệu**, không phải trở
ngại. Ngưỡng cứng sai ở đâu — đó là baseline để đối chiếu. Nếu nó chạy hoàn hảo thì không có
đề tài nào cả. Đừng cố làm nó hoàn hảo trước rồi mới áp ML.

---

## 3. Bộ cột CSV — chốt TRƯỚC khi viết hàm xuất

Đây là quyết định quan trọng nhất của phiên. **Thiếu cột thì phải chạy lại toàn bộ bag để bù**,
nên thà thừa còn hơn thiếu.

### Nhóm định danh — để lần ngược khi debug

| Cột | Ghi chú |
|---|---|
| `scan_id` | chỉ số scan trong bag |
| `point_id` | chỉ số điểm trong scan |
| `x, y, z` | toạ độ trong khung của scan |

### Nhóm đặc trưng — đầu vào của mô hình

| Cột | Vì sao cần |
|---|---|
| `range` | khoảng cách. Nhiễu tự phản xạ tập trung ở dải gần |
| `intensity` | cường độ phản xạ. Tia phản xạ từ thân robot có đặc tính cường độ khác hẳn |
| `row`, `col` | vị trí trong range image. Cho mô hình biết ngữ cảnh hình học |
| `range_map` | range của **bản đồ** tại đúng pixel đó |
| `range_diff` | `range_map − range`. ⭐ **Đây chính là đại lượng removert đang lấy ngưỡng** |
| `range_ratio` | `range / range_map`. Đôi khi ổn định hơn hiệu, nhất là ở dải xa |
| `neighbor_count` | số điểm trong bán kính `r` cho trước. Nhiễu thường thưa, vật thể thật thì đặc |
| `incidence_angle` | góc tới ước lượng từ pháp tuyến cục bộ. **Nếu tốn quá thì bỏ qua vòng đầu**, ghi lại là đã bỏ |
| `resolution_level` | removert chạy đa phân giải — phải biết dòng này sinh ở mức nào |

### Nhóm nhãn — hai cột, đừng gộp

| Cột | Nội dung |
|---|---|
| `baseline_label` | quyết định của **ngưỡng cứng hiện tại** (0 = tĩnh, 1 = động) |
| `gt_label` | nhãn thật. Vòng đầu để trống hoặc `-1` |

⭐ **`baseline_label` là cột quan trọng nhất về mặt nghiên cứu.** Không có nó thì không so được
"mô hình học" với "ngưỡng đặt tay", mà **không có so sánh thì không có khẳng định nào cả** —
bài báo sẽ rỗng. Đừng quên cột này.

### Về `gt_label`

Nhãn thật là chỗ tốn công nhất, không phải phần huấn luyện. Hai đường:

- **Nhiễu tự phản xạ** gán được bán tự động bằng hình học: tia rơi vào trong bán kính thân
  robot thì gần như chắc chắn là phản xạ từ chính nó. Rẻ, làm được ngay.
- **Nhãn động/tĩnh** thì phải gán tay hoặc mượn tập có sẵn. Để sau.

Vòng này chỉ cần xuất được cột trống đúng định dạng.

---

## 4. Những gì CHƯA BIẾT — kiểm tra đầu phiên

Người viết file này chưa thấy code. Hỏi hoặc tự kiểm sáu điều sau trước khi lên kế hoạch:

1. `mini_removert` đang ở đâu, build bằng gì? (nghe nói **C++17 + PCL**, tự viết lại từ removert gốc)
2. Build còn chạy được không? Thử build sạch trước khi sửa bất cứ thứ gì.
3. Bag nằm ở đâu, định dạng gì (ROS1 `.bag` hay ROS2 `.db3`), quay ở cảnh nào, dài bao lâu?
4. Pipeline hiện dừng ở bước nào? "Chưa hoàn chỉnh" nghĩa là chạy được một phần, hay không chạy?
5. Range image đã dựng được chưa? Nếu **rồi** thì `range_diff` gần như có sẵn, việc nhẹ hẳn.
6. Ngưỡng cứng hiện đang đặt ở đâu trong code, giá trị bao nhiêu?

Câu 5 quyết định phiên này dễ hay khó. Kiểm nó trước.

---

## 5. Phía Python — làm sau, đừng làm chung phiên

- Tầng 1 theo yêu cầu của giảng viên: **tái hiện bằng Python trước**, đừng nhảy thẳng vào
  phần cứng.
- MLP nhỏ viết bằng **numpy**. Không torch, không GPU.
- Chỉ số: accuracy, precision, recall, F1 — cùng bộ với đồ án Toán rời rạc, dùng lại được.
- **Phải có baseline trong mọi bảng kết quả:** ngưỡng cứng vs mô hình học, trên cùng dữ liệu.

**Hai đường tách bạch, đừng buộc vào nhau:**

| | Chạy trên gì | Phụ thuộc |
|---|---|---|
| Bài tập nâng cao nộp thầy | dữ liệu mô phỏng + phương pháp luận | không phụ thuộc bag |
| NCKH / chương LVTN | bag thật, ≥ 3 cảnh, có baseline | phụ thuộc bag |

Mô phỏng **đủ** để nộp bài tập, **không đủ** để viết bài báo.

---

## 6. Còn nợ bên Windows — không làm ở phiên này

| Việc | Ghi chú |
|---|---|
| Xuất PDF file hướng dẫn 8086 | `C:\Users\User\Downloads\8086 Sample Program Guide - Sum of Two Numbers.docx`, mở Word → Save As PDF → bỏ vào `nộp bài\8086_testcong\` → commit |
| Nộp UTEXLMS | ⏸ **thầy chưa mở link**, không làm gì được |
| **M3 — outline + bảng phân công** | ⚠️ hạn **trước 06/09**, không phụ thuộc dữ liệu. Thiếu milestone là **0 điểm toàn đồ án Toán rời rạc**. Đây là việc có deadline gần nhất trong tất cả |
| Việc #14 — chỉnh ngân sách giờ | theo hệ quả "báo cáo viết tiếng Anh" |
| Nói trôi 5 câu vấn đáp Đ3 | ở cuối `~/Desktop/DS/chuong2_nhap.md` |

---

## 7. Câu hỏi còn treo với giảng viên

- **M4 rơi vào tuần mấy?** Đã hỏi, chưa có trả lời. Câu này quyết định cả kế hoạch đồ án DS.
- **Hạn nộp bài tập nâng cao?** Nếu trước 18/09 thì bản mô phỏng là bản duy nhất kịp.
- **Bài tập nâng cao 10% hay 20%?** Slide ghi 10%, ghi chú cũ ghi 20% điểm giữa kỳ.

---

## 8. File liên quan bên Windows

| Nội dung | Ở đâu |
|---|---|
| HANDOFF đồ án Toán rời rạc, bản mới nhất | `~/HANDOFF/HANDOFF-DS-01-09.md` |
| Bản 31/08 và 24/08, đọc kèm | `~/HANDOFF/HANDOFF-DS-31-08.md`, `~/HANDOFF/HANDOFF-DS.md` |
| Nháp Chương 2 — Đ1, Đ1b, Đ2, Đ3 | `~/Desktop/DS/chuong2_nhap.md` |
| Repo môn TT KTMT | `C:\Users\User\TT-KTMT\` → `github.com/atunn97/COAA_lab` |

⚠️ **Đừng gửi file `.md` này qua Zalo** — Zalo cắt file markdown ở đúng 1025 byte, đã dính hai
lần. Dùng USB, thư mục chia sẻ của máy ảo, hoặc đẩy lên git.

---

## 9. Kiểm 6 câu của mục 4 — kết quả (phiên Linux chiều 01/09)

Làm đúng thứ tự mục 4 dặn, kiểm trước khi lên kế hoạch. Câu 5 ra kết quả tốt nhất có thể.

| # | Câu hỏi | Kết quả |
|---|---|---|
| 1 | Ở đâu, build bằng gì | `~/mini_removert`, C++17 + PCL 1.14 + Eigen, CMake thuần qua colcon. Đúng như đoán |
| 2 | Build còn chạy không | ✅ **build sạch thật, đã xác nhận**. Lần `colcon build` đầu chỉ ăn cache (0.19s), nên chạy lại bằng `cmake -S . -B build_local` — 7/7 translation unit biên dịch lại từ đầu, **0 cảnh báo**, link xong, exit 0. Nguồn hiện tại sạch về mặt biên dịch; mọi lỗi còn lại là lỗi logic |
| 3 | Bag ở đâu, định dạng gì | Xem mục 10 — repo **không đọc bag**, đã chốt phạm vi lại |
| 4 | Pipeline dừng ở đâu | Chạy hết, không crash, nhưng dở dang giữa refactor E2 (bug frame + `map_cloud` mồ côi) |
| 5 | ⭐ Range image có chưa | ✅ **CÓ RỒI** — `src/range_image/range_image.cpp:38` `buildRangeImage`, `:9` `projectToPixel` |
| 6 | Ngưỡng cứng ở đâu | `src/discrepancy/discrepancy.cpp:64` — `if (diff > threshold)`, giá trị vào từ `argv[5]`, chốt 0.5 |

**Hệ quả của câu 5:** `range`, `range_map`, `row`, `col` lấy được gần như miễn phí. Chỗ ghép
điểm↔pixel cũng có khuôn sẵn — `getDynamicIndices` (`src/filter/filter.cpp:9-32`) đã duyệt
đúng vòng lặp mà hàm xuất CSV cần. Phiên này nằm ở phía DỄ.

### 9.1. Hai chỗ bộ cột CSV ở mục 3 vênh với code thật

**a) `intensity` — hiện KHÔNG lấy được, đã hoãn (xem mục 10).**

**b) `range_diff` — mục 3 ghi `range_map − range` (CÓ DẤU), nhưng code đang vứt dấu đi.**
`discrepancy.cpp:60` dùng `std::fabs(scan_range - map_range)`. Với ML thì dấu mang thông tin
thật: dương = có vật gần hơn map (nhiều khả năng động), âm = xa hơn map (che khuất / map
thiếu dữ liệu). Gộp vào trị tuyệt đối là ném mất phân biệt đó.

→ Khi viết hàm xuất CSV, **đừng lấy lại output của `computeDiscrepancy`**. Tính lại hiệu có
dấu thẳng từ `scan_image` và `map_image` — cả hai đều nằm sẵn trong tay ở chỗ gọi. Vẫn giữ
`computeDiscrepancy` nguyên như cũ cho đường baseline, không đụng vào.

### 9.2. Vênh về hạt: `baseline_label` không thể phụ thuộc `resolution_level`

Mục 3 để `resolution_level` là một cột, ngụ ý mỗi dòng ứng với một (điểm, thang). Nhưng
`baseline_label` = `final_dynamic_indices` (`main.cpp:294`), mà giá trị đó chỉ có SAU khi
vote + revert đã chạy qua **mọi** thang — nó là đại lượng của cả điểm, không của từng thang.

Chưa chốt. Hai đường: (i) mỗi dòng = một điểm, các đại lượng theo thang trải ngang thành
`range_diff_L0..L3`; (ii) mỗi dòng = (điểm, thang), `baseline_label` lặp lại y nhau ở 4 dòng.
**Quyết trước khi viết hàm xuất**, vì đổi sau là chạy lại toàn bộ.

---

## 10. Đã hoãn có chủ ý — để dành lúc rảnh, KHÔNG phải bỏ

Chốt chiều 01/09 sau khi kiểm mục 4. Ghi lại đủ để lần sau vào việc không phải điều tra lại.

### 10.1. Đọc bag Go2 — hoãn

Repo chỉ nhận `<pcd_dir> <poses.txt> <calib.txt>` (`main.cpp:28-30`), tức SemanticKITTI dạng
PCD. Không có reader cho ROS2 `.db3`. Viết reader **phá luật "chỉ ghép module"** ở mục 2 nên
không làm trong phiên này.

**Bag vẫn còn nguyên, đã kiểm toàn vẹn — không hỏng, không mất:**

| Bag | Dung lượng | Kiểm |
|---|---|---|
| `~/dataset/standstill_multifeature_20260810_140258/` | 304M | ✅ mở được, 44.111 message |
| `~/dataset/standstill_multifeature_20260810_095201/` | 551M | ✅ |
| `~/dataset/rosbag2_2026_07_10-12_45_29/` | 279M | ✅ mở được, 39.274 message |
| `~/dataset/rosbag2_2026_07_10-12_29_24/` | 377M | ✅ |
| `~/go2_tests/map_ros2/` | 1.7G | ✅ |

Bản `standstill_..._140258`: dài 167,5s, 2.577 `sensor_msgs/msg/PointCloud2` + 41.534
`sensor_msgs/msg/Imu`.

Muốn dùng thì cần: đọc `PointCloud2` ra cloud, **và** một nguồn pose cho từng frame (KITTI có
`poses.txt` sẵn, bag thì không — phải chạy SLAM hoặc lấy odometry từ bag). Chỗ pose mới là
phần tốn công, không phải chỗ đọc điểm.

### 10.2. Cột `intensity` — hoãn

Đã bị vứt từ khâu chuyển đổi, không phải thiếu ở C++:

- `scripts/bin_to_pcd.py:6-7` — `xyz = points[:, :3]`, header ghi `FIELDS x y z`. Cột thứ 4
  của `.bin` KITTI chính là intensity, bị bỏ ngay lúc ghi PCD.
- `include/io/pcd_frame.hpp` — `Frame.cloud` là `PointCloud<pcl::PointXYZ>`, không có ô chứa.

Muốn có: sửa `bin_to_pcd.py` → convert lại cả sequence (chỉ tốn CPU, dữ liệu `.bin` gốc vẫn
còn) → đổi `PointT` sang `PointXYZI` ở **mọi** module. Thay đổi xuyên suốt, không nhỏ.

⚠️ **Lý do hoãn không chỉ là tốn công:** lập luận đưa `intensity` vào ở mục 3 là *"tia phản xạ
từ thân robot có đặc tính cường độ khác hẳn"* — đó là chuyện của **nhiễu tự phản xạ Livox L1
trên Go2**, không phải của KITTI. Trên KITTI không có hiện tượng đó, nên cột này gần như vô
dụng ở phạm vi đã chốt. Khi nào làm 10.1 thì làm luôn 10.2 — hai việc đi cùng nhau.

### 10.3. `incidence_angle` — hoãn (mục 3 đã cho phép sẵn)

Mục 3 ghi rõ *"nếu tốn quá thì bỏ qua vòng đầu, ghi lại là đã bỏ"*. Ghi ở đây: **đã bỏ vòng đầu.**

### 10.4. Real-time — không thuộc phiên này

Binary là chương trình offline một phát: nhận `scan_idx`, đọc PCD từ đĩa, ghi
`dynamic_indices_scanN.txt`, `return 0`. `CLAUDE.md` ghi rõ đây **không** phải ROS node, không
dùng `rclcpp` — đúng chủ ý thiết kế, không phải khiếm khuyết.

Kiến trúc real-time đã có chỗ riêng: `HANDOFF_VIEC4.md` mục 7 (Removert chạy song song
FAST-LIO2, FAST-LIO2 giữ on-board Jetson) và `HANDOFF_2026-08-13.md:195`. Đọc hai chỗ đó khi
tới lúc, đừng thiết kế lại từ đầu.

### 10.5. Phạm vi đã chốt cho phiên này

**KITTI seq04 / seq06.** Dữ liệu có sẵn: seq04 = 271 scan + 271 label, seq06 = 1101 scan +
1101 label. Có nhãn nên chấm được P/R/F1 — điều kiện bắt buộc để `baseline_label` có nghĩa.

### 10.6. Thứ tự đã đảo so với dự kiến ban đầu

Mục 2 đặt mục tiêu "pipeline chạy trơn đầu–cuối" rồi "xuất CSV". Nhưng `baseline_label` là
cột quan trọng nhất của cả bộ dữ liệu, mà nó lấy từ `final_dynamic_indices` — thứ đang RÁC vì
bug frame ở `main.cpp:159`. Xuất CSV trước = sinh ra tập dữ liệu có cột quan trọng nhất toàn
nhãn sai, rồi phải xuất lại từ đầu.

→ **Chốt: việc 4 → việc 3 → xuất CSV.** Sửa bug xong mới có nhãn đáng tin để đóng gói.

---

## 11. E2 việc 4 — XONG, tiêu chí số 1 đã đạt

Chủ repo tự làm 4.1 + 4.2 (đóng vòng gộp, dời khối voxel và vòng thang ra ngoài). Claude làm
4.3 + 4.4 theo quy ước mới ghi ở `CLAUDE.md` (sửa đổi 01/09).

### 11.1. Đã sửa gì

| Chỗ | Trước | Sau |
|---|---|---|
| `main.cpp` ~159 | `scan_in_map_frame = transform(scan_frame.cloud, relative_pose)` | **xoá** — scan giữ nguyên hệ của nó |
| ground mask của map | `detectGroundMask(map_frame.cloud, ...)` | `detectGroundMask(map_cloud, ...)` |
| `buildRangeImage` scan | `scan_in_map_frame` | `scan_frame.cloud` |
| `buildRangeImage` map | `map_frame.cloud` (một frame lẻ) | `map_cloud` (tích luỹ + voxel) |
| `getDynamicIndices` / `getObservedIndices` | `scan_in_map_frame` | `scan_frame.cloud` |
| dòng in `vs map[map_idx]` | `map_idx` — **ngoài phạm vi** sau khi đóng vòng | in số nguồn đã gộp |
| `n_maps` khối DEBUG | `map_indices.size()` | `1` (số LƯỢT so sánh, không phải số frame) |

Tầng voting (`is_dynamic_at` + vòng REMOVE/REVERT) **không đụng một dòng nào**, đúng yêu cầu
mục 5.1 việc 4.

⚠️ **Ba lỗi phạm vi mà bản 4.1+4.2 chưa build nên chưa lộ:** sau khi đóng vòng gộp thì
`map_frame` và `map_idx` đều hết phạm vi, còn `scan_in_map_frame` đã bị xoá — cả ba vẫn bị
tham chiếu ở dưới. File lúc đó **không biên dịch được**. Bài học: dời khối code xong thì build
ngay, đừng dời tiếp.

### 11.2. Điểm dừng — tất cả đạt

| Kiểm | Mốc | Đo được |
|---|---|---|
| Build sạch | 0 lỗi | ✅ 7/7 unit, **0 cảnh báo** |
| seq04/150 gộp + voxel | 1.141.089 → 118.574 | ✅ **khớp từng chữ số** |
| seq06/939 gộp + voxel | 993.711 → 133.939 | ✅ **khớp từng chữ số** |
| `observed_count` | phải vào {0,1} | ✅ `min=0 max=1` mọi thang (trước là `max=N`) |
| `count==N` | khác 0 | ✅ 32.8% / 49.5% / 57.0% / 62.8% |

### 11.3. ⭐ Tiêu chí số 1 — ĐẠT

| scan | F1 trước việc 4 | F1 sau việc 4 |
|---|---|---|
| seq06/349 | **0.000** | **0.061** (P 0.032 / R 0.590) |
| seq06/939 | **0.000** | **0.131** (P 0.073 / R 0.682) |

Hết 0. Theo mục 9 phiên 20/8 thì đây là cửa ải phân biệt "lỗi cài đặt" với "ý tưởng sai" —
qua được nghĩa là cách gộp map tích luỹ có cơ sở, đúng như AUC 0.876/0.980 đã báo trước.

### 11.4. ⚠️ NHƯNG precision rất tệ — đừng mừng vội

| scan | P | R | F1 |
|---|---|---|---|
| seq04/150 | 0.217 | 0.814 | **0.343** |
| seq06/349 | 0.032 | 0.590 | 0.061 |
| seq06/939 | 0.073 | 0.682 | 0.131 |

Recall khá (0.59-0.81) nhưng precision sụp: seq04/150 có 301 TP / **1.084 FP**. Tức thuật toán
đang **bắt được vật động thật nhưng bắt kèm quá nhiều thứ không phải**.

**seq04/150 tụt từ 0.707 xuống 0.343.** Ba điều phải nhớ trước khi kết luận gì:

1. **Không so cặp được.** Mốc 0.707 đo trên pipeline CŨ (so từng map một, N=4 chọn theo
   khoảng cách). Giờ là thuật toán khác (một map tích luỹ, 9 nguồn theo thang bậc). So thẳng
   hai con số là so hai thứ khác nhau.
2. **Việc 3 chưa làm — và đây gần như chắc chắn là nguyên nhân chính.** Ground đang fit bằng
   HAI lần RANSAC riêng: một trên `scan_frame.cloud`, một trên `map_cloud`. Hai mặt phẳng
   khác nhau ⇒ điểm mặt đường bị loại khỏi ảnh này mà không bị loại khỏi ảnh kia ⇒ sinh
   chênh lệch range giả ⇒ FP. Việc 3 (`detectGroundPlane` một lần trên scan rồi `maskFromPlane`
   hai lần) sinh ra chính là để đóng chỗ này.
3. **Tiêu chí số 6:** ΔF1 < 0.07 đo trên MỘT ground mask thì không kết luận được gì. Muốn
   phát biểu về F1 phải chạy `run_seed_sweep.py` — mà nó lại đang chờ mục 5.2.

→ **Đừng ghi 0.343 vào đâu làm mốc.** Nó là số của trạng thái nửa đường.

### 11.5. Việc tiếp theo, đúng thứ tự

1. **E2 việc 3** — ground một mặt phẳng. `detectGroundPlane` + `maskFromPlane` đã viết sẵn
   trong `ground_filter.hpp`, chỉ việc gọi. Cạm bẫy đã ghi trong chính header đó: PCL trả
   pháp tuyến **dấu tuỳ ý**, nên chỉ được dùng `|n·p + d| <= 0.2`.
2. Đo lại 3 scan trên. Kỳ vọng: precision lên rõ. Không lên ⇒ FP đến từ chỗ khác, đi tìm tiếp
   chứ đừng tune ngưỡng (tune ngưỡng là phá baseline của đề tài — mục 2).
3. Mục 5.2 (nối `run_seed_sweep.py`) để phát biểu được về F1 cho tử tế.
4. Rồi mới tới hàm xuất CSV (mục 3 + ghi chú 9.1, 9.2).

### 11.6. Còn 5 tiêu chí chưa kiểm

Mới kiểm số 1. Số 2 (sweep threshold phải ra **cao nguyên**, không phải vách đá), số 3 (seq06
lên nhiều hơn seq04 — hiện seq04 đang ĐI XUỐNG, cần giải thích), số 4 (in `R` và `#nguồn`),
số 5 (seq07/673 vẫn phải xấu), số 6 (`run_seed_sweep.py`). Danh sách đầy đủ ở
`HANDOFF_2026-08-20.md` mục 9.

---

## 12. E2 việc 3 — XONG về cài đặt, nhưng KHÔNG cải thiện số. Đọc kỹ mục 12.3.

### 12.1. Đã sửa gì

`detectGroundPlane` và `maskFromPlane` đã có sẵn trong `ground_filter.cpp` từ phiên trước và
đã đối chiếu trùng từng điểm với `detectGroundMask` — việc 3 chỉ còn là nối dây ở `main.cpp`:

| Trước | Sau |
|---|---|
| `scan_ground_mask = detectGroundMask(scan_frame.cloud, seed)` | `ground_plane = detectGroundPlane(scan_frame.cloud, seed)` rồi `maskFromPlane(scan_frame.cloud, ground_plane)` |
| `map_ground_mask = detectGroundMask(map_cloud, seed)` — **RANSAC lần hai** | `maskFromPlane(map_cloud, ground_plane)` — **dùng lại mặt phẳng của scan** |

Dùng thẳng hệ số của scan cho map được vì `map_cloud` ĐÃ ở hệ scan (đưa về từ bước gộp).
Cùng hệ toạ độ thì cùng phương trình mặt phẳng — "đề bài C" tự tan biến, đúng như dự đoán ở
header `ground_filter.hpp`.

**Chốt kiểm cài đặt:** `ground seed=0: 63156/126941 (49.7522%)` — **y hệt từng chữ số** so với
trước việc 3. Đúng như header đã khẳng định: `maskFromPlane(c, detectGroundPlane(c,s), t)` ≡
`detectGroundMask(c, s, t)`. Nghĩa là phía scan chứng minh được là không đổi, mọi thay đổi số
liệu chỉ đến từ phía map.

### 12.2. Số đo — việc 3 KHÔNG cải thiện gì

| scan | P trước → sau | R trước → sau | F1 trước → sau | ΔF1 |
|---|---|---|---|---|
| seq04/150 | 0.217 → **0.208** | 0.814 → 0.814 | 0.343 → **0.332** | −0.011 |
| seq06/349 | 0.032 → 0.032 | 0.590 → 0.546 | 0.061 → **0.061** | 0.000 |
| seq06/939 | 0.073 → **0.062** | 0.682 → 0.782 | 0.131 → **0.115** | −0.016 |

**Theo tiêu chí số 6 (ΔF1 < 0.07 trên một mask thì không kết luận), cả ba delta đều nằm dưới
sàn nhiễu.** Không được phép nói việc 3 làm tốt lên hay xấu đi. Chỉ được nói: **không đo thấy
khác biệt.**

⚠️ **Giả thuyết ở mục 11.4 — "hai mặt phẳng RANSAC riêng là thủ phạm chính gây FP" — SAI.**
Đã hợp nhất mặt phẳng rồi mà precision không nhúc nhích. Nguồn FP nằm chỗ khác. Đừng đi lại
đường này ở phiên sau.

### 12.3. ⭐ Số đáng giá nhất phiên này: ground của map chỉ 38.3% so với 49.8% của scan

Thêm một dòng in chẩn đoán (`main.cpp`, ngay sau `map_ground_mask`), seq04/150:

```
ground seed=0:            63156/126941  (49.7522%)   <- scan
ground cua map tich luy:  45428/118574  (38.3119%)   <- map, CÙNG một mặt phẳng
```

**Cùng một mặt phẳng, nhưng bắt được 49.8% điểm của scan mà chỉ 38.3% điểm của map.** Lệch
11.4 điểm phần trăm.

**Cơ chế:** mặt phẳng được fit trên scan — tức fit tại CHỖ ĐỨNG, phạm vi vài chục mét. Map
tích luỹ gộp 9 frame trải dài cả trăm mét đường. Mặt đường thật không phẳng tuyệt đối: càng
xa chỗ fit, nó càng cong ra khỏi mặt phẳng đó. Phần cong quá 0.2m **không bị mask**, nên đi
thẳng vào range image của map như thể là vật thể. Scan không có những điểm đó ⇒ chênh lệch
range ⇒ **false positive**.

Đây cũng giải thích vì sao việc 3 làm seq04 hơi XẤU đi: trước việc 3, `map_cloud` được RANSAC
fit riêng cho chính nó nên mặt phẳng bám sát cả dải đường dài hơn, mask được nhiều đường hơn.
Bằng chứng gián tiếp trong log: `observed` ở thang 64x900 tăng từ **41.603 → 44.164** sau
việc 3 — đúng chiều "map còn lại nhiều điểm hơn vì mask ít đi".

### 12.4. Vì sao vẫn GIỮ việc 3

Giữ, dù không đo thấy lợi:

- Đúng **nguyên lý 3** đã ghi trong `main.cpp` (hai ảnh phải đi qua CÙNG một phép rút gọn).
  Hai mặt phẳng khác nhau là một biến ẩn không kiểm soát được.
- Bớt một lần RANSAC.
- Delta dưới sàn nhiễu nên số liệu **không phản đối** nó.

Nhưng phải ghi rõ: **giữ vì nguyên lý, không phải vì đo được lợi.**

### 12.5. Việc tiếp theo — đi tìm nguồn FP thật

Đừng tune ngưỡng (phá baseline của đề tài, mục 2). Theo `CLAUDE.md`, cách của repo này là
**viết script Python đo độc lập** trước khi đụng C++ — đã bác bỏ 3 giả định sai về RANSAC bằng
cách đó.

Ba hướng, theo thứ tự đáng thử:

1. **Ground cong** (mục 12.3, có số hậu thuẫn mạnh nhất). Kiểm rẻ: chia `map_cloud` theo
   khoảng cách tới gốc scan, in tỷ lệ ground theo từng vành đai. Nếu tỷ lệ tụt dần theo
   khoảng cách thì đúng là mặt phẳng không mô tả nổi map dài. Hướng xử lý *sau khi xác nhận*:
   ground theo ô lưới, hoặc giới hạn bán kính map — **không phải** nới `distance_threshold`.
2. **Parallax / che khuất của map tích luỹ.** Map lấy `min(range)` mỗi pixel trên 9 điểm nhìn
   khác nhau, nên nó "nhìn xuyên" được chỗ scan bị che. `fabs()` ở `discrepancy.cpp:60` tính
   cả hai chiều lệch là như nhau, trong khi hai chiều mang ý nghĩa vật lý khác hẳn (xem 9.1).
3. **Tỷ lệ REVERT ~80-90%** ở cả ba scan. Remove bắt 13.809 ứng viên rồi vứt 12.364. Tầng
   remove đang quá nhạy, hoặc thang thô đang xác nhận nhầm.

### 12.6. Trạng thái để phiên sau vào việc ngay

- Việc 3 + 4 **đã xong và đã build sạch** (0 cảnh báo). Binary hiện tại cho số ĐỌC ĐƯỢC —
  khác hẳn trạng thái đầu phiên.
- **Chưa commit.** `git diff` gồm `src/main.cpp`, `CLAUDE.md`, và file handoff này.
- Vẫn còn 5/6 tiêu chí chưa kiểm (mục 11.6).
- `run_seed_sweep.py` vẫn chờ mục 5.2 của handoff 27/8 — chưa có nó thì mọi phát biểu về F1
  đều vướng tiêu chí số 6.

---

## 13. Điều tra nguồn FP — giả thuyết "mặt đường cong" BỊ BÁC BỎ

Viết `scripts/diag_ground_curve.py` (không đụng C++). Chạy seq04/150 và seq06/939.

### 13.1. Kết quả: (A) sai

| | seq04/150 | seq06/939 |
|---|---|---|
| `R` khớp được | 14.398 m | **−4.251 m** (âm) |
| `R²` | 0.866 | **0.262** |
| Vách `ground%` **dự báo** | 75.9 m | — |
| Vách `ground%` **thật** | **10-20 m** | 20-30 m |

Ba lý do bác bỏ: `R` ra âm ở seq06 (đường không thể vừa lồi vừa lõm); `R²=0.26` tức không có
quy luật `r²`; và ở seq04 vách dự báo lệch vách thật gần một bậc độ lớn. Độ trôi 0.16m qua
90m ứng với bán kính 14km — thẳng gần như tuyệt đối, quá yếu để giải thích gì.

### 13.2. Nguyên nhân THẬT của chênh lệch 49.8% / 38.3%: phân bố bán kính

Cùng vành đai thì tỷ lệ ground **xấp xỉ nhau** (20-30m: scan 37.1% vs map 39.3%; 30-40m:
scan 32.7% vs map **43.0%** — map còn cao hơn). Chênh lệch tổng đến từ **tỷ trọng**:

| | trong 10m | ngoài 30m |
|---|---|---|
| SCAN | 59.137 điểm = **46.6%** tổng | 4.6% |
| MAP_CLOUD | 12.202 điểm = **10.3%** tổng | **32.8%** |

Scan gom gần nửa số điểm trong bán kính 10m nơi ground 67.6%; map trải ra xa nơi ground chỉ
12-40%. Trung bình có trọng số: scan 50.2%, map 39.4% — khớp 49.75% và 38.3% mà C++ in ra.

⇒ **Khoảng chênh 11.4 điểm phần trăm là LÀNH TÍNH.** Map không hề bỏ sót mặt đường của nó.
Manh mối này chết. Nguồn FP vẫn chưa tìm ra.

### 13.3. Phát hiện phụ đáng giữ: DẢI CHÂN TRỜI (cho phần Limitations)

Cột `dinh` nhảy về ≈ −1.75 ở vành đai 50-80m ở **cả ba bảng, cả hai sequence**. Giải mã:
`sd = −z − 1.754`, nên `sd = −1.754` ⇔ `z = 0` ⇔ **đúng tầm cảm biến**. Các đỉnh đo được quy
ra `z` đều nằm trong ±25cm quanh 0.

Cơ chế — hình học lấy mẫu, không phải khuyết tật thuật toán:

| Vành đai | Góc chúi cần | Số tia chạm đất (0.42°/tia) | #điểm thật |
|---|---|---|---|
| 0-10m | 9.95°-24.8° | ~35 | 59.137 |
| 10-20m | 5.0°-9.95° | ~12 | 52.061 |
| 20-30m | 3.35°-5.0° | ~4 | 9.957 |
| 50-60m | 1.67°-2.0° | **< 1** | 753 |
| 70-80m | 1.26°-1.43° | **< 1** | 246 |

Ngoài 40m gần như **không tia nào với tới mặt đường**. Thứ còn trả tín hiệu là chùm gần nằm
ngang, đập vào tường/cây ở đúng tầm cảm biến.

⚠️ **Lỗi trong `diag_ground_curve.py`:** cột `dinh` ngầm giả định "cụm đông nhất là mặt
đường". Ở vành đai xa giả định đó sai, nên cột này **lặng lẽ đổi đối tượng đo giữa chừng**.
Dòng `=== KET LUAN ===` tự động vì thế KHÔNG đáng tin — phải tự đọc bảng. Chưa sửa.

### 13.4. Hướng còn lại cho nguồn FP (chưa thử)

Số đáng nhìn nhất: **ngoài 30m, scan có 5.783 điểm còn map có 38.869 — gấp 6.7 lần.**
`getObservedIndices` tính một điểm scan là "quan sát được" khi map CÓ dữ liệu tại pixel đó.
Vùng xa là nơi map dày đặc còn scan gần như không lấy mẫu nổi, và 9 điểm nhìn cách nhau chục
mét thì ở 60-80m chúng nhìn thấy những bề mặt khác nhau. Đây là nghi phạm tiếp theo.

---

## 14. HÀM XUẤT CSV — XONG (mục tiêu chính của phiên)

### 14.1. Bước 1: `--csv` trong C++

Cờ mới, không có thì hành vi y như cũ và không tốn thêm gì (vòng `projectToPixel` phụ chỉ
chạy khi có cờ).

```bash
./build/mini_removert/mini_removert <pcd_dir> <poses> <calib> 150 0.5 <map_idx...> \
    --csv scan150.csv
```

**32 cột, một dòng = MỘT ĐIỂM:**

```
scan_id, point_id, x, y, z, range, is_ground,
row_L{0..3}, col_L{0..3}, range_map_L{0..3}, range_diff_L{0..3},
observed_L{0..3}, vote_L{0..3}, baseline_label
```

Bốn quyết định thiết kế, đều có lý do:

1. **`range` chỉ MỘT cột**, không phải bốn. `projectToPixel` trả `distance = |p|`
   (`range_image.cpp:20`), không phụ thuộc `h/w` — bốn thang cho ra y hệt một số.
2. **`range_diff` CÓ DẤU**, tính thẳng từ `map_image`, KHÔNG lấy lại `discrepancy_image`
   (hàm đó `fabs()` nên mất dấu). Dương = vật gần hơn bản đồ (nghi động); âm = xa hơn
   (che khuất / bản đồ thiếu dữ liệu).
3. **Ô TRỐNG chứ không phải `inf`** khi bản đồ không có dữ liệu tại pixel. pandas đọc ô
   trống thành NaN; ghi `inf` thì numpy nuốt vào rồi phá mọi phép trung bình về sau.
4. **Một dòng = một ĐIỂM, không phải cặp (điểm, thang)** — chống rò rỉ dữ liệu. Trải theo
   cặp thì mỗi điểm thành 4 dòng gần giống hệt, chia train/test ngẫu nhiên đẩy 3 dòng sang
   tập học và 1 sang tập kiểm tra; mô hình trả lời đúng bằng TRÍ NHỚ, điểm kiểm tra đẹp giả,
   và không ai phát hiện cho tới khi con số đã nằm trong báo cáo.

### 14.2. Bước 2: `scripts/add_gt_label.py`

Ghép cột `gt_label` từ file `.label`. Quy ước chép nguyên `evaluate.py`
(`semantic = label & 0xFFFF`, động ⇔ thuộc `{252..259}`) để hai bên không thể lệch.

```bash
python3 scripts/add_gt_label.py scan150.csv 04     # -> scan150_gt.csv
```

Ba phép kiểm tự động, sai là DỪNG chứ không ghi file hỏng: (1) số dòng khớp số điểm trong
`.label`; (2) `point_id` liên tục 0..n-1; (3) tái lập P/R/F1 rồi đối chiếu `evaluate.py`.

### 14.3. Kiểm chứng — hai đường độc lập trùng khít

| | log C++ | CSV |
|---|---|---|
| số dòng | 126.941 | 126.941 ✅ |
| tổng `is_ground` | 63.156 | 63.156 ✅ |
| tổng `baseline_label` | 1.445 | 1.445 ✅ |

| | `add_gt_label.py` | `evaluate.py` |
|---|---|---|
| TP / FP / FN | 301 / 1.144 / 69 | 301 / 1.144 / 69 ✅ |
| P / R / F1 | 0.208 / 0.814 / 0.332 | 0.208 / 0.814 / 0.332 ✅ |

**Hồi quy:** `dynamic points = 1445` trước và sau khi thêm hàm xuất — không xê dịch. File
14MB cho một scan.

### 14.4. ⭐ Bảng chéo — CSV quy được trách nhiệm cho từng lỗi

```
is_ground | baseline | gt |   so dong | y nghia
----------|----------|----|-----------|------------------------------------
    0     |    0     | 0  |    62.275 | ca hai noi tinh — dung
    0     |    0     | 1  |        65 | FN: nguong bo sot
    0     |    1     | 0  |     1.144 | FP: nguong bao nham
    0     |    1     | 1  |       301 | TP: nguong bat dung
    1     |    0     | 0  |    63.152 | mat duong, dung la tinh
    1     |    0     | 1  |         4 | ** LUAT GROUND NUOT OAN diem dong that **
```

`65 + 4 = 69` = đúng FN mà `evaluate.py` báo. Nhưng `evaluate.py` chỉ nói "69", còn CSV
**tách được** thành 65 do ngưỡng bỏ sót và 4 do luật ép ground nuốt.

Đây chính là lý do phải ghi cả điểm ground kèm cột `is_ground` thay vì lọc bỏ cho gọn: xoá
đi thì 4 dòng kia biến mất và không bao giờ đo được cái giá của luật ground nữa.

### 14.5. Còn lại của mạch CSV

| Bước | Trạng thái |
|---|---|
| 1. C++ xuất CSV một scan | ✅ xong |
| 2. Python ghép `gt_label` | ✅ xong |
| 3. Chạy nhiều scan rồi nối lại | ❌ chưa |
| 4. MLP numpy, so với `baseline_label` | ❌ chưa |

Bước 3 nên dùng **bộ 5 scan chuẩn của từng sequence** (giao thức §16.1 phiên 13/8, danh sách
ở `HANDOFF_2026-08-20.md` mục 2-3) để mọi phép đo so cặp được với các mốc cũ.

---

## 15. KHUNG NGHIÊN CỨU + LỊCH 4 TUẦN — chốt tối 01/09 (phiên Windows, không đụng code)

> Mục này sinh ra từ một buổi bàn hướng, **không có phép đo mới nào**. Mọi con số trích lại từ
> mục 11–14 ở trên. Viết để phiên Linux tới không phải bàn lại từ đầu.
>
> **Ràng buộc mới:** còn ~1 tháng là phải **nộp đề tài NCKH**. Nộp *thuyết minh* (tên, tính cấp
> thiết, mục tiêu, phạm vi, phương pháp, kết quả **dự kiến**, tiến độ) — **không phải nộp kết quả**.
> Việc của tháng này là: có đủ kết quả sơ bộ để thuyết minh không rỗng, và **hứa ra đủ khiêm tốn
> để tháng sau không vỡ**.

### 15.1. Câu hỏi đã đặt: có làm thẳng trên Go2 luôn được không?

**Kết luận: không, không theo đường hiện tại.** Chi phí trước khi viết được dòng học máy đầu tiên:

| Việc | Vì sao bắt buộc | Ước |
|---|---|---|
| Reader ROS2 `.db3` sang cloud | repo chỉ nhận `<pcd_dir> <poses.txt> <calib.txt>` (mục 10.1) | 2–4 ngày |
| **Nguồn pose từng frame** | KITTI có `poses.txt`; bag không có. Phải chạy SLAM **rồi kiểm nó** | 1–2 tuần |
| **Nhãn động/tĩnh** | MLP là học **có giám sát**: không nhãn thì không train, và **không đo** được | 2+ tuần gán tay |
| `intensity` xuyên suốt | đang bị vứt ở `bin_to_pcd.py`, phải đổi `PointXYZ` sang `PointXYZI` mọi module (10.2) | 2–3 ngày |

Cộng lại ~1 tháng, mà ô pose thì dự án đã trượt nhiều tháng. **Ô nhãn là ô không rút ngắn được
bằng cách cố gắng hơn** — đó mới là chỗ chặn thật.

### 15.2. ⭐ CỬA HẸP CHƯA KIỂM — hai bag `standstill_*`. Kiểm cái này TRƯỚC

Nếu **cảm biến đứng yên** thì hai blocker to nhất biến mất cùng lúc:

- **Pose = ma trận đơn vị.** Không cần SLAM, không cần FAST-LIO2, không cần kiểm odometry.
- **Nhãn gần như miễn phí.** Cảm biến bất động nghĩa là nền tĩnh *lặp lại qua thời gian*. Lấy
  **trung vị range của từng pixel qua 2.577 khung** ra nền; khung nào lệch khỏi nền là vật động.
  Đây là *background subtraction* kinh điển, viết bằng numpy trong một buổi.
- Nhiễu tự phản xạ vẫn gán được bằng hình học (bán kính thân robot) như mục 3 đã ghi.

**Câu hỏi phải trả lời, rẻ, làm trong nửa buổi:**

> Trong `~/dataset/standstill_multifeature_20260810_140258/` (304M, 167,5s, **2.577
> `sensor_msgs/msg/PointCloud2`**) — **có người hay vật nào đi ngang qua không?**

Chữ *"multifeature"* trong tên gợi ý là có. Cách kiểm: đọc vài chục khung cách đều, dựng range
image, lấy trung vị theo thời gian rồi đếm pixel lệch mạnh. Không cần dựng cả pipeline.

| Kết quả kiểm | Hệ quả |
|---|---|
| **CÓ vật động** | Có tập Go2 **có nhãn, không cần SLAM, không cần gán tay**. Go2 từ 2 tháng xuống vài ngày. Đổi hẳn cục diện |
| **KHÔNG có** | Đóng cửa này lại, theo đúng ba tầng ở 15.3, Go2 chỉ định tính |

### 15.3. Ba tầng — Go2 KHÔNG nằm trên đường găng

| Tầng | Làm gì | Vì sao | Rủi ro |
|---|---|---|---|
| **1. Bắt buộc** | KITTI seq04/06: MLP vs 3 baseline, chia train/test **theo sequence** | Có nhãn nên có số, có số mới có bài | thấp |
| **2. Rẻ, cứu tên đề tài** | **Hạ cấp KITTI thành LiDAR rẻ giả lập**: chọn 16 rồi 8 tia trong 64, thêm nhiễu range | Chỗ **chứng minh luận điểm trung tâm** | thấp |
| **3. Không hứa trong thuyết minh** | Go2: định tính (ảnh trước/sau); định lượng **chỉ khi** 15.2 mở cửa | Nối với phần cứng thật | cao |

### 15.4. Tầng 2 — vì sao đây là thí nghiệm quan trọng nhất

Tên đề tài có chữ **Low-Cost**, mà KITTI chạy **Velodyne HDL-64E** — thứ đắt nhất thị trường,
ngược hẳn. Cả bài chỉ có KITTI thì đúng một câu hỏi là thủng.

Hạ cấp KITTI vá đúng lỗ đó, vì **nhãn được giữ nguyên** nên đo được đường suy giảm:

```
số tia:        64      32      16       8
ngưỡng cứng   F1 x    x−a     x−2a    sụp
MLP           F1 y    y−b     y−2b    còn đứng      (kỳ vọng: b < a)
```

**"Ngưỡng cứng tụt nhanh hơn khi cảm biến rẻ đi"** — đó chính xác là câu bài báo cần nói, chứng
minh bằng số, trên dữ liệu đã có, tốn khoảng một ngày Python. Không có thí nghiệm này thì chữ
*low-cost* chỉ là lời hứa.

### 15.5. ⚠️ BA BASELINE BẮT BUỘC — và một cái bẫy trong luật "không tune ngưỡng"

Luật *"không tune ngưỡng"* ở mục 2 đúng cho **giai đoạn gỡ lỗi**. Ở **bảng kết quả cuối thì luật
ngược lại** — phải cho baseline cơ hội tốt nhất, nếu không là so lệch kèo theo chiều ngược:

| Giai đoạn | Luật |
|---|---|
| Đang truy nguồn FP | ❌ không đụng τ — đúng như mục 2 |
| Bảng so sánh cuối | ✅ **phải có τ tốt nhất**, chọn trên tập train, đo trên tập test |

Mọi bảng kết quả phải có **ba** dòng baseline:

1. `τ = 0.5` (mặc định hiện tại)
2. **`τ* tốt nhất`** — quét trên tập train. Không có dòng này thì câu hỏi *"em thử τ khác chưa?"*
   làm sập bài ngay tại chỗ.
3. **`τ thích nghi theo thống kê từng scan`** — ví dụ `trung vị + k×MAD` của `range_diff` trong
   chính scan đó. Đây cũng là *ngưỡng thay đổi theo dữ liệu* mà **không cần học máy**, viết 5 dòng
   pandas. Nếu MLP không thắng nổi nó thì phải biết **sớm**, không phải lúc đứng trước hội đồng.

⭐ **Quét τ KHÔNG cần chạy lại C++.** CSV đã có `range_diff_L0..L3` và `observed_L*`, chép luật
vote/revert sang pandas là quét được cả dải τ trong vài giây.
**Cách tự kiểm bắt buộc:** đặt `τ = 0.5` phải tái lập **đúng 1.445 điểm** và **301 / 1.144 / 69** —
trùng thì mới tin phần còn lại của đường quét (cùng tinh thần kiểm chéo ở mục 14.3).

### 15.6. Giao thức đo — chốt trước khi train, đổi sau là chạy lại hết

- **Chia tập theo sequence, KHÔNG theo điểm.** Mục 14.1 đã chống rò rỉ ở mức *(điểm, thang)*,
  nhưng chia ngẫu nhiên theo điểm **trong cùng một scan vẫn rò**: điểm kề nhau gần như trùng đặc
  trưng. Chốt: **train seq04, test seq06**. Đây cũng chính là bằng chứng rẻ nhất cho chữ
  *"thích nghi"*, không tốn dữ liệu mới.
- **Mất cân bằng 0,29%** — seq04/150 chỉ có **370 điểm động thật trên 126.941**. Train thẳng thì
  cách "sai ít nhất" là **đoán tĩnh cho tất cả**: đúng 99,7%, mô hình rỗng tuếch.
  Nên: **không dùng accuracy**. Dùng **PR-AUC**, và so với baseline **ở cùng mức recall** (hoặc
  cùng số FP), không so F1 trần trụi. Thêm class weight hoặc lấy mẫu lại khi train.
- **Trần recall 98,9%.** Luật ground nuốt **4/370** điểm động thật (bảng chéo 14.4). ML chạy sau
  ground filter thì không bao giờ vượt được trần đó — phải ghi vào bảng, đừng để bị hỏi.

### 15.7. Khung khái niệm để viết bài và trả lời hội đồng

- **Ngưỡng cứng chính là một nơ-ron có trọng số đặt tay:** `s = 1×diff − 0.5`, `s > 0` là động.
  Học máy nghĩa là để dữ liệu tìm hai con số đó thay vì người gõ vào.
- **MLP không đi tìm một con số tốt hơn.** Với `w₁·d + w₂·r + b > 0` thì
  `d > (−w₂·r − b)/w₁` — **ngưỡng trở thành một HÀM của ngữ cảnh** `τ(cự ly, thang, mật độ,
  ground…)`. Lớp ẩn cộng ReLU biến `τ(·)` từ đường thẳng thành đường gấp khúc.
  ⚠️ Không có phi tuyến giữa hai lớp thì chồng bao nhiêu lớp cũng vẫn là một nhát cắt thẳng —
  **câu này thầy rất hay hỏi**.
- **Hai nghĩa của "thích nghi", đừng trộn:**

| | Nghĩa | Khả thi |
|---|---|---|
| (i) | τ đổi theo **ngữ cảnh từng điểm** trong cùng loại dữ liệu | ✅ đây đúng là MLP — **khẳng định cái này trong thuyết minh** |
| (ii) | Train KITTI rồi **thả sang Livox L1 của Go2** | ⚠️ *domain shift*, **không miễn phí**. Ghi Future work, đừng hứa |

Đường đi cho (ii) khi tới lúc, rẻ trước đắt sau: **đặc trưng không thứ nguyên** (`range_ratio`,
hoặc chia lệch cho độ phân giải góc tại cự ly đó) → cross-sequence → fine-tune lớp cuối bằng vài
trăm nhãn Go2 → nhãn tự phản xạ theo hình học (mục 3). Dự án đã dính domain shift một lần rồi:
cảnh báo *"không generalize"* ở mốc F1 0.720, `HANDOFF_2026-08-13.md`.

### 15.8. Lịch 4 tuần (đã trừ hao việc môn khác: M3 trước 06/09, 3 bài KT 8086 tuần 3, vấn đáp)

| Tuần | Việc | Ra cái gì |
|---|---|---|
| 1 | Khám nghiệm FP (15.9) + **nửa buổi kiểm bag standstill (15.2)** + bước 3 gộp nhiều scan | baseline đáng tin |
| 2 | MLP numpy + 3 baseline (15.5) + chia theo sequence | **bảng số đầu tiên** |
| 3 | Tầng 2 — hạ cấp KITTI, bảng suy giảm theo số tia | **luận điểm trung tâm có bằng chứng** |
| 4 | Viết thuyết minh. Go2 để ở mục *khảo sát ban đầu* | nộp đề tài |

### 15.9. VIỆC ĐẦU TIÊN CỦA PHIÊN LINUX TỚI — khám nghiệm FP bằng CSV, không đụng C++

Câu hỏi cần đóng: **FP đến từ LUẬT QUYẾT ĐỊNH hay từ ĐƯỜNG ỐNG?** Phân biệt này quyết định bài
đứng hay đổ, vì MLP ăn **cùng bộ đặc trưng** sinh ra từ cùng đường ống:

> Nếu FP dồn ở vùng xa, MLP sẽ học được luật *"range > 30 thì tĩnh"*, precision nhảy vọt, bảng đẹp
> — nhưng cái nó học **không phải phân biệt động/tĩnh, mà là một cái cổng khoảng cách**. Hội đồng
> hỏi *"vậy chỉ cần cắt bán kính là baseline cũng lên phải không?"* là bài sập trong một câu.

Ba bảng `groupby` trên `scan150_gt.csv` — CSV tối nay **chính là dụng cụ khám nghiệm**:

| Bảng | Đọc ra gì |
|---|---|
| **1. FP theo vành đai 10m** | trên 70% trong 1.144 FP nằm ngoài 30m còn TP dồn trong 30m nghĩa là nghi phạm 13.4 đúng, tức **đường ống** |
| **2. FP theo số thang bỏ phiếu** (`vote_L0..L3`) | ⭐ bảng quyết định. Cả 4 thang xác nhận nghĩa là lệch **nhất quán**, tức hình học/đường ống. Chỉ lệch ở thang mịn nghĩa là nhiễu quanh ngưỡng, tức **luật quyết định** |
| **3. dấu của `range_diff` ở FP** | dương lệch hẳn là che khuất/parallax (đường ống). Đối xứng quanh 0 là nhiễu ngưỡng |

Tỷ lệ REVERT 89,5% (13.809 ứng viên còn 1.445) sẽ tự sáng nghĩa khi có bảng 2.

**Giới hạn cứng: MỘT phiên.** Không mở ngỏ. Hết phiên, hai nhánh:

- **Ra đường ống** → vá đúng chỗ đó **vào baseline** (cắt bán kính, hoặc đòi mật độ map tại pixel),
  đo lại, rồi đi bước 3. Mất cái "gain" giả, nhưng phần còn lại là thật. **Baseline mạnh lên là bài
  khoẻ hơn**, không phải yếu đi.
- **Ra luật quyết định** (FP rải đều, chỉ lệch ở thang mịn) → hết việc đào, đi thẳng bước 3–4, và
  câu chuyện đã có sẵn: *"ngưỡng cố định không sống nổi khi mật độ lấy mẫu đổi theo cự ly"*.

**Ba điều kiện để tuyên bố "baseline đủ tử tế, dừng đào":** (1) mỗi cụm FP lớn gọi tên được thuộc
đường ống hay luật quyết định — cần **biết**, không cần sửa hết; (2) mọi cái vá hiển nhiên mà một
người cài Removert tử tế đương nhiên làm (giới hạn bán kính, ground, voxel) đã nằm trong baseline,
vì MLP học được chúng miễn phí; (3) **ổn định và giải thích được, không cần đẹp** — P 0.2 kèm câu
chuyện có bằng chứng thì đăng được, P 0.2 kèm *"chưa rõ vì sao"* thì không.

### 15.10. Phương án B — chỉ dùng nếu hội đồng bắt buộc phải có phần cứng thật

Thì **đổi đề tài, đừng đổi lịch.** Đề tài khả thi trong 1 tháng trên chính bag standstill:
*"Đặc tả nhiễu tự phản xạ trên LiDAR giá rẻ và bộ lọc hình học"* — không cần SLAM, không cần gán
nhãn tay, và **87–92%** là con số rất đáng kể mà chưa ai trong lớp có. Kiểu *characterization*,
ít hào nhoáng hơn nhưng chắc chắn về đích.
