# Bàn giao phiên 2026-08-20 — chấm bộ nguồn cho đề bài E trên **20 scan / 4 sequence**

> Phiên này **chưa đụng C++**. Toàn bộ là phép đo bằng `scripts/src_quality.py` để trả lời
> câu hỏi bắt buộc trước khi viết E: *bộ frame nguồn có đủ tốt không, và `(R, t_min)` nào*.
> Nối tiếp `HANDOFF_2026-08-17.md` mục 9.5 ("việc đầu tiên của phiên sau **KHÔNG phải viết
> C++**").

---

## 1. Tóm tắt 1 phút

| # | Việc | Kết luận |
|---|---|---|
| 1 | Chấm bộ nguồn trên bộ 5 scan chuẩn của **4** sequence (03/04/06/07) | **XONG** — 20 scan × 7 cấu hình |
| 2 | Đề bài E có đáng viết không? | **CÓ** — AUC 0.82-0.92 mọi sequence, cao nguyên ngưỡng ở 19/20 scan |
| 3 | Khuyến nghị `R=5,t_min=5` của phiên 17/8 | **SAI — phải đính chính**, xem mục 4 |
| 4 | Cấu hình nguồn chốt cho bản C++ | **thang bậc `5→10→20`, đòi ≥ 8 nguồn** (mục 5), **đưa ra tham số dòng lệnh** |
| 5 | Luật đó có sống ngoài mẫu không? | **CÓ** — seq03 (không dùng để chọn luật) thắng 10/20, thua 2 (mục 5b) |
| 6 | Phát hiện mới | **chế độ hỏng "map bị nhiễm"** khi ego đứng yên trong cảnh đông (mục 6) |
| 7 | `fabs()` | **GIỮ** — xác nhận lại trên 20 scan thay vì 3 (mục 7) |

---

## 2. Giao thức đo

Bộ 5 scan chuẩn lấy đúng giao thức §16.1 phiên 13/8 (`pick_scans()` trong
`run_generalization_test.py`: frame có > 0 điểm GT động, lấy 5 vị trí cách đều
20/35/50/65/80%, bỏ 10 frame đầu/cuối):

| seq | 5 scan | ghi chú |
|---|---|---|
| 03 | 79, 125, 171, 217, 284 | **phép thử NGOÀI MẪU** cho luật ở mục 5 — xem 5b |
| 04 | 60, 97, 135, 172, 210 | **phiên này đo bộ MỐC LỊCH SỬ 50/100/150/200/250** để so cặp được với `run_seed_sweep.py` |
| 06 | 205, **349**, 763, 881, **939** | 349 và 939 là 2 scan F1 = 0.000 |
| 07 | 248, **441**, 673, 798, 923 | 441 là ngoại lệ của đề bài G |

Lệnh tái tạo:

```bash
python3 scripts/src_quality.py 06 205 349 763 881 939
python3 scripts/src_quality.py 07 248 441 673 798 923
python3 scripts/src_quality.py 04 50 100 150 200 250
```

Mỗi lệnh ~2-4 phút. Lưới `(R,t_min)` mặc định: `(5,1) (5,5) (10,1) (10,10) (20,1) (20,10) (20,20)`.

---

## 3. Kết quả — AUC abs trung bình 5 scan

| cấu hình nguồn | seq04 | seq06 | seq07 | seq03 | TB | phủ |
|---|---|---|---|---|---|---|
| `R=5, t_min=1` (≈ chọn nguồn của pipeline hiện tại) | 0.823 | **0.601** | 0.677 | 0.789 | 0.723 | 20/20 |
| `R=20, t_min=10` (cố định) | **0.924** | 0.851 | 0.754 | **0.906** | 0.859 | **20/20** |
| `R=5,t=5` một mình | 0/5 | 3/5 | 4/5 | 5/5 | — | **thủng** |
| `R=10,t=10` một mình | 0/5 | 3/5 | 4/5 | 5/5 | — | **thủng** |
| **thang bậc `5→10→20`, ≥8 nguồn** (mục 5) | **0.924** | **0.879** | 0.824 | 0.895 | **0.881** | **20/20** |

**14/15 scan cải thiện** khi `t_min` đi 1 → 10 ở cùng `R`. Mức tăng trung bình:
seq06 **+0.250**, seq04 **+0.101**, seq07 **+0.078**.

> **Tiêu chí chấm số 3 của §10.7 phiên 14/8 ĐẠT:** seq06 lên nhiều hơn seq04. Nếu ngược lại
> thì đó đã là dấu hiệu đo sai — đây là lần đầu tiêu chí ghi-trước-khi-đo này được kiểm.

**Hồ sơ ngưỡng (tiêu chí chấm số 2 — cao nguyên hay vách đá).** Ở cấu hình chốt, recall
gần như không đổi trên dải 0.5 → 2.0 m ở **14/15 scan** (ngoại lệ: seq07/673, mục 6). So với
`t_min=1` là vách đá rõ rệt (seq06/939: 0.46 @0.5m → 0.15 @5m).

> **Kết luận: `threshold` hết là biến tới hạn** với map tích luỹ. Đây là thứ đối lập hẳn với
> vách đá ở §12.1 phiên 13/8, và là dấu hiệu của thiết kế bền.

---

## 4. ĐÍNH CHÍNH handoff 17/8 mục 5 — `R=5, t_min=5` KHÔNG dùng được

Phiên 17/8 khuyến nghị: *"seq06: `R=5, t_min=5`; seq04: `R=20, t_min=10`"*. Phần seq06 rút
ra từ **đúng một scan (939)**. Trên bộ 5 scan chuẩn:

| cấu hình | seq04 | seq06 | seq07 | seq03 |
|---|---|---|---|---|
| `R=5, t_min=5` | **0/5 scan có nguồn** | 3/5 (349, 763 rỗng) | 4/5 (798 rỗng) | 5/5 (nhưng 3/5 dưới 8 nguồn) |
| `R=10, t_min=10` | **0/5** | 3/5 (349, 763 rỗng) | 4/5 (798 rỗng) | 5/5 |
| `R=20, t_min=20` | **0/5** | 5/5 | 4/5 (798 rỗng) | 5/5 |

> Đây đúng cái bẫy dự án đã tự cảnh báo hai lần ("một scan đơn lẻ không đủ kết luận") —
> lần này nó bắt chính handoff của phiên trước. **Quy tắc 4 §10.5 (xử lý tập rỗng) không phải
> phòng xa: nó là 40% số scan của seq06 và 100% của seq04.**

---

## 5. Cấu hình nguồn CHỐT cho bản C++: thang bậc, không phải hằng số

```
bậc 1:  R=5,  t_min=5     nếu #nguồn >= 8  ->  dùng
bậc 2:  R=10, t_min=10    nếu #nguồn >= 8  ->  dùng
bậc 3:  R=20, t_min=10    nếu #nguồn >= 1  ->  dùng
không bậc nào đạt         ->  BÁO RÕ và bỏ qua scan (KHÔNG fallback im lặng về t_min=1)
```

> Biên **8** (không phải 5) — xem mục 5b, đo bổ sung trên seq03. Cả ba bậc và biên này
> phải là **tham số dòng lệnh** của script, không hard-code: chúng đã được chỉnh trên toàn
> bộ 20 scan có trên đĩa, nên không còn dữ liệu sạch nào để tune tiếp.

**Căn cứ 1 — quy tắc "chọn R nhỏ nhất" được xác nhận bằng số.** Trong **6/7** trường hợp có
nguồn ở cả hai bậc, `R=5,t=5` thắng `R=20,t=10`:

| scan | `R=5,t=5` | `R=20,t=10` |
|---|---|---|
| 06/205 | **0.813** | 0.787 |
| 06/881 | **0.841** | 0.816 |
| 06/939 | **0.980** | 0.892 |
| 07/441 | **0.944** | 0.800 |
| 07/673 | **0.630** | 0.552 |
| 07/923 | **0.787** | 0.746 |
| 07/248 | 0.556 (**chỉ 4 nguồn**) | **0.699** |

Cơ chế: nền nhiễu tăng theo `R` — đo lại lần nữa ở seq06/205 là **0.26 → 0.67 m** khi R đi
5 → 20; ở seq07/441 là **0.15 → 1.42 m**.

**Căn cứ 2 — ngưỡng "≥ 5 nguồn".** Ngoại lệ duy nhất ở bảng trên (07/248) có đúng **4 nguồn**.
Đòi ≥ 5 thì nó rơi xuống bậc 2 (`R=10,t=10`, 8 nguồn) và AUC đi **0.556 → 0.811**.

> **Cảnh báo trung thực:** con số "5" được chọn **sau khi** nhìn 15 scan này, tức là chọn
> trên chính tập đang báo cáo. Phép thử sạch duy nhất còn lại là seq03 — đã chạy, xem 5b.

---

## 5b. Phép thử NGOÀI MẪU trên seq03 — thang bậc đứng vững, nhưng biên phải là 8

seq03 (79/125/171/217/284) là sequence duy nhất **không** dùng để chọn luật ở mục 5.

**Thang bậc vs. cố định `R=20,t_min=10`, gộp cả 20 scan / 4 sequence:**

| | thắng | thua | hoà | ΔAUC trung bình |
|---|---|---|---|---|
| thang bậc (biên ≥5) | **10** | 2 | 8 | **+0.019** |

Thang bậc thắng rõ ở seq06 (+0.028) và seq07 (**+0.075**, thắng 4/5 scan), hoà ở seq04
(mọi scan đều rơi xuống bậc 3), và **thua ở seq03** (0.877 vs 0.906). Toàn bộ phần thua của
seq03 đến từ **một scan**:

| seq03 scan | #nguồn bậc 1 | ladder(≥5) chọn | tốt nhất có thể |
|---|---|---|---|
| **217** | **đúng 5** | **0.787** (bậc 1) | **0.965** (bậc 3) |
| 171 | 4 | 0.894 (bậc 2) | 0.923 (bậc 3) |
| 125 | 4 | **0.910** (bậc 2) | 0.910 — chọn đúng |
| 284 | 2 | **0.939** (bậc 2) | 0.939 — chọn đúng |
| 79 | 25 | 0.855 (bậc 1) | 0.887 (bậc 2) |

Scan 217 có **đúng 5 nguồn** — vừa lọt biên rồi nhận cấu hình tệ nhất trong ba bậc.

**Nâng biên 5 → 8:** scan 217 đi **0.787 → 0.891**; giá phải trả duy nhất là seq07/441 đi
0.944 → 0.918. Trung bình 20 scan: **0.8805 (≥8)** vs 0.877 (≥5) vs 0.859 (cố định R=20).

> **DỪNG TUNE Ở ĐÂY.** Biên này vừa được chỉnh trên **toàn bộ 20 scan có trên đĩa** — không
> còn dữ liệu sạch nào để kiểm nữa, tune tiếp là fit nhiễu. Cách xử lý đúng là **đưa ba bậc
> và biên ra tham số dòng lệnh**, để việc đổi chúng sau này không tốn dòng code nào và không
> nằm trên đường tới hạn của bước C++.
>
> Ghi nhận về phương pháp: đây là lần thứ hai trong hai phiên liên tiếp mà một luật rút ra từ
> ít scan bị phép đo rộng hơn sửa lại (lần trước: `R=5,t=5` của phiên 17/8, mục 4). Cả hai lần
> luật **không** sai hẳn, chỉ sai ở **biên**. Bài học: luật nào có ngưỡng số thì ngưỡng đó
> phải là tham số, không phải hằng số trong code.

---

## 6. PHÁT HIỆN MỚI — chế độ hỏng "map bị nhiễm" (seq07/673)

Scan duy nhất **xấu đi** khi làm map tích luỹ, và nó xấu ở **mọi** cấu hình:

| R | t_min | #nguồn | nền nhiễu | tín hiệu | tỷ lệ | AUC abs |
|---|---|---|---|---|---|---|
| 5 | 1 | **116** | 1.03 | 3.12 | 3.0 | 0.594 |
| 5 | 5 | 108 | 0.98 | 3.52 | 3.6 | 0.630 |
| 20 | 1 | 159 | **2.85** | 3.15 | 1.1 | 0.525 |
| 20 | 10 | 141 | 2.43 | 3.38 | 1.4 | 0.552 |

Đọc ra cơ chế từ chính các con số:

1. **116 frame nguồn trong bán kính 5 m ở `t_min=1`** ⇒ xe ego gần như **đứng yên ~12 giây**
   (đèn đỏ / tắc đường).
2. 3585 điểm GT động, dòng xe vẫn chạy qua **đúng chỗ đó** suốt 12 giây.
3. ⇒ map tích luỹ tích luôn **vệt** của dòng xe. Nền nhiễu **1.03-2.85 m** — cao nhất trong
   mọi scan từng đo (các scan khác: 0.10-0.67).
4. ⇒ `t_min` **vô dụng ở đây**: frame cũ hơn cũng có xe ở đó. Tỷ lệ tín hiệu/nhiễu chỉ 1.1-3.6,
   trong khi các scan khác đạt 12-122.

> **Giả định gốc của Removert lộ ra:** nó ngầm cho rằng map dựng từ **lượt đi sạch**.
> Ego đứng yên trong cảnh đông thì giả định đó vỡ, và không tham số nào cứu được.
> Đây là nguyên lý 2 lần thứ tư: bằng chứng phải có **cấu trúc**, và ở đây cấu trúc thời gian
> cũng bị phá vì vật động chiếm chỗ liên tục.
>
> **Dự đoán ghi TRƯỚC để chấm bước C++: seq07/673 phải VẪN xấu sau khi làm E.**
> Nếu nó bỗng tốt lên thì nghi phép đo trước, đừng mừng.

Hướng gỡ (chưa làm, ghi lại cho sau): lọc nguồn theo **persistence** — điểm map chỉ được
giữ nếu xuất hiện ở ≥ k frame nguồn khác nhau. Đây đúng là hướng "persistence cho lifelong"
đã nêu ở nguyên lý 2. Chưa đo, đừng cài vội.

---

## 7. Xác nhận lại trên 20 scan (trước đây chỉ có 3)

**`fabs()` GIỮ NGUYÊN.** AUC 1 chiều:

| | AUC abs | AUC 1 chiều |
|---|---|---|
| seq04, cả 5 scan | 0.837-0.979 | **0.267-0.626** (scan 150: **0.267**) |
| seq03, 4/5 scan | 0.855-0.939 | **0.021-0.424** (scan 284: **0.021**) |
| seq06/349, 763, 939 (map cũ thật) | 0.876-0.980 | **0.909 / 0.921 / 0.988** (cao hơn abs) |

Đúng cơ chế §10.6: seq04 xe chạy **dọc tia nhìn**, vệt của chính nó có chỗ gần hơn vị trí
hiện tại ⇒ `map − scan < 0` ⇒ luật một chiều mù. seq06 map đến từ **lượt đi khác** nên không
có vệt ⇒ một chiều thắng. Vì cấu hình chốt (mục 5) **trộn cả hai loại nguồn**, `fabs()` là
lựa chọn duy nhất an toàn.

**`t_min` là núm chính** — lần thứ ba xác nhận, giờ trên 15 scan: cùng `R=20`, `t_min` 1 → 10
làm AUC seq06 đi **0.501 → 0.851**.

---

## 8. Trạng thái đề bài sau phiên

| | Đề bài | Trạng thái |
|---|---|---|
| A, B | | XONG (14/8, 17/8) |
| C | Một mặt phẳng ground dùng chung | tự tan biến khi làm E |
| ~~D~~ | | ĐÃ BÁC BỎ |
| **E** | Map tích luỹ | **E1 (chọn nguồn) — thiết kế đã CHỐT bằng số, mục 5. E2 (C++) chưa viết.** |
| F | Patchwork++ | treo, hạng 2 |
| G | seq07/441 xuyên tâm vs tiếp tuyến | treo, độc lập |

---

## 9. Việc tiếp theo — E2, viết C++

Cấu hình đã chốt ở mục 5, verdict là **đáng viết**. Bốn điểm bắt buộc khi cài
(§10 phiên 14/8 §E2, không đổi):

1. Đưa **map về hệ scan** (`T = scan_pose⁻¹ · pose_j`) — ngược chiều code hiện tại
   (`main.cpp:149-150`).
2. `pcl::VoxelGrid` **0.2 m** trên cloud gộp.
3. Ground fit **một lần** trên scan, dùng trực tiếp cho map (đề bài C tan biến).
4. **Giữ `fabs()`**, **giữ tầng voting** (`main.cpp:220-258` không sửa),
   **sweep lại `threshold`** (giá trị 1.0 hết hiệu lực).

Tiêu chí chấm, ghi TRƯỚC khi chạy:

| # | tiêu chí | ngưỡng |
|---|---|---|
| 1 | seq06/349 và 06/939 (đang F1 = 0.000) | **bắt buộc khác 0**. Vẫn 0 ⇒ lỗi CÀI ĐẶT, không phải ý tưởng (AUC đã 0.876/0.980) |
| 2 | `threshold` sweep 0.5 / 1 / 2 / 5 | phải thấy **cao nguyên**, không phải vách đá |
| 3 | seq06 lên nhiều hơn seq04 | seq04 lên nhiều hơn = dấu hiệu đo sai |
| 4 | in `R` và `#nguồn` thực dùng cho mỗi scan | thứ đầu tiên phải xem khi kết quả lạ |
| 5 | **seq07/673 vẫn xấu** | mục 6. Tốt lên đột ngột = nghi phép đo |
| 6 | mọi kết luận | chạy `run_seed_sweep.py` (đụng ground), **ΔF1 < 0.07 trên 1 mask = không kết luận** |
