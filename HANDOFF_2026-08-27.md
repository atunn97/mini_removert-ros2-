# Bàn giao phiên 2026-08-27 — E1 xong, E2 xong việc 1+2 (gộp nguồn + voxel)

> Nối tiếp `HANDOFF_2026-08-20.md` mục 9. Nhánh: `e2-map-tich-luy`.
>
> **Trạng thái E2: việc 1 + 2 XONG và đã đối chiếu (mục 4c). Việc 3 + 4 CHƯA LÀM.**
> Hệ quả phải nhớ trước khi chạy bất cứ thứ gì: binary hiện tại vẫn còn dòng
> `scan_in_map_frame` cũ, giờ áp ma trận "map → scan" lên chính scan — **mọi con số F1 nó
> in ra đều là RÁC** cho tới khi việc 4 xong. Đừng đọc, đừng ghi vào đâu.

---

## 1. Tóm tắt 1 phút

| # | Việc | Kết luận |
|---|---|---|
| 1 | Tái lập mốc regression TRƯỚC khi đụng gì | **XONG** — 0.707 ± 0.023, khớp từng chữ số (mục 2) |
| 2 | `scripts/select_source_ladder.py` (E1) | **XONG** — 3 điểm dừng + 6 phép thử biên đều đạt (mục 3) |
| 3 | Phát hiện: seq06/939 nằm **đúng trên biên** `min_src=8` | `>=` vs `>` đáng 0.088 AUC (mục 3.2) |
| 4 | Hướng "gộp map cũ + map tích luỹ" | **HOÃN — có cơ chế phản đối bằng số** (mục 4.1) |
| 5 | Hướng "chọn nguồn một phía" | **CHƯA ĐO — không phải bãi bỏ**, và là điều kiện bắt buộc cho real-time (mục 4.2) |
| 6 | `run_seed_sweep.py` nối vào selector mới | **CHƯA LÀM** — cố ý, xem mục 5.2 |

---

## 2. Mốc regression, đo lại trên code chưa đụng E2

`python3 scripts/run_seed_sweep.py` (seq04, 5 scan mốc × 6 ground mask):

```
trung binh cua cac mask : 0.707
do lech chuan giua mask : 0.023
min / max               : 0.657 / 0.723   (bien do 0.066)
P / R trung binh        : 0.679 / 0.746
seed 0 (moc lich su)    : 0.720
```

Khớp **từng chữ số** với mốc trong `CLAUDE.md`. Nghĩa là binary vẫn tiền định, dữ liệu không
xê dịch, và mọi ΔF1 < 0.066 đo trên MỘT mask trong phiên sau là nhiễu.

Chi tiết đáng nhớ trong bảng: **scan 250 đi 0.526 (seed 0) → 0.713 (seed 5)**, biên độ
**0.187** — còn rộng hơn scan 150 (0.182). Đừng dùng scan 250 một mình để chấm E2.

---

## 3. E1 — `scripts/select_source_ladder.py`

### 3.1. Nó làm gì

Thay `select_maps_by_distance.py` cho đề bài E. Lấy **mọi** frame thoả điều kiện, theo thang
bậc nới dần; ba bậc và biên đều là **tham số dòng lệnh** (`--rungs`, `--min-src`), đúng yêu
cầu mục 5b phiên 20/8. Giữ nguyên contract cũ: `map_idx` ở **dòng cuối stdout**.

Biên `--min-src` áp cho mọi bậc **trừ bậc cuối** — bậc cuối là lưới an toàn, chỉ đòi ≥ 1.
Áp cho cả bậc cuối thì scan nghèo nguồn rơi thẳng xuống "không bậc nào đạt", mất trắng
những scan mà bậc cuối vẫn cứu được.

Ba điểm dừng kiểm tra, chạy trên dữ liệu thật:

| scan | bậc chọn | #nguồn | khớp handoff |
|---|---|---|---|
| seq06/939 | **1** (R=5, t=5) | 8 | mục 5 (AUC 0.980) |
| seq04/150 | **3** (R=20, t=10) | 9 | mục 4 — thủng cả bậc 1 lẫn bậc 2 |
| seq03/217 | **2** (R=10, t=10) | 11 | mục 5b — 5 nguồn ở bậc 1, không đủ 8 |

Sáu phép thử biên đều đạt: `--min-src 5` kéo seq03/217 về bậc 1 · `tail -1` ra đúng dãy
`map_idx` · cờ gõ nhầm / thiếu giá trị / `--rungs` sai định dạng / `scan_idx` ngoài dải đều
báo lỗi và thoát mã 1 · **không bậc nào đạt thì stdout KHÔNG có dòng số nào**, nên script
gọi nó không thể nhặt nhầm danh sách rỗng rồi chạy tiếp.

### 3.2. Phát hiện: seq06/939 nằm ĐÚNG trên biên

Bậc 1 của scan đó ra **đúng 8 nguồn** — bằng chằn chặn `--min-src 8`. Viết `>= 8` thì nó
lấy bậc 1 (**AUC 0.980**); lỡ viết `> 8` thì rơi xuống bậc cuối (**AUC 0.892**). Một ký tự,
chênh 0.088. Đã ghi comment cảnh báo ngay tại dòng đó trong `pick_rung()`.

### 3.3. Quan sát: bộ nguồn của seq06/939 toàn là QUÁ KHỨ

`[104 105 106 107 108 109 110 111]` cho scan 939 — cách 833 frame, **một phía hoàn toàn**.
Thứ chọn ra bộ này là bán kính `R`, không phải `t_min`: nó bắt được một **lượt đi khác** qua
cùng con phố 83 giây trước. Xem hệ quả ở mục 4.2.

Ngược lại seq04/150 chọn `[136…140, 160…163]` — **cả hai phía**. Nên "E2 luôn một phía" là
SAI: nó một phía khi có loop closure, hai phía khi không.

---

## 4. Hai hướng đã cân nhắc trong phiên này

### 4.1. GỘP cách cũ (nhiều map rời + voting) với map tích luỹ — HOÃN

**Ý tưởng:** giữ nguyên 4 map lân cận đang chạy, cộng thêm map tích luỹ như một nguồn bằng
chứng nữa, để hai cách bù cho nhau.

**Vì sao hoãn — cơ chế, không phải cảm tính.** Bảng mục 6.3 phiên 14/8, trung vị `(map−scan)`
tại đúng pixel của điểm động, seq06/939:

| map | 106 (cách 833 frame) | 936 (cách 3) | 941 (cách 2) | 942 (cách 3) |
|---|---|---|---|---|
| trung vị | **17.45 m** | 0.93 | 0.01 | 0.17 |
| % vượt thr=1.0 | **100.0%** | 45.2% | 41.0% | 50.7% |

Tầng voting cho **mỗi map một phiếu ngang nhau** (`vote_count/observed_count > 0.5`). Gộp
một map hoàn hảo (100%) với ba map gần mù (41–51%) ra tỷ lệ ~0.59 — lơ lửng sát vạch 0.5.
**Map gần KÉO TỤT phiếu của map xa, chứ không bổ sung.** Gộp ở đây là **pha loãng**, không
phải cộng thêm.

Muốn gộp thật thì map tích luỹ phải có **trọng số cao hơn** — mà trọng số đó chỉ có thể tune
trên chính 20 scan đã dùng hết. Đó là fit nhiễu (mục 5b phiên 20/8).

**Điều kiện để bàn lại:** làm E2 một mình trước, rồi đo **hai cách có flag trùng điểm không**.
Trùng phần lớn ⇒ gộp không thêm gì, đóng hướng này lại. Khác nhau nhiều ⇒ mới đáng thiết kế
luật gộp có trọng số. Làm cả hai cùng lúc thì F1 nhúc nhích cũng không quy tội được cho cái
nào — nhất là dưới quy tắc ΔF1 < 0.07.

### 4.2. CHỌN NGUỒN MỘT PHÍA (chỉ quá khứ) — CHƯA ĐO, KHÔNG PHẢI BÃI BỎ

**Ghi rõ để phiên sau không đọc nhầm: hướng này chưa hề bị bác bỏ.** Nó chưa được đo.

Ba việc đã biết:

1. **Phép đo gần nhất thì ngược dấu.** Ràng buộc `|map_idx − scan_idx| ≤ 50` (mục 6.2 phiên
   14/8) làm seq06 tụt **0.145 → 0.068** — mất một nửa. Đó KHÔNG phải đề xuất một phía, nhưng
   nó cho thấy trực giác "map gần thì tốt hơn" sai dấu ở sequence có loop closure.
2. **Một phía không phải chuyện tối ưu, mà là ràng buộc kiến trúc.** Chạy cạnh FAST-LIO trên
   Go2 thì frame tương lai **chưa tồn tại** — muốn có phải chịu độ trễ vài frame
   (`HANDOFF_2026-08-13.md` mục 4.2b). Đây là thứ đứng giữa dự án và mục tiêu gắn vào
   pipeline chính.
3. **E2 về nguyên tắc chạy được một phía, luật ANTI thì không** — ANTI đòi map cả hai phía
   mới có dấu để đảo. Đó là một lý do nữa để ANTI đã bị bác bỏ còn E2 thì không.

**Muốn chốt thì phải đo, không suy ra được:** thêm điều kiện `j < scan` vào `select_sources()`
rồi chạy lại `src_quality.py` trên bộ 5 scan chuẩn của 4 sequence. Trước khi có số đo đó,
đừng ghi nó vào danh sách "đã bác bỏ" mà cũng đừng cài vội.

### 4.3. Nhắc lại cái đã bác bỏ, để khỏi đề xuất vòng ba

`~~Thêm ràng buộc thời gian cho select_maps_by_distance.py~~` — đã đo, seq06 tụt một nửa
(mục 6.2 phiên 14/8). Sửa selector CŨ theo bất kỳ hướng nào cũng là sửa một thứ sắp bị thay.

---

## 4b. Phần cơ khí của E2 đã làm trong phiên này

Chia đôi công việc E2: phần dưới đây đã xong, phần lõi trong `main.cpp` (gộp nguồn về hệ
scan, voxel, bỏ vòng lặp map) **chủ repo tự làm**.

**`CMakeLists.txt`** — thêm `filters` vào `find_package(PCL ... COMPONENTS)` cho
`pcl::VoxelGrid`.

**`ground_filter`** — thêm hai hàm, **giữ nguyên `detectGroundMask` không đụng một chữ**:

- `detectGroundPlane(cloud, seed, thr)` → `Eigen::Vector4f` đã chuẩn hoá. Ném
  `std::runtime_error` nếu RANSAC không ra mặt phẳng — không trả (0,0,0,0), vì mặt phẳng
  rỗng sẽ làm `maskFromPlane` gán TOÀN BỘ điểm là ground, tức mọi điểm bị ép static, ra
  F1 = 0 mà không kêu tiếng nào.
- `maskFromPlane(cloud, plane, thr)` → `|n·p + d| <= thr`. Trị tuyệt đối vì PCL trả pháp
  tuyến dấu tuỳ ý.

Phần RANSAC tách ra hàm dùng chung `segmentGround()` trong anonymous namespace, để hai hàm
public **không thể** lệch cấu hình.

**Kiểm chứng (2 phép đo, không phải suy luận):**

1. **Refactor không đổi hành vi cũ.** seq04/150, seed 0, cấu hình mốc: **F1 = 0.727**,
   ground mask 49.75% — trùng khít giá trị trong bảng mục 2.
2. **Đường ground mới trùng đường cũ TỪNG ĐIỂM.** Chương trình thử riêng trên seq04/150,
   seed 0/1/2: `detectGroundMask` và `maskFromPlane(detectGroundPlane(...))` lệch **0 điểm**.

> Điểm 2 ban đầu tôi ghi ngược trong header (dự đoán hai mask sẽ lệch nhẹ, vì tưởng bộ
> inlier của PCL tính trên mô hình trước khi refit). **Phép đo bác bỏ, và source xác nhận
> vì sao:** với `setOptimizeCoefficients(true)`, PCL refit hệ số rồi **chọn lại inlier**
> bằng `selectWithinDistance` trên hệ số mới
> (`/usr/include/pcl-1.14/pcl/segmentation/impl/sac_segmentation.hpp:124-125`), mà hàm đó
> với SACMODEL_PLANE chính là `|n·p + d| <= threshold`. Hai đường trùng nhau theo cấu tạo.
>
> **Hệ quả cho E2:** đổi sang đường ground mới KHÔNG tự nó làm F1 xê dịch. Nên khi E2 chạy
> và F1 đổi, nguyên nhân nằm ở map tích luỹ chứ không phải ở ground — **so sánh với mốc
> 0.707 ± 0.023 vẫn là so sánh CÓ CẶP**. Đây là thứ đáng tiền nhất của phép đo này.

---

## 4c. E2 việc 1 + 2 XONG (gộp nguồn về hệ scan, voxel 0.2m) — đã đối chiếu 2 sequence

Việc 3 (ground một mặt phẳng) và việc 4 (bỏ vòng lặp map) **chưa làm**. Binary hiện tại vẫn
còn dòng `scan_in_map_frame` cũ, giờ áp ma trận "map → scan" lên chính scan — **F1 nó in ra
là RÁC** cho tới khi việc 4 xong. `map_cloud` sau voxel cũng chưa ai dùng.

| scan | #nguồn | trước voxel | C++ sau voxel | `src_quality.py` | lệch |
|---|---|---|---|---|---|
| seq04/150 (bậc 3) | 9 | 1.141.089 | **118.574** | 118.575 | 1 điểm |
| seq06/939 (bậc 1) | 8 | 993.711 | **133.939** | 133.942 | 3 điểm |
| seq03/217 (bậc 2) | 11 | 1.384.704 | 119.364 | *(chưa đo)* | — |

Lệch 1–3 điểm là do PCL và numpy tính key voxel bằng hai đường khác nhau nên điểm nằm đúng
biên ô rơi khác phía. **Khớp ở mức này nghĩa là chiều biến đổi đúng.**

> **Bài học đo đạc, đáng nhớ:** số điểm **trước** voxel MÙ với lỗi sai chiều biến đổi — biến
> đổi cứng không tạo ra hay làm mất điểm nào, nên sai chiều vẫn ra đúng 1.141.089. Chỉ **tỷ lệ
> co lại sau voxel** mới tố cáo được: đúng chiều thì các frame chồng khít, còn ~10%; sai chiều
> thì văng lệch nhau, còn cao hơn hẳn. Chọn nhầm phép kiểm thì có chạy cũng không biết gì.

Tỷ lệ co lại tự nó cũng hợp lý: seq03/217 có 11 nguồn nên chồng nhau nhiều nhất (còn 8.6%),
seq06/939 chỉ 8 nguồn và đến từ **lượt đi khác** nên chồng ít hơn (13.5%).

**Số đáng nhớ nhất lấy được trong lúc đối chiếu** — seq06/939, cùng `R=5`, chỉ đổi `t_min`:

| | #nguồn | tín hiệu | tỷ lệ tín hiệu/nhiễu | AUC |
|---|---|---|---|---|
| `t_min=1` (≈ chọn nguồn của pipeline hiện tại) | 15 | 0.40 | 2.4 | 0.668 |
| `t_min=5` (bậc 1) | 8 | **22.30** | **121.9** | **0.980** |

Tín hiệu gấp **56 lần** chỉ vì đổi bộ frame nguồn, trên một scan có vật động chạy
**0.30 m/frame** — tức nằm sâu trong vùng "vật chậm" mà `CLAUDE.md` ghi F1 TB chỉ 0.148.
Ít nguồn hơn (8 < 15) mà tốt hơn hẳn: **chất lượng nguồn, không phải số lượng nguồn.**

### 4c.1. Lý do từng mảnh code của việc 2 (voxel)

**`#include <pcl/filters/voxel_grid.h>`** — `VoxelGrid` nằm ở thư viện `pcl_filters`, tách
riêng khỏi `pcl_common`. Đó cũng chính là lý do `CMakeLists.txt` phải thêm component
`filters`: thiếu header thì lỗi lúc biên dịch, thiếu component thì lỗi lúc **link**.

**`constexpr float VOXEL_LEAF_M = 0.2f`, đặt cạnh `v_angle_min`** — quy ước `CLAUDE.md`:
`main.cpp` giữ mọi hằng số tinh chỉnh. Không hard-code `0.2f` ba lần trong `setLeafSize`, vì
ba chỗ đó phải luôn bằng nhau (ô lập phương); tách hằng số thì không thể lệch. Giá trị 0.2
không tự do chọn: `scripts/src_quality.py` dùng `VOXEL = 0.2`, nên đổi số này là mọi mốc AUC
của phiên 20/8 hết đối chiếu được.

**Cloud đích riêng (`map_cloud`) thay vì lọc tại chỗ** — lọc tại chỗ *an toàn*, PCL có xử lý
`input == output` bằng cloud tạm (`/usr/include/pcl-1.14/pcl/filters/filter.h:126-134`).
Nhưng giữ cloud riêng thì còn **cả hai** con số trước/sau voxel để in ra — mà đó chính là
phép kiểm của bước này. Ghi đè lên `accumulated` là tự vứt mất bằng chứng.

**`setLeafSize` trước `filter`** — `filter()` mới là lúc thực sự chạy; mọi setter phải xong
trước đó. Gọi ngược thì lọc bằng leaf mặc định, không có lỗi nào báo.

**Khối voxel đặt SAU vòng lặp, không phải trong** — voxel từng frame rồi mới nối thì 9 điểm
gần trùng nhau vẫn sống sót, vì chúng nằm ở 9 cloud khác nhau, không ô nào gộp được chúng.
Nghe hợp lý ("gọn từng frame cho nhẹ") mà làm đúng việc cần làm không xảy ra.

**In cả hai số kèm phần trăm** — con số tuyệt đối sau voxel một mình không nói được gì.
**Tỷ lệ co lại** mới là thứ tố cáo sai chiều biến đổi (xem khung cảnh báo ở mục 4c).

---

## 5. Việc tiếp theo

### 5.1. E2 — viết C++ (bốn bước, mục 9 phiên 20/8 không đổi)

1. `CMakeLists.txt:8` thêm `filters` vào `find_package(PCL ... COMPONENTS)`. Thiếu là lỗi
   **lúc link** (`undefined reference to pcl::VoxelGrid<...>::applyFilter`), không phải lúc
   biên dịch.
2. Gộp nguồn về **hệ scan**: `T = scan_pose⁻¹ · (poses[j] · Tr)` — **ngược chiều** code hiện
   tại (`main.cpp:149-150`). Rồi `pcl::VoxelGrid` **0.2 m** trên cloud gộp.
   Kiểm: số điểm map sau voxel phải sát cột `#map_pt` của `src_quality.py`.
3. Ground fit **một lần** trên scan, dùng chung hệ số cho map. `ground_filter.cpp:24` đã tính
   `coefficients` rồi vứt đi — chỉ cần thêm đường trả nó ra. Cạm bẫy: PCL trả pháp tuyến
   **dấu tuỳ ý**, chỉ dùng `|n·p + d| <= 0.2`.
4. Scan **giữ nguyên hệ của nó** (bỏ `scan_in_map_frame`). **GIỮ tầng voting**
   (`main.cpp:218-258`): với 1 map thì vote/observed ∈ {0,1}, tỷ lệ 1 > 0.5 vẫn đúng, mà
   REVERT đa thang vẫn chạy qua đúng đường đó. Xoá là mất luật "mọi thang thô phải xác nhận"
   (F1 0.630 vs 0.492).

Tiêu chí chấm giữ nguyên 6 điểm ở mục 9 phiên 20/8. Quan trọng nhất vẫn là số 1:
**seq06/349 và 06/939 bắt buộc khác 0**; vẫn 0 ⇒ lỗi cài đặt, không phải ý tưởng.

### 5.2. Nối `run_seed_sweep.py` vào selector mới — LÀM SAU KHI E2 CHẠY

Sửa nhỏ, nhưng **cố ý chưa làm**: binary hiện tại dựng một range image cho **mỗi** map, mà
thang bậc cho tới **72 nguồn** ở seq06/939 bậc 3 — gấp 18 lần công việc, sweep từ ~2 phút
thành gần 40 phút. Với E2 thì không sao vì gộp một lần rồi chỉ dựng một ảnh.

Bốn chỗ phải sửa cùng lúc, thiếu chỗ nào cũng ra log nói dối:

| dòng | sửa |
|---|---|
| `:32-33` | xoá `N_MAPS='4'`, `MAX_DIST='4.0'` → thêm `RUNGS='5:5,10:10,20:10'`, `MIN_SRC='8'` |
| `:41-43` | đổi tên script + hai tham số vị trí đuôi thành `--rungs`/`--min-src`. Dòng poses/calib **giữ nguyên** |
| `:31` | `THRESHOLD='1.0'` **hết hiệu lực** với map tích luỹ — đưa ra thành `--threshold` |
| `:82` | header đang in `N=... max_dist=...`, đổi sang `rungs=... min_src=...` |

Thêm một cái bẫy ngầm: `select_maps` trả `None` khi returncode ≠ 0. Trước đây nghĩa là "script
lỗi"; giờ nghĩa là **"không bậc nào đạt"** — một kết quả hợp lệ. `np.nanmean` sẽ lặng lẽ bỏ
qua scan đó, nên nếu 2/5 scan bị từ chối mà bảng vẫn ghi "F1 TB tren 5 scan" thì con số ấy
**không so được với mốc 0.707**. Phải in rõ số scan thực sự vào trung bình.

### 5.3. Lỗi nhỏ phát hiện dọc đường — `src_quality.py --grid` không chạy

Docstring quảng cáo `python3 scripts/src_quality.py 04 150 --grid 5:1,10:5,20:10`, nhưng
dòng 302 lọc tham số bằng `args = [a for a in sys.argv[1:] if not a.startswith('--')]`:
nó bỏ chữ `--grid` mà **giữ lại giá trị** `5:1,10:5,20:10`, rồi ném vào `int()` làm scan
index → `ValueError`. Cùng lỗi này cũng dính `--seeds`.

Phiên 20/8 không gặp vì mọi lệnh trong mục 2 đều dùng lưới mặc định. Không chặn việc gì,
nhưng sửa trước khi cần tới, kẻo mất mười phút tưởng hỏng dữ liệu.

---

## 6. Trạng thái working tree khi đóng phiên — đọc trước khi gõ lệnh gì

**Chưa commit gì cả.** Nhánh `e2-map-tich-luy`, 6 file đang sửa dở + 2 file mới:

```
 M CMakeLists.txt                       filters component (xong)
 M include/ground_filter/ground_filter.hpp   2 hàm mới (xong)
 M src/ground_filter/ground_filter.cpp       segmentGround + 2 hàm mới (xong)
 M src/main.cpp                         E2 viec 1 + 2 (xong), viec 3 + 4 CHUA
 M scripts/select_source_ladder.py      E1 (xong) + guard --min-src
 M scripts/run_seed_sweep.py            SUA DO, DANG HONG - xem duoi
?? HANDOFF_2026-08-27.md
?? PLAN_2026-08-27.md
```

**Hai thứ đang hỏng, cố ý để nguyên:**

1. **`run_seed_sweep.py` chạy là `NameError`.** Dòng 82 vẫn in `N={N_MAPS} max_dist={MAX_DIST}`
   trong khi hai biến đó đã bị xoá. Và hai hằng mới đang mang **giá trị cũ**:
   `RUNGS = '4'` (phải là `'5:5,10:10,20:10'`), `MIN_SRC = '4.0'` (phải là `'8'`).
   Docstring dòng 39 cũng còn ghi tên script cũ.
   *Không sửa vội:* chưa xong việc 4 thì chạy sweep bằng nó là 72 map × binary cũ (mục 5.2).
2. **F1 của binary là RÁC** — dòng `scan_in_map_frame` cũ đang áp ma trận "map → scan" lên
   chính scan. Không crash, vẫn in ra số trông bình thường. Việc 4 xoá dòng đó.

**Việc đầu tiên của phiên sau:** E2 việc 3 (ground một mặt phẳng — gọi `detectGroundPlane`
trên scan, rồi `maskFromPlane` hai lần: scan và `map_cloud`), rồi việc 4 (bỏ vòng lặp map,
thay `scan_in_map_frame` bằng `scan_frame.cloud` ở cả 3 chỗ, **giữ nguyên tầng voting**).
Chi tiết ở mục 5.1.

Xong việc 4 mới có con số F1 đọc được, và lúc đó tiêu chí chấm số 1 là thứ xem đầu tiên:
**seq06/349 và 06/939 bắt buộc khác 0.**
